/**
 * 보물 색인이 몇 줄인가.
 *
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │ **수 하나다 — 글이 아니라 레포에 둘 수 있다.**                            │
 * └──────────────────────────────────────────────────────────────────────────┘
 *
 * 색인의 **글**은 게임 콘텐츠라 DB에만 있지만(`0041`) 「몇 줄인가」는 인쇄된 표의
 * 길이일 뿐이다 — 레벨 눈금·번영도 문턱과 같은 성격이다(구현 결정 42·284).
 *
 * 이 수가 필요한 까닭은 **표가 비어 있을 때도 칸이 서야 하기 때문**이다. 표를 아직
 * 안 넣었다고 찾은 보물을 적을 데가 없으면 안 된다(절대 원칙 3, 구현 결정 371과
 * 같은 결).
 */
export const TREASURE_COUNT = 75
