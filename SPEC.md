# 잠자는 사자 2호점 (Sleeping Lion II) — 설계 사양서 (SPEC)

> 이 문서는 이전 설계 논의를 이어받아 Claude Code(또는 다른 도구)에서 구현을 시작하기 위한 핸드오프 사양서입니다.
> 모든 항목은 사용자(형님)와의 논의로 확정된 결정이며, 미결정 사항은 문서 하단 `열린 질문`에 명시합니다.

---

## 0. 앱 이름 (확정)

- **한국어 간판:** 잠자는 사자 2호점 — Gloomhaven 시작 여관 '잠자는 사자(Sleeping Lion)'의 분점/아지트 컨셉. 우리 파티만의 공간이라는 인조크 톤.
- **영문 워드마크:** Sleeping Lion II
- **레포명:** `sleeping-lion-2`
- **코드 식별자 (PWA 앱ID·패키지명):** `sl2`

---

## 1. 앱 정체성 (가장 중요 — 오해 방지)

**이 앱은 두 축으로 구성된다.**

### 축 ① 캠페인 기록지 (영속) — 앱의 본체

**한 시나리오(전투)가 끝난 뒤의 정산 결과**를 기록·관리한다: 획득 골드, 얻은 퍽/특혜, 경험치, 레벨업, 아이템 변동, 시나리오 완료/잠금 해제, 파티 번영/명성 변화 등. "세션 사이의 영속 기록·성장 관리 도구"다.

이쪽이 IndexedDB 영속·서버 동기화·권한 모델의 대상이며, 이 문서의 5~8장은 전부 이 축에 대한 것이다.

### 축 ② 인게임 도구 패널 (휘발성) — 플레이 중 보조

게임 플레이 중에 쓰는 **작고 독립적인 도구 몇 개**를 제공한다.

**담는 그릇은 위젯 보드다** (화면 이름 **행낭**, 경로 `#/satchel`). 격자 위에 위젯을 놓고 크기를 조절하는 화면이며, 런처 앱이나 아이패드 위젯 편집 화면의 조작감을 기준으로 삼는다. 편집 모드와 플레이 모드가 있고 격자는 편집 모드에서만 보인다. 상세는 [`milestone/`](./milestone/)가 정본이다.

채택 도구는 **아래 다섯으로 한정**한다:

1. **원소 트래커** — 6원소의 강함→약함→소멸 하강을 라운드 버튼으로 처리
2. **공격 보정 덱** — 표준 덱 구성(수치·기호만)의 뽑기·셔플·리셔플 트리거
3. **주도권 정렬** — 입력된 이니셔티브 수치를 행동 순서로 정렬(동점 처리 포함)
4. **HP/XP 트래커** — 실물 다이얼을 대신하는 두 숫자 세기 (2026-08-02 추가)
5. **라운드 트래커** — 지금 몇 라운드인가. 넘기면 원소가 한 단계 내려간다 (2026-08-02 추가)

> **2026-08-02 개정 — 넷째 도구를 들인다.** 이전 문서는 셋으로 한정하면서 아래
> '범위 밖'에 **HP**를 함께 적었다. 그때 막으려던 것은 **본격 전투 트래킹** —
> 몬스터 개체를 세고 상태이상을 카운트다운하며 피해를 자동으로 적용하는 것 —
> 이었고, 그 선은 그대로다.
>
> HP/XP 트래커는 그것이 아니다. **실물 다이얼을 화면으로 옮긴 것**이며 원소
> 트래커와 성격이 같다 — 손으로 돌리던 것을 대신 세어줄 뿐이다.
>
> - **최대 체력을 모른다.** 클래스도 레벨도 알지 못하고 상한을 강제하지 않는다.
> - **피해를 적용하지 않는다.** 공격 결과를 계산해 깎지 않는다. 사람이 누른다.
> - **상태이상·몬스터는 여전히 없다.**
> - **인스턴스마다 값이 따로다.** 체력과 경험은 사람마다 다르다 — 식탁 위에
>   하나뿐인 원소판과 반대다.

> **2026-08-02 추가 — 라운드 트래커.** 판의 사실이므로 **행낭 전체에 하나뿐이다**
> (원소판과 같고, 사람마다 다른 HP/XP와 반대다). 넘기면 원소가 함께 내려간다 —
> 실물에서 한 동작이므로 갈라 두지 않는다. 12장 열린 질문 7을 여기서 닫는다.

**개발용 예외 — Test 위젯.** 배치를 시험하기 위한 빈 사각형 위젯 하나를 레지스트리에 둔다. 아무 기능이 없으며 위 '셋으로 한정' 원칙의 예외다. 실제 도구가 갖춰지면 뺄지 판단한다.

**축 ②의 절대 제약:**
- **도구 런타임 상태는 영속 저장하지 않는다.** 뽑은 카드·원소 상태 등은 메모리에만 두고 새로고침하면 초기화된다. 단 **위젯 배치와 화면 설정은 사용자 설정이므로 저장한다** — `localStorage`가 먼저 열리고 서버가 계정별로 뒤따라 맞춘다. 성격이 다르며 5.2가 정본이다. 어느 쪽도 IndexedDB에 쓰지 않는다.
- **캠페인 데이터와 연결하지 않는다 — 단 하나를 뺀다.** 도구는 캐릭터·캠페인 레코드를 쓰지 않고, 읽는 것도 아래 한 가지뿐이다. **전투 세션에 참여해도 이 제약은 그대로다** — 전투는 캠페인이 아니라 사람들의 일시적 모임이다(5.4).

> **2026-08-05 개정 — 공격 보정 덱은 퍽을 읽는다.** 이전 문서는 "읽지도 쓰지도
> 않는다"로 뭉뚱그렸다. 그러나 **보정 덱의 구성은 캐릭터가 얻은 퍽이 정한다** —
> 퍽을 얻을 때마다 사람이 설정 화면에서 장수를 다시 맞추는 것은 도구가 아니라
> 일이다. 퍽은 사용자 데이터이고 축 ①이 이미 그것을 들고 있으므로, 같은 것을
> 두 곳에 적어 두면 반드시 어긋난다.
>
> **읽기만 한다.** 도구는 캐릭터를 고치지 않는다. 방향이 한쪽뿐이므로 축 ①의
> 기록이 도구 때문에 흔들리는 일은 없다.
>
> **서버가 없어도 도는 것은 여전하다**(절대 원칙 3). 퍽을 읽을 수 없으면 표준
> 덱으로 시작한다 — 덱이 비는 것이 아니라 기본값이 되는 것이다.

> **2026-08-08 개정 — 캐릭터 이름표까지 읽는다. HP/XP도 그것을 쓴다.** 위 예외를
> 쓰려면 **어느 캐릭터인지 먼저 고를 수 있어야 한다** — 고를 것이 없는데 퍽만
> 읽을 수는 없다. 그래서 읽는 범위를 **식별자·이름·클래스 표식**까지로 넓힌다.
>
> **HP/XP가 같은 것을 쓴다.** 값이 위젯 인스턴스에 묶여 있었는데, 인스턴스 id는
> 기기마다 다르므로 전투에서 판을 나눠도 서로 다른 열쇠를 본다 — 넷이 앉아도
> 아무것도 겹치지 않는다. **파티원 모두가 알아보는 이름표**가 있어야 한 사람의
> 체력이 한 자리에 모인다. 보정 덱도 같은 이유로 캐릭터에 묶는다.
>
> **읽는 것은 여전히 그 세 칸뿐이다.** 골드·경험·아이템·메모는 읽지 않고, 고치는
> 일은 어느 경우에도 없다.
>
> **안 골라도 도구는 돈다**(절대 원칙 3). 로그인 전이거나 캐릭터를 아직 안
> 만들었으면 종전대로 인스턴스 id를 열쇠로 쓰고, 그때는 제 기기 안에서만 센다.
>
> **원소·라운드·주도권은 여전히 캠페인을 모른다.** 그 셋은 상 위의 사실이지
> 사람의 것이 아니라 이름표가 필요 없다.
- **룰 엔진이 아니다.** 확률 계산, 자동 판정, 상태이상 카운트다운, HP 추적은 하지 않는다. 사용자가 눈으로 보는 것을 대신 세어주는 수준까지만.

> **2026-08-01 개정 — 도구 런타임 상태의 서버 공유.** 이전 문서는 "서버로 동기화하지 않는다"를 축 ②의 제약에 함께 적었다. 그러나 실물 게임에서 원소판은 **식탁 한가운데 놓인 공용물**이다. 한 사람이 밀면 모두가 본다. 각자 화면에서 따로 세면 서로 어긋나고, 어긋난 것을 말로 맞추느라 도구를 쓰는 뜻이 사라진다.
>
> 그래서 **전투 세션에 참여한 동안에만** 도구 런타임 상태를 참여자끼리 공유한다(5.4). 아래 셋은 그대로 지킨다.
>
> - **영속이 아니다.** 전투가 끝나면 상태는 사라진다. 어느 기기에도 남지 않는다.
> - **옵트인이다.** 전투에 참여하지 않으면 종전대로 메모리 전용이며, 서버 없이 혼자 완전히 동작한다.
> - **캠페인과 무관하다.** 전투는 캠페인·캐릭터 레코드를 읽지 않는다.

### 여전히 범위에서 제외되는 것

- 상태이상 카운트다운·몬스터 개체 관리·피해 자동 적용 등 **본격 전투 트래킹**
  (HP/XP **숫자 세기**는 위 4번으로 들어왔다. 규칙을 돌리는 것은 여전히 밖이다.)
- 보정 덱 **확률 계산**, 자동 룰 판정
- 몬스터 능력 덱 / 몬스터 스탯표 → 카드 원문·스탯 텍스트가 필요하므로 3장 저작권 경계에 걸린다. 데이터팩(Phase 3)이 갖춰지기 전에는 구현하지 않는다.

---

## 2. 기준 게임

- **Gloomhaven 1판 (오리지널)** + **Jaws of the Lion (사자의 턱)**
- 두 게임의 원소·상태이상·성장 체계는 동일하므로 데이터 모델을 공유한다.
- Frosthaven, GH 2판은 현재 범위 밖 (데이터 모델은 확장 가능하도록 설계하되 구현하지 않음).

---

## 3. 저작권 경계 (반드시 준수)

**핵심 원칙: 앱(클라이언트·서버 어디에도) 저작권 콘텐츠 원문을 저장·전송하지 않는다.**

> **2026-07-31 개정 — 텍스트와 아트를 분리한다.**
> 이전 문서는 "Cephalofair 저작물을 일절 포함하지 않는다"고 뭉뚱그렸다. 그러나
> 저작자 Isaac Childres가 **Gloomhaven Creator Pack을 CC BY-NC-SA 4.0으로 직접
> 공개**했음이 1차 출처로 확인됐다(13.1장). 라이선스가 있는 것과 없는 것을 같이
> 묶어둘 이유가 없으므로 다음과 같이 나눈다.
>
> - **텍스트 콘텐츠 → 종전대로 일절 미포함.** 카드 능력 텍스트, 시나리오 서사,
>   몬스터 스탯표. 라이선스 유무와 무관한 우리 자체 원칙이다.
> - **아트 에셋 → CC BY-NC-SA 4.0 조건을 지키는 선에서 선별 포함 허용.**
>   원소·상태이상·클래스 아이콘 등. 조건과 격리 규칙은 13.1장이 정본.

- 카드 능력 텍스트, 시나리오 서사 텍스트, 몬스터 스탯표 등 Cephalofair Games의 **텍스트 저작물 원문은 배포물에 일절 포함하지 않는다.**
- 퍽(perk)은 **텍스트 없이 인덱스/체크마크로만** 추적한다. 예: "퍽 슬롯 3번 적용됨" (카드 문구 저장 안 함).
- 콘텐츠가 필요한 부분은 두 경로로만 채운다:
  1. **사용자 직접 입력** — 사용자가 자기 소유 게임을 참조해 수치를 입력.
  2. **사용자 소유 데이터팩(JSON) 로드** — 앱은 데이터팩을 동봉·배포하지 않고 *형식(스키마)만* 정의. 각 사용자가 로컬에서 로드.

**서비스 성격: 공개 배포, 사실상 지정 파티용.** (2026-07-31 변경 — 이전에는 비공개·폐쇄형이었다.)

GitHub Pages 배포를 위해 레포를 public으로 전환했다. 따라서:

- **레포 소스와 배포된 사이트 모두 누구나 볼 수 있다.** GitHub Pages 사이트는 레포가 private이어도 항상 공개이므로(접근 제어는 Enterprise 전용), Pages를 쓰는 한 이 성질은 피할 수 없다.
- 실사용은 여전히 형님과 지정 파티뿐이지만, 그것은 **아무도 안 찾아온다는 뜻일 뿐 접근 통제가 아니다.**

**이 변경의 저작권상 의미가 중요하다.** 이전 문서는 폐쇄성을 "안전 마진"으로 적었는데, **그 마진은 이제 없다.** 위의 "콘텐츠 원문 미저장" 원칙이 유일한 보호막이며, 그래서 더 엄격히 지켜야 한다.

- 저작물 원문을 레포에 커밋하는 순간 그대로 공개된다. 데이터팩 JSON을 실수로 커밋하는 일이 없도록 `.gitignore`로 막고, 커밋 전에 반드시 확인한다.
- 서버(Supabase)로도 콘텐츠 원문을 올리지 않는다는 원칙은 그대로다.
- 아트 에셋을 쓰게 되면 CC BY-NC-SA 4.0의 **출처표시·동일조건공유 의무가 공개 배포에서 실제로 발생한다.** 13장 🟡 항목 참조.

공개가 부담스러워지면 되돌릴 선택지는 Cloudflare Pages 등 private 레포를 지원하고 접근 제어까지 되는 호스팅으로 옮기는 것이다. (2026-08-05 개정 — 이전에는 "라즈베리파이 자가호스팅"을 두 번째 선택지로 적었다. 백엔드가 관리형(Supabase)으로 정해져 우리가 돌볼 기계가 없으므로 그 길은 사라졌다. 정적 호스팅을 옮기는 것과 백엔드는 이제 별개다.)

### 3.1 세 배포 (2026-08-01 추가, 같은 날 개정)

**같은 코드에서 셋이 나온다.** 처음에는 둘로 적었으나, 그러면 서버가 없는 동안
로그인 화면을 배포된 페이지에서 볼 수가 없어 하나를 더 두었다.

| 모드 | 로그인 | 백엔드 | 어디에 |
|---|---|---|---|
| `demo` | 없음 | 없음 | **갈 자리 없음** (아래) |
| `mock` | 있음 | **가짜(브라우저 안)** | **지금의 GitHub Pages** → 정식 후에는 로컬만 |
| `live` | 있음 | 진짜 | **GitHub Pages** (2026-08-05 확정) |

`mock`은 공개 주소에 올라가므로 **시험판임이 화면에 박혀 있어야 한다.**
계정과 기록이 그 브라우저 안에만 있다는 것을 걷을 수 없게 알린다.

> **2026-08-05 — 셋 중 하나만 배포된다.** 정식을 Pages에 올리기로 하면서
> (아래) 세 모드의 자리가 바뀌었다. **레포 하나에 Pages 사이트는 하나뿐**이므로
> `live`가 그 자리를 차지한다.
>
> - **`mock`** — 정식을 열기 전까지는 지금처럼 Pages에 있고, 그 뒤로는 **로컬에서만**
>   띄운다.
> - **`demo`** — 원래 "정식이 딴 데로 가고 나면 Pages에 남길 것"이었다. 정식이
>   Pages에 눌러앉았으므로 **갈 자리가 없어졌다.** 모드 자체는 코드에 남지만
>   배포 대상이 없다.
>
> 둘 다 정말 필요해지면 그때 정적 호스팅을 하나 더 둔다. 지금은 쓸 일이 없다고
> 본다 — `demo`는 애초에 "언젠가 남에게 보여줄 때"를 위한 것이었다.

**빌드 시점에 가른다.** 런타임 분기로 두면 한 번들이 세 모드를 다 알게 되어, `mock`이 진짜 백엔드를 가리키는 사고가 설정 한 줄로 일어난다. 셋을 갈라 두면 `mock` 번들에는 서버로 나가는 길이 아예 없다.

> **2026-08-05 개정 — 이유가 바뀌었다.** 이전에는 "**라즈베리파이 주소가 공개
> 번들에 박히면 집 서버를 광고하는 셈**"이라고 적었다. 백엔드가 Supabase로
> 정해지면서 그 근거는 사라졌다 — 관리형 서비스의 주소와 `anon` 키는 **공개해도
> 되는 값이고**(Supabase 문서가 명시한다), 보호는 주소를 감추는 것이 아니라
> **RLS 정책**이 한다. 감출 집 서버가 없다.
>
> 가르는 것 자체는 그대로 둔다. 근거만 "주소를 감춘다"에서 "**시험판이 진짜
> 데이터에 닿지 못하게 한다**"로 옮긴다.

**기본값은 닫힌 쪽이다.** 플래그가 없거나 잘못됐으면 **로그인을 요구한다.** 반대로 두면 설정 하나 빠뜨렸을 때 정식 배포가 활짝 열린다. `VITE_BASE`로 이미 두 번 덴 자리다.

**`service_role` 키는 클라이언트에 절대 넣지 않는다.** 이것은 RLS를 우회한다. 번들에 들어가도 되는 것은 `anon` 키뿐이며, 둘을 헷갈리면 공개 번들이 데이터베이스를 통째로 여는 열쇠가 된다.

**정식도 GitHub Pages에 올린다 (2026-08-05 확정).** 이전 문서는 "정식 서비스를 열면 Pages 배포를 **내린다**"고 적었으나, 그 근거는 집 서버 주소를 감추려던 것이었다. `anon` 키와 프로젝트 주소는 공개해도 되는 값이므로 `live` 번들을 Pages에 그대로 올려도 새는 것이 없다.

**한 곳만 살린다는 원칙은 그대로다.** 레포 하나에 Pages 사이트가 하나뿐이므로 저절로 지켜진다 — `live`로 바꾸는 순간 `mock`은 사라진다.

그래서 딸려오는 것이 하나 있다. **`mock`을 올려둘 자리가 없어진다.** 서버 없이 로그인 흐름을 만져보거나 남에게 보여주려면 로컬에서 띄우는 수밖에 없다. 그것으로 부족해지면 그때 다른 정적 호스팅을 하나 더 두는 것을 검토한다 — 지금은 로컬로 충분하다고 본다.

**Pages를 쓰기 때문에 따라오는 제약 셋** (N2에서 다룬다):

1. **서브경로에 놓인다.** `https://<user>.github.io/<repo>/` 아래이므로 `VITE_BASE`를 맞춰야 한다. **`live` 빌드도 예외가 아니다** — 빌드와 미리보기의 값이 어긋나면 화면이 하얘지고 콘솔에 아무것도 안 뜬다(README에 두 번 덴 기록이 있다).
2. **인증 리다이렉트 주소를 Supabase에 등록해야 한다.** 서브경로까지 포함한 주소다.
3. **인증 응답이 해시 라우터와 부딪힐 수 있다.** 우리는 `#/login` 꼴의 해시 라우팅을 쓰는데, Supabase가 구식 implicit flow로 토큰을 돌려주면 `#access_token=...` 형태라 `readRoute`가 라우트로 읽지 못한다. **`supabase-js` v2의 기본값인 PKCE를 쓰면 `?code=...`로 오므로 부딪히지 않는다** — N2에서 확인만 하면 된다.

셋 다 **커스텀 도메인을 붙이면 함께 수월해진다**(base가 `/`가 되고 리다이렉트 주소도 짧아진다). 지금 정할 일은 아니다.

**서버 동기화 경계 (저작권 직결):**
- 서버를 통해 동기화되는 것은 **순수 진행 상태(사실 수치)뿐이다**: HP 최대치, 골드, 경험치, 레벨, 퍽 인덱스, 아이템 소유 여부(식별자), 시나리오 완료 상태, 번영/명성 수치 등.
- 카드 원문·시나리오 서사·몬스터 능력 텍스트 등 저작물성 콘텐츠는 **동기화 대상에서 제외.** 콘텐츠는 각 기기 로컬에만 존재.

**인게임 도구(1장 축 ②)의 저작권 경계:**

채택된 세 도구가 안전한 이유는 **전부 수치·기호·규칙 구조만 다루고 저작물 원문을 담지 않기** 때문이다.

- **원소 트래커** — 6원소의 3단계 상태기계. 원소 이름/아이콘 외에 텍스트 없음.
- **공격 보정 덱** — 표준 덱은 수치와 기호의 구성일 뿐이다(+0 다수, ±1, ±2, x2, null). 이는 게임 메커닉의 수치 표현이지 저작물 원문이 아니다. 캐릭터별 추가/제거 카드는 **사용자가 직접 구성**한다.
- **주도권 정렬** — 사용자가 입력한 숫자의 정렬. 앱이 들고 있는 콘텐츠 없음.

반면 **몬스터 능력 덱·몬스터 스탯표는 카드 원문과 스탯 텍스트 자체가 저작물**이므로 도구로 만들 수 없다. 필요하다면 Phase 3 데이터팩 로더를 통해 사용자가 자기 소유 데이터를 불러오는 경로로만 가능하다.

---

## 4. 아키텍처 개요

```
┌─────────────────────────────────────────────┐
│  클라이언트 (React + TS + Vite, PWA)          │
│  - 한국어 UI 우선 (i18n 구조 여지)            │
│  - 모든 로직 로컬, offline-first              │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ 축 ② 인게임 도구 패널 (휘발성)          │  │
│  │  원소 트래커 / 보정 덱 / 주도권 정렬     │  │
│  │  메모리 상태만. 전투 중에만 공유(5.4).   │  │
│  │  축 ①과 데이터 연결 없음.               │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │ 축 ① 캠페인 기록지 (영속)               │  │
│  │  로컬 저장: IndexedDB (Dexie)          │  │
│  │  JSON export/import (파일 백업)        │  │
│  └──────────────┬────────────────────────┘  │
└─────────────────┼───────────────────────────┘
                  │ 온라인 시 동기화 (진행 상태만)
                  ▼
┌─────────────────────────────────────────────┐
│  Supabase (관리형)                           │
│  - Postgres + Auth + Realtime                │
│  - 접근 통제는 RLS 정책                       │
│  - 축 ①은 테이블에 저장(동기화)               │
│  - 축 ②는 Broadcast로 흘려보냄(미저장)        │
│  - "덤 스토리지 + 브로드캐스트" 역할만        │
│  - 룰 판정/검증 없음                          │
└─────────────────────────────────────────────┘
```

### 4.1 프론트엔드 스택 (확정)
- **React + TypeScript + Vite**
- 상태관리: **Zustand** — 행낭 착수와 함께 도입(`milestone/done/M0-기반-정비.md`). 스토어는 전역 디렉토리를 만들지 않고 기능 폴더 안에 둔다. `persist` 미들웨어는 **사용자 설정 스토어에만** 붙이고 도구 런타임 스토어에는 금지(5.2)
- 로컬 영속: **IndexedDB via Dexie.js**
- 린트/포맷: ESLint + Prettier
- 컴포넌트: 함수형 컴포넌트
- 폴더 구조: 기능별(feature-based) 분리
- 테스트: Vitest (핵심 도메인 로직 우선 — 정산 계산, 동기화 병합 로직)
- **PWA로 설치 가능하게 구성** (`navigator.storage.persist()` 요청 포함)
- 네이티브 셸(Tauri)은 현재 채택 안 함 — 웹앱 기반으로 확정. (미래 옵션으로만 기록)

### 4.2 백엔드 (확정 — 2026-08-05 변경)

#### 먼저 정책 — 우리가 돌볼 서버를 두지 않는다

**제품보다 이것이 먼저다.** 기계도, VPS도, 컨테이너도 우리 손으로 돌리지 않는다.
서버 쪽 필요는 **관리형 서비스로만** 채운다.

이 정책이 백엔드를 고른 축이다. 앞으로 서버가 얽힌 결정이 나올 때마다 여기로
돌아온다.

- **자가호스팅은 되돌아갈 길로도 두지 않는다.** Supabase는 오픈소스라 자가호스팅이
  가능하지만 그 길은 택하지 않는다. "언제든 옮길 수 있다"고 적어두면 그것이 곧
  계획이 된다.
- **금지 대상은 '기계'다.** 우리가 띄우고 지켜봐야 하는 것 — 서버·VPS·컨테이너 —
  을 두지 않는다는 뜻이지, 무료 등급을 쓰지 말라는 뜻이 아니다. **남의 관리형
  위에서 도는 예약 작업은 기계가 아니다** — 무료 등급의 일시정지를 GitHub Actions
  예약 실행으로 넘기는 것(12장 10)은 정책 안이다.
- **돈으로 넘길 수 있는 것과 없는 것을 가른다.** 기계를 두어야만 풀리는 문제라면
  돈을 낸다. 다만 부품 하나로 넘어가는 문제까지 돈으로 풀지는 않는다 — 파티 넷이
  쓰는 도구다.
- **offline-first(3장)와 짝이다.** 서버는 있어도 그만인 계층이므로 남에게 맡겨도
  판이 위태롭지 않다. 두 원칙이 서로를 떠받친다.

#### 고른 것 — Supabase, 관리형으로

Postgres + Auth + Realtime.

> **"Supabase는 관리형이다"가 아니라 "Supabase를 관리형으로 쓴다"이다.** 둘 다
> 제품 고유명사이지 운영 방식이 아니다. 위 정책이 운영 방식을 정하고, 제품은
> 그 안에서 고른다.

> **이전 결정을 뒤집었다.** 원래는 **PocketBase를 라즈베리파이에 자가호스팅**하기로
> 확정돼 있었다. 뒤집은 이유는 둘이다.
>
> 1. **위 정책.** PocketBase가 탈락한 것은 제품이 나빠서가 아니라 **관리형 선택지가
>    아예 없어서**다 — 그 제품을 고르면 기계가 딸려온다.
> 2. **Broadcast가 5.4와 더 잘 맞는다.** 이쪽이 더 중요하다 — 아래 참조.
>
> 부하는 어차피 문제가 아니었다. 한 판에 메시지 2,000건, 동시 연결 4개, 저장은
> 메가바이트 단위다. **어느 쪽이든 무료 한도의 1%도 쓰지 않는다.** 고른 기준은
> 요금이 아니라 *누가 돌보는가*와 *설계 원칙에 맞는가*였다.

- **Postgres.** 스키마는 마이그레이션 파일로 버전 관리한다.
- **접근 통제는 RLS 정책.** PocketBase의 API 규칙과 개념이 거의 1:1이라 규칙 표가 그대로 옮겨간다. **RLS를 켜지 않은 테이블은 `anon` 키만으로 통째로 열린다** — 여기가 가장 조심할 곳이다.
- **Realtime은 두 갈래를 구분해 쓴다.**
  - 축 ① 동기화(5.3) → **Postgres Changes.** 테이블에 남아야 하는 것이다.
  - 축 ② 전투 공유(5.4) → **Broadcast.** DB에 쓰지 않고 메시지만 흘려보낸다.
- **`anon` 키는 공개해도 된다.** 보호는 RLS가 한다. `service_role` 키는 클라이언트에 절대 넣지 않는다(3.1).
- **미사용 시 일시정지가 있다(무료 등급, 약 1주일).** **GitHub Actions의 예약 실행으로 주기적으로 가벼운 쿼리를 날려 깨워둔다** (2026-08-05 확정, 12장 10). 예약 작업은 서버가 아니고 GitHub Actions는 이미 배포에 쓰고 있으므로, 워크플로 파일이 하나 느는 것 외에 새로 돌볼 것이 없다. 구현은 N2 — 깨울 대상이 있어야 만들 수 있다.
- 인증: **초대 링크 + 이메일 + 비밀번호** (2026-08-05 변경). 매직링크는 쓰지 않는다.
  - **진짜 이메일 주소를 받는다.** 지어낸 주소(`아무개@sl2.local` 따위)로 계정을 만들면 지금은 같아 보여도 **나중에 비밀번호 재설정 메일을 보낼 수 없다.** 진짜 주소를 받아두면 그 길이 열린 채로 남고, 오늘 드는 비용은 없다.
  - **확인 메일과 비밀번호 재설정은 당장 만들지 않는다.** 둘 다 메일을 실제로 보내야 하므로 별도 SMTP가 필요하다(Supabase 내장 발송은 발송량 제한이 엄해 실서비스용이 아니다). **정말 잊어버리는 일이 생기면 그때 Resend 같은 관리형 발송 서비스를 붙인다** — 관리형이므로 4.2의 정책에 걸리지 않는다.
  - **확인 메일을 끄고 시작하는 이유.** 켜면 초대 링크를 받은 사람이 가입 도중 메일함까지 다녀와야 한다. 파티원 넷이 쓰는 도구에서 남의 주소로 가입하는 위협은 사실상 없다.
  - **이메일은 로그인에만 쓰고 화면에는 내보내지 않는다.** 파티원 목록에 서로의 주소가 보이면 곤란하다. 표시용 이름은 `profiles`에 따로 둔다.

> **이전 결정을 뒤집었다.** 원래는 "이메일/매직링크 미사용 → **SMTP 불필요**"였다.
> 이메일이 싫어서가 아니라 **자가호스팅에 메일 서버가 없어서** 나온 결정이었고,
> 백엔드가 Supabase로 바뀌면서 그 전제가 사라졌다. Auth의 기본 경로를 그대로 타면
> 아이디를 억지로 앉히는 우회가 필요 없고, RLS도 `auth.uid()`로 자연스럽게 걸린다.

**Broadcast가 왜 중요한가.** SPEC 5.4는 "전투가 끝나면 **어느 기기에도 남지 않는다**"고 못박았다. PocketBase에서는 값을 공유하려면 일단 테이블에 써야 해서, 써 놓고 지우는 절충이 필요했다. Broadcast는 애초에 저장하지 않고 지나가는 메시지라 **약속을 절충 없이 지킨다.** 저장하지 않는 것이 기본값이면 실수로 남을 일도 없다.

---

## 5. 저장·동기화 전략

### 5.1 3계층 저장
1. **1차 저장: IndexedDB (Dexie)** — offline-first. 네트워크 없이도 완전 동작.
2. **영속 승격: `navigator.storage.persist()`** — 브라우저 자동 eviction·미방문 만료 방어 (사용자의 명시적 삭제는 못 막음).
3. **최종 안전망: JSON export/import** — 브라우저 밖 실제 파일 백업. 어떤 브라우저 조작에도 독립적. 파일 저장은 File System Access API(지원 환경) → Blob 다운로드(폴백).

### 5.2 인게임 도구의 상태 (축 ②)

> **2026-07-31 개정.** 이전 문서는 "위 3계층 어디에도 저장하지 않는다"고 뭉뚱그렸다. 그러나 축 ②를 **위젯 보드**로 만들면서 배치를 저장하지 않으면 열 때마다 다시 배치해야 해 도구로 쓸 수 없음이 드러났다. 이전 문서가 이미 덱 구성을 예외 후보로 두며 "사용자 설정이지 캠페인 데이터가 아니다"라고 성격을 구분해 두었으므로, 그 선을 기준으로 아래와 같이 나눈다.

**두 가지를 구분한다. 이 구분이 축 ② 저장 정책의 전부다.**

| 무엇 | 성격 | 저장 위치 |
|---|---|---|
| 위젯 배치(무엇이 어디에 얼마 크기로), 툴바 위치, 그 밖의 도구 화면 설정 | **사용자 설정** | `localStorage` **+ 서버(계정별 한 줄)** |
| 뽑은 보정 카드, 원소 하강 상태, 라운드, HP/XP 등 도구가 돌아가며 만드는 값 | **도구 런타임** | **메모리 + 판이 도는 동안만 남는 자리** — 혼자면 `sessionStorage`, 전투 중이면 서버(5.4) |

**도구 런타임 상태는 행낭 전체에 하나뿐이다.** 같은 도구를 위젯으로 여럿 놓아도 같은 것을 본다 — 식탁 위 원소판은 하나이고 위젯은 그것을 비추는 창이다. 위젯을 여럿 놓는 뜻은 **같은 상태를 다른 자리·다른 방향에서 보려는 것**이며, 그래서 위젯 회전과 짝을 이룬다(태블릿을 상 가운데 놓고 마주 앉는 경우).

- **도구 런타임 상태는 여전히 메모리가 정본이다.** Zustand 스토어에 `persist` 미들웨어를 붙이지 않고, `localStorage`·IndexedDB에 쓰지 않는다.

> **2026-08-08 개정 — 새로고침은 견딘다.** 이전 문서는 "새로고침하면 초기 상태로 돌아가는 것이 의도된 동작"이었다. **그것이 틀렸다.**
>
> 지하에서 세 시간 하는 동안 태블릿은 잠기고 브라우저는 탭을 버린다. 손이 미끄러져 당겨 새로고침되기도 한다. 그때마다 라운드와 원소가 처음으로 돌아가면 실물 판과 어긋나고, **어긋난 것을 사람이 기억해서 되맞춰야 한다.** 도구가 일이 되는 자리다.
>
> **가르는 선은 "메모리냐 아니냐"가 아니라 "판이 끝나면 남느냐"다.** 처음에 메모리 전용으로 못박은 까닭이 그것이었는데, 수단을 원칙으로 적어 두었던 것이다. 판이 도는 동안만 남는 자리라면 원칙을 지킨다.
>
> | 어디 | 언제 사라지나 |
> |---|---|
> | `sessionStorage` | **탭을 닫으면** — 서버에 못 닿을 때의 대비(`runtime/keep.ts`) |
> | `satchel_runtime` (서버) | **하루 뒤** — 혼자 쓸 때의 방 |
> | 전투 상태 (서버) | **전투를 닫으면** — 참여 중일 때(5.4) |

> **2026-08-08 재개정 — 판 하나 = 방 하나.** 위에서 배치는 계정에 묶고 판은
> 브라우저마다 두었더니, **같은 계정으로 두 곳에서 열면 화면이 갈라졌다.** 전투는
> 다른 사람과 나누라고 만든 것인데 같은 사람의 기기 둘 사이에도 전투를 열어야
> 맞춰지는 꼴이었다.
>
> 방을 둘로 정리한다. **혼자면 내 계정이 방이고, 전투에 앉으면 그 전투가 방이다.**
> 오가는 방식은 두 방이 같다 — Broadcast로 즉시, 표에 뒤따라(`runtime/room.ts`).
>
> **"판이 끝나면 남지 않는다"는 하루 규칙으로 지킨다.** 서버에는 "탭을 닫았다"는
> 신호가 오지 않으므로 탭 단위로 지울 수가 없다. 전투 방과 같은 규칙이며, 한 판이
> 하루를 넘길 일은 없다. `localStorage`·IndexedDB 금지는 그대로다 — 그 둘은 스스로
> 사라지지 않는다.
>
> `localStorage`와 IndexedDB는 여전히 금지다. 그 둘은 다음 주에 앱을 열어도 남아 있어, 지난 판의 원소가 타오르고 있는 꼴이 난다.
>
> 실어 나를 수 있게 **네 스토어를 한 뭉치로 뜨는 계층**을 둔다(`runtime/snapshot.ts`). 저장하는 것도 파티원과 나누는 것도 같은 뭉치이며, **거르는 자리도 하나다** — 저장소에서 읽은 것, 서버가 준 것, 파티원이 보낸 것이 모두 같은 문을 지난다.
- **사용자 설정은 `localStorage`가 먼저 열리고 서버가 뒤따라 맞춘다.** IndexedDB(Dexie)에는 넣지 않는다 — 그것은 축 ①의 것이다.

> **2026-08-08 개정 — 사용자 설정을 서버에도 둔다.** 이전 문서는 "`localStorage`에만 두고 서버로 동기화하지 않는다"였다. 근거는 둘이었다. ① 배치는 화면 크기를 타니 기기마다 다른 편이 맞다 ② 축 ①과 물리적으로 떼어 놓아야 나중에 동기화 대상에 섞이지 않는다.
>
> **①이 무너졌다.** 레이아웃을 **열 수를 키로** 나눠 저장하도록 만들면서(구현 결정 6) 폰 것과 태블릿 것이 한 뭉치에 들어가도 서로 밟지 않게 되었다. 화면 크기가 다르다는 것이 더는 나눠 둘 이유가 아니다.
>
> **그리고 실제로 잃었다.** iOS 홈화면 아이콘을 지우자 행낭이 통째로 초기화됐다 — 아이폰은 홈화면 앱 저장소를 사파리와 따로 두고 아이콘을 지울 때 그 칸을 함께 지운다. 기록지를 서버로 옮긴 것과 같은 원인이다. 계정 id로 열쇠만 갈라 놓는 것으로는 이것을 막지 못한다.
>
> **②는 표를 따로 두어 지킨다.** `satchel_settings`는 사람 하나에 한 줄이며 캠페인·캐릭터·파티를 참조하지 않는다. 축 ②가 캠페인 데이터를 읽지도 쓰지도 않는다는 제약(1장)은 그대로다 — 제 화면 설정을 제 계정에 두는 것은 캠페인 데이터가 아니다.
>
> **절대 원칙 3은 그대로다.** 정본은 여전히 로컬이고 서버는 **백업이자 기기 사이를 잇는 다리**다. 못 닿아도 행낭은 완전히 돌고, 서버 오류는 전부 삼킨다 — 신호 없는 자리에서 위젯을 못 옮기면 도구가 아니다.
>
> 기기가 어긋나면 **늦게 고친 쪽이 이긴다**(5.3). 칸별로 합치지 않는다 — 배치는 통짜로 하나의 그림이라 반쪽씩 섞으면 위젯이 겹치거나 사라진 자리가 남는다. 견주는 시각은 **뭉치 안에 기기가 찍어 둔 것**이다. 서버의 `updated_at`은 도착 시각이라 언제나 로컬보다 늦어, 그것으로 견주면 로컬이 영영 진다.
>
> **도구 런타임 상태는 이 개정과 무관하다.** 뽑은 카드·원소·라운드는 여전히 메모리 전용이며 서버에 가지 않는다.
- 공격 보정 덱의 **덱 구성**도 사용자 설정에 속한다. 초기 구현에서는 넣지 않되, 넣게 되면 위 표의 첫 줄을 따른다.

구현 계획은 [`milestone/done/M3-레이아웃-모델과-영속.md`](./milestone/done/M3-레이아웃-모델과-영속.md)가 정본이다.

### 5.3 서버 동기화 (Supabase)
- **offline-first + 서버 동기화 하이브리드.** 오프라인 변경은 로컬 큐에 쌓고 온라인 복귀 시 순차 동기화.
- **입도 분리:** 데이터를 campaign / character / scenario_record / event_log 등 레코드 단위로 나눠 저장. 서로 다른 필드를 건드리는 편집은 충돌하지 않음.
- **충돌 처리:** 레코드 단위 **last-write-wins + `updated_at` 타임스탬프**. 핵심 필드에는 **version 필드로 낙관적 잠금**(if_version 개념). 충돌 시 사용자에게 알림을 주되 자동으로 last-write-wins 처리(수동 병합 UI는 후순위).
- **실시간 공유:** Supabase Realtime의 **Postgres Changes** 구독으로 다기기·다플레이어 반영. 축 ②의 전투 공유는 이것이 아니라 Broadcast를 쓴다(5.4) — 남아야 하는 것과 지나가야 하는 것을 갈라 둔다.

### 5.4 전투 세션 (축 ②의 실시간 공유)

축 ①의 동기화(5.3)와 **성격이 다르다.** 기록지는 판이 끝난 뒤의 정산을 남기는 것이라 커밋 단위로 밀어도 되지만, 원소판은 판이 도는 **몇 초 단위로** 여럿이 만진다. 그래서 규칙을 따로 둔다.

**세 겹의 구조.**

| 층 | 무엇 | 수명 | 저장 |
|---|---|---|---|
| **파티** | 같이 노는 사람들의 묶음 | 지속 | 서버 (테이블) |
| **전투(방·참여자)** | 이번 판에 모인 사람들 | 일시 — 끝나면 사라진다 | 서버 (테이블, 짧은 수명) |
| **전투(내부 상태)** | 원소·라운드·HP/XP·보정 덱 | 판이 도는 동안만 | **Broadcast(즉시) + 전투 행(따라잡기). 판을 접으면 행이 지워진다** |
| **행낭 설정** | 위젯 배치·툴바 위치 | 지속 | `localStorage` + 서버(계정별, 5.2) |

> **2026-08-08 개정 — 값을 전투 행에도 둔다.** 이전 문서는 "Broadcast로 흘려보내고 아무 데도 쓰지 않는다"였다. **그것으로는 두 가지가 안 된다.** ① 새로고침하면 다음에 누가 만질 때까지 빈 화면이다. ② 넷이 다 같이 새로고침하면 판이 통째로 날아간다 — 아무도 안 들고 있다.
>
> 그래서 값을 `battles.state`에 둔다. **수명을 전투와 같게 묶는다** — 판을 접으면 행이 지워지고 값도 함께 간다. 별도 표가 아니라 같은 행에 두는 까닭이 그것이다. 사람이 접지 않고 그냥 앱을 닫는 경우를 위해 **하루 지난 판은 서버가 거둔다**(`sweep_stale_battles`).
>
> **즉시 반영은 여전히 Broadcast다.** 행에 쓰는 것은 뒤따라가며, 새로 들어오거나 새로고침한 사람이 따라잡는 데만 쓴다. 왕복을 기다려 그리면 손가락이 미끄러진다.
>
> **공유 범위도 넓혔다.** 원소만이 아니라 라운드·HP/XP·보정 덱까지 간다. 실물에서 넷이 같은 상을 보고 있으므로, 라운드가 사람마다 다르거나 누가 뭘 뽑았는지 안 보이면 도구가 상을 못 비춘다. 위젯 배치는 그대로 각자의 것이다.
>
> **얹는 것은 참여자만이다.** 파티원이라도 이 판에 앉지 않았으면 못 얹는다 — 옆에서 구경하다 남의 판을 굴리는 일이 없어야 한다(`0007_battle_state.sql`).

> **2026-08-05 — 넷째 줄이 갈라져 나왔다.** 백엔드를 Supabase로 정하면서 **방과
> 값의 저장 방식이 달라졌다.** 누가 참여 중인지는 서버가 알아야 하므로 테이블에
> 남지만, 원소 여섯 값은 **Broadcast로 흘려보내고 아무 데도 쓰지 않는다.**
> 아래 "영속이 아니다"를 절충 없이 지키는 길이다(4.2).

**행낭을 열면 여전히 자기 배치가 열린다.** 공유되는 것은 **내부 상태뿐이다** — 남의 위젯 배치가 내 화면을 덮지 않는다. 이 구분이 5.2 표의 두 줄과 정확히 겹친다.

**통로에는 알맹이를 싣지 않는다(2026-08-09).** Broadcast는 기본이 공개 채널이고, 잠그려면 `realtime.messages`에 정책을 걸어야 하는데 **그 표는 Supabase 소유라 SQL Editor의 어떤 역할로도 손이 닿지 않는다**(`postgres`도 `supabase_privileged_role`도 거절당한다). 표에는 진작 RLS를 걸어 두었으니 **같은 값이 다니는 두 길 중 한쪽만 잠근 꼴**이었다.

그래서 통로를 **"바뀌었다"는 신호만 나르는 길**로 바꿨다. 받는 쪽은 신호를 보고 **표에서 값을 읽어 온다** — 자격은 표의 RLS가 본다.

| 통로 | 알리는 것 | 값은 어디서 |
|---|---|---|
| `satchel:<계정id>` | 배치가 바뀜 / 혼자일 때의 판이 바뀜 | `satchel_settings` · `satchel_runtime` — **제 것만** |
| `battle:<전투id>` | 그 판이 바뀜 | `battles.state` — **앉은 사람만** |

**통로를 엿들어 알 수 있는 것은 "언제 무엇인가 바뀌었다"뿐이다.** 가짜 신호를 보내도 상대가 제 권한으로 표를 한 번 더 읽을 뿐이다.

**표에 먼저 쓰고 그다음에 알린다.** 순서가 뒤바뀌면 받은 쪽이 아직 옛 값이 든 표를 읽는다.

**대가는 지연이다.** 탭한 사람의 화면은 여전히 즉시 바뀌고, 늦어지는 것은 옆 사람 화면이다. 반 초 남짓이라 판에 지장이 없다.

**전투에 앉아도 배치는 계정 통로에 남는다.** 구성은 제 것을 쓰고 상태만 파티의 전투를 따른다 — 화면 크기가 사람마다 다르므로 배치를 나누면 남의 화면이 내 것을 덮는다.

**공유 대상은 축 ②의 순수 수치·식별자뿐이다.** 원소 6개의 3단계 상태, 라운드 수, HP/XP 수치, 주도권 숫자, 보정 카드의 **식별자**. 3장의 서버 경계를 넘지 않는다 — 카드 원문은 어느 층에도 올리지 않는다.

**충돌 처리는 축 ①과 다르다.** 5.3의 레코드 단위 last-write-wins는 그대로 쓰되, **탭 반응은 로컬에서 즉시 보여주고 서버 확정이 뒤따라 덮는다.** 왕복을 기다리면 손가락이 미끄러진다. 값이 3단계뿐이라 뒤늦게 덮여도 사용자가 이해할 수 있다.

**끊기면 혼자 모드로 강등한다.** 판이 멈추면 안 된다. 로컬로 계속 쓰고, 돌아오면 서버 상태와 견줘 다를 때만 **"서버에 맞출까, 내 것으로 덮을까"**를 한 번 묻는다. 값이 여섯 개뿐이라 이 정도로 충분하다.

**전투에 참여하지 않으면 종전 그대로다.** 서버가 없어도, 로그인 세션만 있으면 혼자서 완전히 동작한다.

---

## 6. 권한 모델

- **다플레이어 공유:** 여러 사용자가 같은 캠페인을 본다.
- **자기 캐릭터만 편집:** 각 사용자는 **본인 캐릭터만 편집** 가능. 타인 캐릭터는 **읽기 전용.**
- **파티 상태(번영·명성·시나리오 진행 등)는 파티장만 편집.**
- **파티장도 남의 캐릭터는 읽기 전용** (본인 캐릭터만 편집).
- **파티장 위임:** 현 파티장이 언제든 다른 멤버로 파티장을 변경 가능. 위임은 현 파티장만 실행 가능.

### 6.1 접근 제어 (2026-08-01 추가)

**로그인하기 전에는 어떤 기능도 쓸 수 없다.** 기록지도 행낭도 마찬가지다. 예외는 **공지·라이선스 페이지(`#/notice`)** 하나뿐이다 — Creator Pack의 출처표시 의무(13.1)는 로그인 여부와 무관하게 배포에서 발생하므로, 표기를 게이트 뒤에 숨기지 않는다.

**게이트의 기준은 '서버에 닿는가'가 아니라 '이 기기에 유효한 세션이 있는가'다.** 매번 서버에 물으면 3장의 offline-first 원칙이 죽는다 — 신호 없는 자리에서 판을 벌이면 앱이 통째로 멈춘다. 첫 로그인만 네트워크를 요구하고, 이후에는 저장된 세션으로 오프라인에서도 열린다.

**클라이언트의 게이트는 UX일 뿐이다.** 레포와 번들이 공개이므로 화면을 가리는 것은 아무것도 막지 못한다. **강제는 전부 Supabase의 RLS 정책에서 이뤄진다.** 클라이언트는 "안 보여주기", 서버는 "안 주기". `anon` 키가 번들에 실려 있어도 상관없는 이유가 이것이다 — 키는 문을 여는 것이 아니라 문 앞까지 데려다줄 뿐이고, 들여보낼지는 RLS가 정한다.

### 6.2 파티와 전투 (2026-08-01 추가)

**파티는 평평하다.**
- 누구나 파티를 만들 수 있다.
- 한 사람이 **여러 파티**에 속할 수 있다.
- **파티원이면 누구나 다른 사람을 초대할 수 있다.** 파티장만의 권한이 아니다.

> 6장 첫머리의 "파티장" 개념은 **축 ①(기록지)의 편집 권한**에 대한 것이다. 파티 가입·초대·전투 생성에는 파티장이 필요 없다. 두 가지를 섞지 않는다.

**초대는 링크로 한다.** 초대코드를 불러주는 대신 링크를 만들어 메신저로 보낸다. 링크를 열면 로그인(또는 가입) 후 파티에 들어온다.

링크는 메신저를 타고 흐르고 전달되고 캡처된다. 그래서:
- 추측할 수 없는 토큰을 쓴다.
- **만료**가 있다.
- **취소**할 수 있다.
- 링크만으로는 아무 데이터도 보이지 않는다 — 로그인이 먼저다.

**전투는 파티 안에서 열린다.**
- 파티원이면 **누구나** 전투를 만들 수 있다.
- 접속 중인 파티원에게 **새 전투가 열렸음이 알려진다.**
- 알림을 받은 사람은 **참여를 고른다.** 자동으로 끌려들어가지 않는다.
- 참여자끼리 축 ②의 내부 상태를 공유한다(5.4).
- **원소판은 공용물이므로 참여자 누구나 만질 수 있다.** 축 ①의 "본인 것만 편집"을 여기 적용하지 않는다.

---

---

## 7. 데이터 모델 (초안 — 구현 시 확정)

> 저작권 경계에 따라 모든 콘텐츠는 식별자/인덱스/수치로만 표현. 원문 텍스트 없음.

### Campaign
- `id`, `name`
- `prosperity` (번영 수치)
- `reputation` (명성 수치)
- `partyAchievements[]` (식별자 배열)
- `globalAchievements[]` (식별자 배열)
- `unlockedScenarios[]`, `completedScenarios[]` (시나리오 번호 배열)
- `partyLeaderUserId`
- `createdAt`, `updatedAt`, `version`

### Character
- `id`, `campaignId`, `ownerUserId`
- `class` (클래스 식별자 — 아이콘/코드, 텍스트 아님)
- `name`
- `level`, `xp`, `gold`
- `checkmarks` (전투 목표 체크마크 진행)
- `perks[]` (적용된 퍽 슬롯 인덱스 배열 — 텍스트 없음)
- `items[]` (아이템 식별자 배열)
- `personalQuestProgress`
- `retired` (bool)
- `updatedAt`, `version`

### ScenarioRecord (시나리오 정산 기록 — 기본 기록 단위 ①)
- `id`, `campaignId`
- `scenarioNumber`
- `status` (locked / unlocked / completed)
- `outcome` (success / fail / 미완)
- `playedAt`
- `rewards` (골드·경험치·잠금해제 등 정산 결과 요약, 수치·식별자만)
- `participatingCharacterIds[]`

### ManualAdjustment (수동 보정 — 기본 기록 단위 ②)
- `id`, `campaignId`, `characterId`
- `field` (gold / xp / level / item / perk 등)
- `delta` 또는 `newValue`
- `note` (사용자 메모 — 자유 텍스트, 저작물 아님)
- `appliedAt`

### EventLog (도시/도로 이벤트 처리 이력)
- `id`, `campaignId`
- `type` (city / road)
- `cardId` (이벤트 카드 번호 — 식별자만, 텍스트 없음)
- `choice` (선택지 A/B 등)
- `appliedAt`

> **기록의 기본 단위는 둘 다 지원:** ① 시나리오(전투) 1회 = 정산 기록 1건 → 그 결과로 캐릭터 갱신. ② 수동 보정 = 수시로 캐릭터 상태 직접 조정.

### 데이터팩 스키마 (선택 로드용 — 앱은 형식만 정의, 콘텐츠 미동봉)
- 몬스터 수치, 시나리오 메타데이터 등을 사용자가 채워 넣는 JSON 형식.
- 앱은 이 스키마를 **검증·로드하는 로더만** 제공하고, 실제 데이터 파일은 배포하지 않는다.
- 스키마 형식(구조)은 Gloomhaven Secretariat의 에디션 JSON 구조를 **참고**할 수 있으나, 그 콘텐츠 데이터 자체는 가져오지 않는다 (13장 참조).

---

## 8. 다중 캠페인

- **처음부터 다중 캠페인 관리를 지원.** 한 사용자가 여러 캠페인에 소속·전환 가능.
- 캠페인 목록 → 선택 → 해당 캠페인의 파티/캐릭터/시나리오 뷰.

---

## 9. UI 언어

- **한국어 UI 우선.** 문자열은 i18n 구조(예: react-i18next)로 분리해 향후 다국어 여지를 남긴다.
- 정식 i18n을 붙이기 전까지는 `src/i18n/messages.ts`가 임시 보관소다. **번역본이 실제로 있는 문구만** 여기 둔다 — 한국어뿐인 문구까지 끌어오면 키만 늘고 읽기 어려워진다. 화면은 지금 `ko`만 읽는다.
- 화면 문구의 문체는 SPEC 용어와 별개다. SPEC은 기술 용어(캠페인 기록지·인게임 도구·정산)를 쓰고, 화면은 여관 문체로 옮긴다(일지·행낭). 원칙은 `WelcomePage.tsx` 주석에 있다.

---

## 10. 로드맵 (제안)

**Phase 1 — 캠페인 기록 (MVP, 영속성 우선)**
- 로컬 저장(IndexedDB/Dexie) + JSON export/import
- 다중 캠페인 CRUD, 파티 시트, 캐릭터 시트
- 시나리오 정산 기록 + 수동 보정
- (서버 없이 로컬만으로 완전 동작하는 상태를 먼저 완성)

**Phase 2 — 동기화·다플레이어**
- Supabase 연동(관리형), 초대 링크+비밀번호 인증, 로그인 게이트(6.1)
- 파티·전투 세션과 축 ②의 실시간 공유(5.4, 6.2)
- 진행 상태만 동기화, 레코드 단위 last-write-wins + version 잠금
- 권한 모델(본인 캐릭터 편집 / 파티장 파티 편집 / 위임)
- Supabase Realtime — 축 ①은 Postgres Changes, 축 ②는 Broadcast

**Phase 3 — 편의**
- 데이터팩 로더(사용자 소유 JSON)
- 정적 규칙 참조(원소 규칙 요약 등 — 단, 저작물 원문 아닌 일반 설명 수준)
- PWA 설치 최적화, `storage.persist()` UX

**트랙 T — 인게임 도구 패널 (Phase와 병행)**

축 ②의 **도구 자체는** 영속 계층·서버·권한 모델에 아무 의존이 없으므로 위 Phase 순서를 기다릴 필요가 없다. Dexie 스키마가 자리잡은 뒤 Phase 1과 병행해 진행한다.

**단 실시간 공유(5.4)는 예외다.** 파티·전투·인증이 있어야 하므로 Phase 2에 딸린다. 도구를 먼저 혼자 쓸 수 있게 만들고, 공유는 나중에 얹는다 — 그 순서라야 서버가 없을 때도 도구가 살아 있다.

- T1. 도구 패널 셸(라우팅 `/tools`, 컴팩트 레이아웃)
- T2. 원소 트래커
- T3. 공격 보정 덱 (덱 구성 편집 + 뽑기/셔플)
- T4. 주도권 정렬

각 도구는 자기 완결적이므로 하나씩 독립적으로 완성·검증한다. 도메인 로직(하강 상태기계, 덱 셔플·소진, 동점 정렬)은 UI와 분리해 Vitest로 테스트한다.

---

## 11. 확정 사항 요약 체크리스트

- [x] 앱 이름: 잠자는 사자 2호점 / Sleeping Lion II / 레포 `sleeping-lion-2` / 코드 `sl2`
- [x] 앱 정체성: 두 축 — ① 캠페인 기록지(영속, 본체) + ② 인게임 도구 패널(휘발성)
- [x] 인게임 도구는 원소 트래커 / 공격 보정 덱 / 주도권 정렬 / HP/XP 트래커 / 라운드 트래커 다섯으로 한정, 영속·동기화·캠페인 연결 없음
- [x] 본격 전투 트래킹(상태이상·몬스터 개체·피해 자동 적용)과 확률 계산은 여전히 범위 밖
- [x] 기준: GH 1판 + 사자의 턱
- [x] 프론트: React + TS + Vite, Zustand, 한국어 UI
- [x] 로컬 저장: IndexedDB(Dexie), offline-first
- [x] **운영 정책: 우리가 돌볼 서버를 두지 않는다.** 관리형 서비스로만 채우고 자가호스팅은 되돌아갈 길로도 두지 않는다 (4.2, 2026-08-05 확정)
- [x] 백엔드: Supabase를 관리형으로, Postgres. 접근 통제는 RLS (2026-08-05 — PocketBase 자가호스팅에서 변경)
- [x] 축 ②의 전투 상태는 Broadcast로 흘려보내고 저장하지 않는다 (4.2 / 5.4)
- [x] 인증: 초대 링크 + **진짜 이메일** + 비밀번호 (2026-08-05 — "이메일 미사용"에서 변경). 확인 메일·재설정은 필요해질 때 관리형 발송 서비스로
- [x] 정식 배포도 GitHub Pages. 레포당 사이트가 하나라 `live`가 그 자리를 쓰고 `mock`은 로컬로 (3.1)
- [x] 로그인 전에는 공지 페이지 외 아무 기능도 쓸 수 없음 (6.1)
- [x] 파티는 평평 — 누구나 만들고 누구나 초대, 여러 파티 가입 가능 (6.2)
- [x] 축 ②의 내부 상태는 전투 참여 중에만 공유, 배치는 개인 (5.4)
- [x] 동기화: 진행 상태만, last-write-wins + version 잠금, Realtime
- [x] 권한: 본인 캐릭터만 편집, 파티 상태는 파티장, 파티장 위임 가능
- [x] 다중 캠페인
- [x] 저작권(텍스트): 카드 원문·시나리오 서사·몬스터 스탯 미저장. 사용자 입력/데이터팩 로드
- [x] 저작권(아트): Creator Pack을 CC BY-NC-SA 4.0 조건 하에 선별 도입. `public/assets/creator-pack/`에 격리 (13.1장)
- [x] 코드 라이선스: MIT. 에셋은 각자 라이선스 (NOTICE.md)
- [x] 서체: Pirata One(OFL)만 사용. Nyala·High Tower Text는 재배포 불가라 금지
- [x] 안전망: JSON export/import
- [x] 기록 단위: 시나리오 정산 + 수동 보정 둘 다
- [x] 웹앱 기반 확정 (Tauri 등 네이티브 셸 미채택)
- [x] 참고 프로젝트: Gloomhaven Secretariat (13장, 등급별 활용 방침)

---

## 12. 열린 질문 (구현 중 확정 필요)

1. ~~**아이템/클래스 식별자 체계**~~ → **닫힘 (2026-08-07).** **클래스는 아이콘 번호, 아이템은 사용자가 친 글자.** 둘을 다르게 정한 이유는 성격이 다르기 때문이다.

   **클래스는 Creator Pack `Icon Pack/Class Icons and Augments.pdf`의 쪽 번호(1~21)로 저장한다.** 이름을 담지 않는다 — 클래스 이름은 게임 콘텐츠이고, 잠긴 클래스는 이름 자체가 스포일러다(3장). 아이콘은 봉투 겉면에 인쇄돼 있어 열기 전에도 보이는 것이라 가릴 것이 없고, 사람은 손에 든 봉투와 같은 그림을 찾으면 된다. 21종이면 글룸헤이븐 17 + 사자의 턱 4로 전부 덮인다. 0은 아직 안 고른 것이다.

   **아이템은 목록을 갖지 않고 사용자가 적는다.** 종류가 수백이고 확장마다 늘어나므로 번호를 매길 표를 우리가 만들 수 없다. 구현 결정 2가 이미 "사용자가 입력한 라벨"을 열어 두었다.

2. ~~**퍽 슬롯 표현**~~ → **닫힘 (2026-08-07).** **켜진 슬롯 번호의 배열.** 클래스별 퍽 시트 구조를 우리가 갖고 있지 않으므로 칸 수를 넉넉히 내주고(얻은 수 + 3, 최소 15) 사람이 제 시트에서 몇 번째 줄인지 보고 그 번호를 켠다.

   **얻을 수 있는 퍽의 수는 셈한다** — 레벨이 오를 때마다 하나(2레벨부터), 체크마크 셋마다 하나. 어느 퍽인지는 모르지만 몇 개인지는 규칙에서 나오는 수치다. 켠 수가 얻은 수를 넘어도 막지 않는다 — 규칙을 판정하지 않는다는 선을 여기서도 지킨다.
3. **데이터팩 JSON 스키마 상세:** 필드 정의를 Phase 3 진입 시 확정. (GHS 에디션 JSON 구조 참고 — 13장)
4. **초대 링크 수명·취소 정책 상세:** 만료 기본값, 1회용인지 N회용인지, 취소 UX.
5. **오프라인 큐 충돌 알림 UX:** 자동 last-write-wins 시 사용자 알림의 표현 방식.
6. **보정 덱 구성 편집 UX:** 캐릭터별 추가/제거 카드를 사용자가 어떻게 설정할지. 덱을 몇 개까지 동시에 띄울지(캐릭터 수만큼? 하나만?)도 함께 정한다.
7. ~~**원소 하강 트리거**~~ → **닫힘 (2026-08-02).** **라운드 트래커가 일괄 하강시킨다.** 매 라운드 여섯 번씩 누르는 것은 도구가 아니라 일이다. 개별 조작도 그대로 남으므로 어긋난 것만 손으로 고칠 수 있다.

   하강은 탭 순서와 **다르다.** 탭은 꺼짐 → 타오름 → 사그라듦으로 돌지만, 하강은 한 방향으로만 내려가고 바닥에서 멎는다 — 꺼진 원소가 라운드가 지났다고 다시 타오르지 않는다.

   **규칙을 판정하는 것은 아니다.** 언제 라운드가 끝나는지는 사람이 정하고, 우리는 실물에서 손으로 하던 일(표식을 한 칸씩 되돌리기)을 대신할 뿐이다.
8. ~~**도구 패널 배치**~~ → **닫힘 (2026-07-31).** 별도 경로 `#/satchel`로 둔다. 오버레이는 쓰지 않는다 — 플레이 중 폰으로 여는 전체 화면이므로 기록지 위에 겹칠 이유가 없다. 화면 이름은 **행낭**이고 식별자만 영어로 둔다.

   ---

   *아래 셋은 2026-08-05에 백엔드를 Supabase로 정하면서 새로 열린 것이다(4.2). 전부 N2에서 닫는다.*

9. ~~**아이디 로그인을 Auth에 어떻게 앉힐 것인가**~~ → **닫힘 (2026-08-05).** **진짜 이메일 주소로 로그인한다.** 아이디를 억지로 앉힐 일이 없어졌다. 확인 메일과 비밀번호 재설정은 당장 만들지 않고, 실제로 필요해지면 관리형 발송 서비스를 붙인다. 상세는 4.2.
10. ~~**무료 등급의 일시정지**~~ → **닫힘 (2026-08-05).** **GitHub Actions의 예약 실행(`schedule`)으로 주기적으로 가벼운 쿼리를 날려 깨워둔다.**

    예약 작업은 서버가 아니므로 4.2의 정책을 어기지 않는다. GitHub Actions는 이미 배포에 쓰고 있어 **새로 돌볼 것은 워크플로 파일 하나뿐**이고, 유료 등급($25/월)은 파티 넷이 쓰는 도구에 과하다.

    **한 가지 함정을 알고 간다.** GitHub은 **레포에 60일간 활동(커밋)이 없으면 예약 워크플로를 스스로 꺼버린다.** 판이 뜸한 기간은 대개 레포도 조용한 기간이므로, 깨우려고 둔 것이 같은 이유로 먼저 잠드는 일이 실제로 일어날 수 있다. N2에서 이것까지 함께 다룬다 — 꺼졌을 때 알아차릴 방법이 있어야 한다.
11. ~~**정식 배포를 어디에 올릴 것인가**~~ → **닫힘 (2026-08-05).** **GitHub Pages를 그대로 쓴다.** 레포 하나에 사이트가 하나뿐이라 "한 곳만 살린다"가 저절로 지켜지고, `mock`은 로컬에서 띄운다. 따라오는 제약 셋(서브경로·리다이렉트 등록·해시 라우터)은 3.1에 적었고 N2에서 다룬다.

---

## 13. 참고 프로젝트 및 에셋 출처 (Gloomhaven Secretariat 활용 방침)

**참고 대상:** [Gloomhaven Secretariat (GHS)](https://gloomhaven-secretariat.de/) — Lurkars가 만든 오픈소스 컴패니언 앱. Angular 기반 PWA, 소스 AGPL-3.0. ([GitHub](https://github.com/Lurkars/gloomhavensecretariat))

**주의 — 성격 차이:** GHS는 **본격 실시간 전투 트래커**(주도권·HP·상태이상·몬스터 개체·몬스터 덱·보정 덱)까지 포함한다. 우리(Sleeping Lion II)는 기록지가 본체이고 인게임 도구는 다섯으로 한정되므로, GHS의 전투 트래킹 대부분은 여전히 범위 밖이다.

참고 대상은 **캠페인/캐릭터/파티/시나리오 시트, 데이터 모델, UI 컨셉**, 그리고 우리가 채택한 세 도구(원소·보정 덱·주도권)에 한해 **그 UI 컨셉**까지로 한정한다. 여기서도 13장 표의 원칙은 그대로다 — 눈으로 보고 우리 식으로 재구현하며, AGPL 코드는 이식하지 않는다.

**저작권 등급별 활용 방침 (반드시 준수):**

| 구분 | 방침 |
|---|---|
| **컨셉·UX·레이아웃·데이터 모델** | 🟢 자유 참고. 아이디어·구조는 저작권 대상 아님. 우리 식으로 재구현. |
| **폰트 (Pirata One)** | 🟢 직접 채택. Google Fonts, SIL Open Font License(OFL). Gloomhaven 특유의 제목 폰트. 앱에 임베드 가능. |
| **아트 (클래스·원소·상태이상 아이콘 등)** | 🟡 조건부. 상당수가 Isaac Childres의 **Creator Pack (CC BY-NC-SA 4.0)**. 출처표시·비상업·동일조건공유 준수 시 사용 가능(우리 앱은 비영리이므로 NC 조건은 충족). **단 공개 배포로 전환했으므로 BY(출처표시)와 SA(동일조건공유) 의무가 실제로 발생한다** — 에셋을 쓰면 출처를 페이지에 표기해야 한다. **GHS 저장소에서 긁지 말고 Creator Pack 원본에서 받을 것** (GHS 번들엔 Worldhaven 등 라이선스가 다른 출처가 섞임). 각 에셋의 개별 라이선스를 사용 전 확인. |
| **GHS 소스 코드** | 🔴 참고만, 이식 금지. AGPL-3.0 카피레프트. 게다가 Angular↔React라 이식 불가. 눈으로 배우는 것만 허용. |
| **GHS 번들 게임 콘텐츠 (카드 원문·시나리오 서사·몬스터 스탯 텍스트)** | 🔴 미포함. GHS는 이를 번들하지만 우리 3장 원칙은 더 엄격하다. **콘텐츠 데이터 자체는 가져오지 않고**, JSON **스키마 형식(구조)만** 데이터팩 로더 설계에 참고한다. |

**요약 원칙:** 형식·구조·아이디어·OFL 폰트는 가져오고, **Cephalofair 텍스트 콘텐츠와 AGPL 코드는 가져오지 않는다.** 아트는 13.1장의 조건을 지켜 선별 도입한다.

---

## 13.1 Gloomhaven Creator Pack (아트 에셋 정본)

**출처:** https://boardgamegeek.com/thread/1733586/files-for-creation

**라이선스 근거 (1차 출처 확인됨).** 저작자 Isaac Childres(@Cephalofair, Designer/Publisher)가 해당 스레드 첫 글에서 직접 선언했다:

> Update 2: I would like to make it clear that I am sharing these assets under creative commons license **BY-NC-SA 4.0**. ... you can use these assets for non-commercial applications with proper attribution, and any derivative work based off these assets is protected under the same license as well.

### 지켜야 할 조건

| 조건 | 내용 |
|---|---|
| **BY** | 저작자 식별(Isaac Childres / Cephalofair Games), 저작권 표시, 라이선스 고지, 면책 고지, 원본 링크 유지 |
| **NC** | 비영리. 광고·유료화·판매 금지 |
| **SA** | **개변한 에셋은 그 결과물도 BY-NC-SA 4.0**이 된다 |
| 변경 표시 | 무엇을 어떻게 바꿨는지 명시 (의무) |

### 격리 규칙 (SA 전염을 막기 위해 반드시 지킨다)

SA는 개변한 에셋에만 미치고 나란히 놓인 우리 MIT 코드까지 끌어내리지 않는다(CC 용어로 collection과 adaptation의 차이). **단 그 경계는 파일이 분리돼 있을 때만 유지된다.**

1. Creator Pack 유래 에셋은 **`public/assets/creator-pack/` 안에만** 둔다.
2. **`.tsx`에 인라인 SVG로 박아 넣지 않는다.** 그 소스 파일이 개작물로 취급될 소지가 있다. (우리가 직접 그린 `LionCrest.tsx` 같은 원저작물은 무관하다.)
3. 팩 전체(약 200MB)를 통째로 커밋하지 않는다. **실제 쓰는 아이콘만 선별한다.**
4. 파일을 추가·수정할 때마다 `public/assets/creator-pack/ATTRIBUTION.md`의 표를 갱신한다.

### 팩에서 가져오지 않는 것

- **서체 파일.** 팩에 섞여 있어도 저작자가 라이선스할 수 있는 대상이 아니다(아래 참조).
- **텍스트 콘텐츠.** 카드 원문·시나리오 서사·몬스터 스탯 텍스트. 3장 자체 원칙.

### 서체 — 셋 중 하나만 쓸 수 있다

원 스레드는 게임에 쓰인 서체 셋을 알려주지만, **저작자가 라이선스한 것은 에셋이지 서체가 아니다.** 서체는 그의 소유가 아니며 단지 "게임에 이걸 썼다"는 정보다.

| 서체 | 판정 | 사유 |
|---|---|---|
| **Pirata One** | 🟢 사용 중 | SIL OFL 1.1 |
| **Nyala** | 🔴 금지 | Windows 번들 독점 서체. 재배포 불가 |
| **High Tower Text** | 🔴 금지 | Monotype 독점(MS Office 번들). 스레드의 다운로드 링크는 정식 배포처가 아니다 |

라틴 본문 세리프가 필요해지면 OFL 대체를 쓴다 — High Tower 자리엔 **EB Garamond**나 **Cardo**, Nyala 자리엔 **Gentium Book Plus**나 **Alegreya**.

### 상표는 별개

CC 라이선스는 상표권을 부여하지 않는다("Patent and trademark rights are not licensed under this Public License"). "Gloomhaven"·"Jaws of the Lion"·"Cephalofair"는 Cephalofair Games의 상표이며, 호환성을 설명하기 위해 **지명적으로만** 언급한다. 로고·워드마크는 쓰지 않고 면책 문구를 페이지에 유지한다.

### 미결 — 저작자 사전 연락 여부

같은 스레드에서 "이 파일들로 Vassal 모듈이나 앱을 만들어도 되냐"는 질문에 Isaac이 답했다:

> This is intended for creating new content. **If someone wanted to create a Vassal mod or an app, I'd prefer they still talk to me first.**

우리가 만드는 것이 정확히 그 '앱'이다. 시간순으로는 이 발언(2017-02-24)이 먼저이고 CC BY-NC-SA 선언(Update 2)이 나중이며, 부여된 CC 라이선스는 철회 불가이고 앱 용도를 배제하지도 않는다. 따라서 **법적으로는 CC 조건 준수로 충분하다고 판단**한다. 다만 공개 배포이고 저작자가 이 경우를 콕 집어 언급했으므로, 에셋을 실제로 도입하기 전에 한 번 연락해두는 편이 안전하다. **결정 보류 — 형님 판단.**
