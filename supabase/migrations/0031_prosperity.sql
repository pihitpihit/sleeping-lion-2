-- ============================================================================
-- 번영도
-- ----------------------------------------------------------------------------
-- ┌──────────────────────────────────────────────────────────────────────────┐
-- │ **판에 적힌 수 하나다 — 나머지는 셈해서 낸다.**                           │
-- └──────────────────────────────────────────────────────────────────────────┘
--
-- 실물 시트가 「번영도: 2」처럼 수를 적고, 그 수로 상점에 풀리는 아이템 카드가
-- 정해진다(`rules/prosperity.ts`). 떡갈나무에서 얻는 것은 **여기 더해지는 것이
-- 아니라 따로 보인다** — 어디서 온 번영도인지 갈려야 되짚을 수 있다.
-- ============================================================================

alter table public.campaigns
  add column if not exists prosperity smallint not null default 1
  check (prosperity between 1 and 9);
