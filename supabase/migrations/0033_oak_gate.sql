-- ============================================================================
-- 떡갈나무는 그 조건이 다 차야 열린다
-- ----------------------------------------------------------------------------
-- ┌──────────────────────────────────────────────────────────────────────────┐
-- │ **코드가 글을 읽어 알아내지 않는다 — 표에 표를 단다.**                    │
-- └──────────────────────────────────────────────────────────────────────────┘
--
-- 위대한 떡갈나무는 금화 100개 기부(B봉투)로 열린다(형님이 정했다). 그 조건은
-- 개봉 조건 표의 한 줄이고 **글은 DB에만 있어** 코드가 알아볼 수 없다 — 글자를
-- 뒤져 찾으면 문구를 한 번 고칠 때마다 조용히 어긋난다.
--
-- 그래서 그 줄에 표를 하나 단다. 켠 칸이 그 줄의 상자 수만큼 차면 열린다.
-- ============================================================================

alter table public.unlock_conditions
  add column if not exists opens_oak boolean not null default false;
