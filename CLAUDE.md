# CLAUDE.md — 프로젝트 지침 (Claude Code 자동 로드)

이 파일은 Claude Code가 세션 시작 시 자동으로 읽는 프로젝트 컨텍스트다.
전체 설계는 같은 폴더의 **`SPEC.md`를 먼저 읽고** 시작할 것.

---

## 앱 이름 (확정)

- **한국어 간판:** 잠자는 사자 2호점
- **영문 워드마크:** Sleeping Lion II
- **레포명:** `sleeping-lion-2`
- **코드 식별자 (PWA 앱ID·패키지명):** `sl2`
- 컨셉: Gloomhaven 시작 여관 '잠자는 사자'의 분점/아지트 — 우리 파티만의 공간.

---

## 이 프로젝트가 뭔가

Gloomhaven / Jaws of the Lion 용 웹앱. **두 축으로 구성된다** (SPEC.md 1장이 정본).

**축 ① 캠페인 기록지 (영속) — 본체.** 시나리오(전투)가 끝난 뒤의 정산 결과(골드·경험치·레벨업·퍽·아이템·시나리오 진행)를 기록·관리한다. IndexedDB 영속·서버 동기화·권한 모델은 전부 이 축의 것이다.

**축 ② 인게임 도구 패널 (휘발성).** 플레이 중 쓰는 작은 도구 여섯 — **원소 트래커 / 공격 보정 덱 / 주도권 정렬 / HP/XP 트래커 / 라운드 트래커 / 골드 카운터**. 이 여섯으로 한정한다. 전부 **실물 부품을 화면으로 옮긴 것**이며 규칙은 돌리지 않는다. SPEC 1장의 개정 주석이 정본. (2026-08-02 HP/XP·라운드, 2026-08-10 골드 카운터. 개발용 Test 위젯은 2026-08-10에 걷어냈다.)

축 ②의 제약 (위반 금지):
- **판이 끝나면 남지 않는다.** 메모리가 정본이고 `persist` 미들웨어·Dexie 테이블·`localStorage` 금지. **단 새로고침은 견딘다**(2026-08-08 개정, SPEC 5.2) — 혼자 쓸 때는 `sessionStorage`(탭을 닫으면 사라짐), 전투 중이면 서버(전투를 닫으면 지워짐). 가르는 선은 "메모리냐"가 아니라 **"판이 끝나면 남느냐"**다.
- **캠페인 데이터와 연결하지 않는다 — 읽는 것만 좁게 연다.** 캐릭터·캠페인 레코드를 **고치는 일은 없다.** 읽는 것은 ① 공격 보정 덱이 퍽을 읽는 것(2026-08-05) ② **캐릭터의 식별자·이름·클래스 표식**(2026-08-08) 둘뿐이다. ②가 필요한 까닭은 ①을 쓰려면 어느 캐릭터인지 고를 수 있어야 하고, **HP/XP와 덱이 전투에서 나뉘려면 파티원 모두가 알아보는 이름표가 있어야 하기 때문**이다 — 위젯 인스턴스 id는 기기마다 달라 넷이 앉아도 아무것도 안 겹친다. 골드·경험·아이템·메모는 읽지 않는다. **원소·라운드·주도권은 여전히 캠페인을 모른다** — 상 위의 사실이지 사람의 것이 아니다. SPEC 1장이 정본.
- **룰 엔진이 아니다.** 확률 계산·자동 판정 없음.

**2026-08-01 개정 — 전투 중 실시간 공유.** 위 목록에 "동기화 대상 아님"이 함께 있었다. 실물 원소판은 식탁 한가운데 놓인 공용물이라 각자 따로 세면 어긋난다 — 그래서 그 하나만 풀었다(SPEC 5.4). 나머지는 그대로다.
- **영속이 아니다.** 전투가 끝나면 사라진다. 어느 기기에도 남지 않는다.
- **옵트인이다.** 전투에 참여하지 않으면 종전대로 메모리 전용이고, 서버 없이 혼자 완전히 동작한다.
- **공유되는 것은 내부 상태뿐이다.** 위젯 배치는 각자의 것으로 남는다 — 남의 배치가 내 화면을 덮지 않는다.

**여전히 범위 밖:** 상태이상 카운트다운·몬스터 개체 관리·피해 자동 적용 등 본격 전투 트래킹, 보정 덱 확률 계산, 몬스터 능력 덱/스탯표(저작권 — 데이터팩 없이는 불가). HP/XP는 **숫자를 세는 것까지만** 들어왔다 — 최대 체력을 모르고 피해를 적용하지 않는다.

## 절대 원칙 (위반 금지)

1. **저작권(텍스트):** 카드 원문·시나리오 서사·몬스터 스탯 텍스트는 코드·데이터·서버 어디에도 넣지 않는다. **레포와 배포 사이트가 공개이므로**(SPEC 3장) 커밋하는 순간 그대로 공개된다.
1-1. **저작권(아트):** Creator Pack 에셋은 CC BY-NC-SA 4.0이라 **선별 도입은 허용**된다(SPEC 13.1장). 단 **`public/assets/creator-pack/`에만** 두고 **`.tsx`에 인라인 SVG로 박지 않는다** — SA가 소스 파일까지 번질 소지가 있다. 추가·수정 시 그 디렉토리의 `ATTRIBUTION.md` 표를 반드시 갱신한다.
1-2. **서체:** Pirata One(OFL)만 쓴다. **Nyala·High Tower Text는 독점 서체라 금지.** 라틴 세리프가 필요하면 EB Garamond / Cardo / Gentium Book Plus / Alegreya(전부 OFL). 콘텐츠는 식별자/인덱스/수치로만 표현. 텍스트는 사용자 입력 또는 사용자 소유 데이터팩(JSON) 로드로만 채운다. 데이터팩 파일 자체는 배포물에 동봉하지 않는다.
2. **서버 동기화 경계:** 서버로는 순수 진행 상태(수치·식별자)를 동기화한다. **저작물성 콘텐츠는 레포와 배포물에 절대 넣지 않는다** — 마이그레이션 SQL도 레포다.
   - **2026-08-11 개정 — 클래스 수치는 DB에 둔다.** 원래 문구는 "저작물성 콘텐츠는 각 기기 로컬에만"이었다. 막으려던 것은 **공개 배포**이고 그 선은 그대로인데, **우리 DB는 공개가 아니다** — RLS로 승인된 사람만 읽는다. 클래스 이름·핸드 사이즈·레벨별 최대 체력이 `character_classes`에 들어가며, **값은 앱 안의 관리자 화면에서 넣는다**(레포에는 표와 정책만). 카드 원문·시나리오 서사·몬스터 텍스트는 여기에도 안 넣는다. SPEC 3장이 정본.
3. **offline-first:** 서버 없이 로컬(IndexedDB)만으로 완전히 동작해야 한다. 서버는 백업·동기화·공유용 부가 계층일 뿐이다.
   - **로그인 게이트도 이 원칙을 깨지 않는다.** 게이트의 기준은 "서버에 닿는가"가 아니라 **"이 기기에 유효한 세션이 있는가"**다(SPEC 6.1). 첫 로그인만 네트워크를 요구한다. 매 실행마다 서버에 물으면 신호 없는 자리에서 앱이 통째로 멈춘다.
   - **실시간 공유가 끊겨도 판은 멈추지 않는다.** 혼자 모드로 강등하고 돌아올 때 한 번 맞춘다(SPEC 5.4).
4. **우리가 돌볼 서버를 두지 않는다 (2026-08-05 확정).** 기계도, VPS도, 컨테이너도 우리 손으로 돌리지 않는다. 서버 쪽 필요는 **관리형 서비스로만** 채운다.
   - **이것이 백엔드를 고른 축이다.** PocketBase가 탈락한 것은 제품이 나빠서가 아니라 **관리형 선택지가 아예 없어서**다 — 그 제품을 고르면 라즈베리파이가 딸려온다. Supabase는 관리형으로 쓴다. SPEC 4.2가 정본.
   - **자가호스팅은 되돌아갈 길로도 두지 않는다.** Supabase는 오픈소스라 자가호스팅이 가능하지만 그 길은 택하지 않는다. "언제든 옮길 수 있다"고 적어두면 그것이 곧 계획이 된다.
   - **금지 대상은 '기계'다.** 우리가 띄우고 지켜봐야 하는 것(서버·VPS·컨테이너)을 두지 않는다는 뜻이지 무료 등급을 쓰지 말라는 뜻이 아니다. **남의 관리형 위에서 도는 예약 작업은 기계가 아니다** — 무료 등급의 일시정지는 GitHub Actions 예약 실행으로 넘긴다(SPEC 12장 10, 확정). 기계를 두어야만 풀리는 문제라면 그때 돈을 낸다.
   - 이 원칙은 절대 원칙 3(offline-first)과 짝이다. **서버는 있어도 그만인 계층이므로** 남에게 맡겨도 판이 위태롭지 않다.

## 스택

- React + TypeScript + Vite
- 상태: Zustand
- 로컬 영속: IndexedDB via Dexie.js
- 백엔드: **Supabase를 관리형으로** (Postgres) — 인증은 초대 링크 + **진짜 이메일** + 비밀번호, 접근 통제는 RLS
  - **지어낸 주소를 쓰지 않는다.** 나중에 비밀번호 재설정 메일을 보낼 길이 막힌다. 확인 메일·재설정은 당장 만들지 않고 필요해지면 관리형 발송 서비스를 붙인다(SPEC 4.2).
  - **이메일은 로그인에만 쓰고 화면에 내보내지 않는다.** 표시용 이름은 `profiles`에 따로 둔다.
- 배포: **정식도 GitHub Pages.** 레포당 사이트가 하나라 `live`가 그 자리를 쓰고 `mock`은 로컬에서만 띄운다(SPEC 3.1).
  - 2026-08-05에 **PocketBase 자가호스팅에서 바꿨다.** 절대 원칙 4(돌볼 서버를 두지 않는다)와, Broadcast가 SPEC 5.4("전투가 끝나면 어느 기기에도 남지 않는다")를 절충 없이 지키기 때문이다. SPEC 4.2가 정본.
- 실시간: Supabase Realtime — **축 ①은 Postgres Changes, 축 ②는 Broadcast**(저장하지 않는다)
- 테스트: Vitest
- 린트/포맷: ESLint + Prettier
- PWA 구성 (`navigator.storage.persist()` 포함)
- UI: 한국어 우선, i18n 구조(react-i18next 등)

## 코딩 규약

- 함수형 컴포넌트만.
- 기능별(feature-based) 폴더 구조.
- 도메인 로직(정산 계산, 동기화 병합)은 UI에서 분리하고 Vitest로 테스트.
- 타입은 SPEC.md의 데이터 모델을 기준으로 정의.

## 참고 프로젝트 & 에셋 방침 (SPEC.md 13장이 정본)

참고: **Gloomhaven Secretariat (GHS)** — https://gloomhaven-secretariat.de/ , Angular PWA, 소스 AGPL-3.0.
GHS는 본격 실시간 전투 트래커까지 포함하므로, **캠페인/캐릭터/파티/시나리오 시트·데이터모델·UI 컨셉**, 그리고 우리가 채택한 세 도구(원소·보정 덱·주도권)의 **UI 컨셉**까지만 참고한다. 코드 이식은 금지.

저작권 등급별 방침:
- 🟢 컨셉·UX·데이터모델 → 자유 참고, 우리 식으로 재구현.
- 🟢 폰트 **Pirata One** (OFL) → 직접 임베드. Gloomhaven 특유의 제목 폰트.
- 🟡 아트(클래스·원소·상태이상 아이콘 등) → 대부분 **Creator Pack (CC BY-NC-SA 4.0)**. 출처표시·비상업·동일조건공유 준수 시 사용. **GHS 저장소가 아니라 Creator Pack 원본에서 받고**, 각 에셋 라이선스를 사용 전 확인.
- 🔴 GHS 소스 코드(AGPL) → 참고만, 이식/복붙 금지.
- 🔴 GHS 번들 게임 콘텐츠(카드 원문·시나리오 서사·몬스터 텍스트) → 미포함. JSON **스키마 형식만** 데이터팩 로더 설계에 참고.

## 진행 순서 (SPEC.md 로드맵 참조)

**Phase 1 (지금 시작):** 로컬 전용 캠페인 기록.
- IndexedDB/Dexie 스키마 + 타입 정의
- 다중 캠페인 CRUD
- 파티 시트, 캐릭터 시트
- 시나리오 정산 기록 + 수동 보정
- JSON export/import
- **서버 없이 로컬만으로 완결되는 상태를 먼저 만든다.**

**Phase 2:** Supabase 동기화·다플레이어·권한.
**Phase 3:** 데이터팩 로더, 정적 규칙 참조, PWA 최적화.

**트랙 T (Phase 1과 병행):** 인게임 도구 패널. 영속 계층에 의존이 없으므로 Phase 순서를 기다리지 않는다.
- T1 도구 패널 셸 → T2 원소 트래커 → T3 공격 보정 덱 → T4 주도권 정렬
- 각 도구의 도메인 로직(하강 상태기계, 덱 셔플·소진, 동점 정렬)은 UI와 분리해 Vitest로 테스트.

---

## 구현 결정 기록

SPEC.md에 없던 결정을 여기 남긴다. 뒤집을 수 있으나, 뒤집을 때는 이 항목을 고칠 것.

1. **캐릭터 현재 상태의 정본은 `Character` 레코드의 필드다.** `ScenarioRecord`·`ManualAdjustment`는 이력 로그이며, 정산 입력 시 델타를 계산해 캐릭터 필드에 적용한다. 되돌리기는 역델타 적용으로 처리. — 이벤트 소싱(기록을 폴드해 현재값 산출)을 택하지 않은 이유는 SPEC 5.3의 레코드 단위 last-write-wins 동기화와 충돌하기 때문이다.
2. **퍽·아이템은 인덱스/식별자 기반이되, 사용자가 입력한 라벨을 로컬 전용 필드로 옵션 저장한다.** 동기화 대상에서 제외. SPEC 3장이 "사용자 직접 입력"을 허용하므로 저작권 원칙에 어긋나지 않으며, 인덱스만으로는 사용자가 실물 퍽 시트를 세어야 하는 UX 구멍을 메운다.
3. **`ownerUserId`·`version`·`updatedAt`는 Phase 1부터 스키마에 포함한다.** 로컬 전용 단계에선 쓰이지 않지만 Phase 2 마이그레이션 비용을 줄인다. `ownerUserId`는 nullable.

### 행낭(축 ②) 구현 결정 — 2026-07-31

4. **격자 라이브러리를 쓰지 않고 직접 만들었다.** react-grid-layout류는 자유형 대시보드용이라 과하다. 직접 만들어야 배치 로직을 순수 함수로 떼어 Vitest로 검증할 수 있고(SPEC 4.1 요구), 번들도 작다. 대가로 밀어내기(reflow)는 포기하고 **겹침 금지 + 못 놓으면 원위치**로 간다.
5. **열 수는 구간표가 아니라 목표 셀 크기(84px)에서 역산한다.** 구간표는 넓은 화면에서 셀이 100px을 넘어 "앱 아이콘 수준"에서 멀어졌다. 역산하면 360~2560px 전 구간에서 셀이 75~89px로 모인다. 격자 폭 상한은 1600px.
5-1. **셀은 정사각형이 아니다. 가로는 폭을, 세로는 높이를 각자 채운다**(2026-08-10). 한 변으로 두면 **한쪽을 채울 때 다른 쪽이 남는다** — 아이폰 15 Pro에서 여덟 줄에 맞춰 높이를 74px로 줄이자 폭에도 같은 값이 쓰여 45px가 남았고, 그것이 좌우로 갈려 빈 띠가 되었다. 두 축을 따로 잡으면 85×77로 양쪽이 다 찬다.
5-2. **줄 수를 막는 것은 셀 하한(72px)과 납작함 한도(폭의 0.85배)뿐이다.** "얼마나 남았는가"로 멈추는 조건을 두 번 두었다가(반 칸 → 간격 한 칸) 두 번 다 걷어냈다 — 둘 다 15 Pro의 여덟째 줄을 막았다. 납작함 한도는 반대쪽을 막는다: 15 Pro Max가 아홉 줄까지 가면 94×76으로 납작해지는데 여덟 줄이면 94×87로 반듯하다. **줄 하나보다 반듯한 칸이 낫다.** 위쪽 한도(1.25배)는 가로 폰처럼 높이가 크게 남는 자리에서 셀이 82×140으로 어긋나는 것을 막는다.
5-3. **아래쪽 안전영역은 절반만 비운다.** 통째로 비우면 격자 아래에 45px짜리 띠가 남고, 화면 모서리가 둥근 자리라 더 크게 보인다. 홈 인디케이터 막대는 아래에서 8px쯤에 5px 두께로 눕는다 — 절반을 남기면 막대 위로 위젯이 오지 않으면서 띠는 절반으로 준다. 좌우와 위는 그대로 다 비운다(노치와 곡면이 실제로 화면을 먹는다).
5-4. **격자 수치를 도구 띠 메뉴 아래에 적는다.** 기기의 실제 보드 크기는 화면·안전영역·홈화면 여부마다 달라 손으로 셈해 맞힐 수 없다 — 두 번 빗나가고 나서 붙였다.
6. **레이아웃은 열 수를 키로 따로 저장한다.** 처음 보는 열 수는 가장 가까운 것에서 파생한다. **가로(x·w)만 비율로 옮기고 세로는 옮기지 않는다** — 행 수는 열 수와 무관하게 높이에서 나온다.
7. **툴바 두께는 모드와 무관하게 고정한다.** 편집 모드에서 띠가 두꺼워지면 보드가 좁아지고 격자가 다시 계산된다. 그러면 편집하다 플레이로 돌아갈 때마다 배치가 흔들린다. (2026-08-10 — 연장 목록을 `+` 단추의 메뉴로 접으면서 띠에는 아이콘만 남았다. 목록이 길어져도 띠는 그대로다.)
8. **툴바 기본 위치는 UA가 아니라 뷰포트 크기·방향으로 정한다.** UA 판별은 틀리고 낡는다.
9. **드래그 중에는 저장하지 않는다.** 미리보기만 들고 있다가 손을 뗄 때 확정·저장한다.

### 원소 효과 구현 결정 — 2026-07-31

10. **눈으로 골라야 하는 것은 `lab/`에서 고른다.** 시각 효과는 코드를 읽어 판단할 수 없고, 고칠 때마다 커밋·배포·재확인을 도는 것은 너무 느리다. `lab/*.html`은 브라우저로 직접 열리는 한 파일이며 값을 실시간으로 만진다. **배포에 섞이지 않는다** — Vite는 루트 `index.html`만 진입점으로 삼고 `public/` 바깥은 dist에 넣지 않으므로, `lab/`은 둘 다 아니다. 고른 값은 `값 내보내기`로 뽑아 컴포넌트 상수로 옮기고, 그 상수 곁에 실험실 막대 이름(`e-count` 등)을 적어 되짚을 수 있게 한다.
11. **원소 효과는 메타볼로 만든다.** 흐린 원을 알파 문턱으로 잘라내면 서로 닿은 것들이 하나로 이어지고 가장자리가 다시 또렷해진다. 레이어드 물방울(형태가 안 살고 빛무리로 뭉개짐)·난류 일그러짐(`feTurbulence`가 매 프레임 지형을 다시 만들어 비쌈)·캔버스 입자(합성 이점을 잃음)와 견줘 고른 것이다. **불투명도를 애니메이션하지 않는다** — 알파가 문턱 아래로 떨어지면 서서히 스러지는 대신 툭 잘린다. 크기를 줄이면 흐림에 묻혀 저절로 사라진다.
12. **렌더 중 `Math.random()`을 쓰지 않는다.** `react-hooks/purity`가 막는다(같은 입력에 같은 결과여야 렌더를 되돌릴 수 있다). 인스턴스 id를 씨앗으로 삼는 순수 난수기를 쓴다 — 위젯마다 다른 모양이 나오면서도 한 위젯은 몇 번을 다시 그려도 같은 모양으로 돌아온다. 석판을 끄는 동안 계속 다시 그려지므로 이게 없으면 끌 때마다 불이 새로 붙는다.
13. **뭉침 필터는 인스턴스마다 따로 둔다.** 흐림 반지름이 아이콘 크기에 비례하므로 `id`를 나눠 쓰면 한 위젯의 크기가 다른 위젯의 뭉침까지 바꾼다. `useId()`로 가르되 콜론을 빼야 `url(#...)`에 쓸 수 있다.
14-1. **고급 효과는 스위치 하나로 끈다(`effects/registry.ts`의 `ADVANCED_EFFECTS`).** 지금은 **꺼져 있고** 모든 원소가 공통 빛무리로 돌아간다. 사파리·아이폰에서 크롬과 다르게 보인다는 이야기가 있었는데 헤드리스 WebKit으로는 재현되지 않았다 — 그것은 진짜 사파리가 아니다(애플의 GPU 합성 경로가 빠져 있다). 여러 기기에서 안정된 것을 확인한 뒤 다시 켠다. **코드는 지우지 않는다** — 지웠다 되살리면 그동안 고른 값과 알아낸 것이 함께 사라진다. 켜기 전에 `lab/fire.html`의 '사파리 진단' 스위치로 실기에서 확인할 것.
14-2. **빛무리 색은 아이콘 색과 따로 간다(`glowOf`).** 아이콘 색이 늘 빛으로 쓸 만한 것은 아니다. 어둠은 아이콘이 거의 검정(`#202830`)이라 제 색으로 빛나면 어두운 바탕에 묻혀 타오르는지 알 수 없다. 어둠만 안쪽 보라(`#A06CFF`) → 바깥 파랑(`#4C6FF5`) 두 겹을 준다. **아이콘 색 자체는 원본을 지킨다** — 바꾸는 것은 빛뿐이다.
14-3. **원소 트래커의 배치 방향은 위젯의 가로세로 비가 아니라 두 갈래를 다 재 보고 고른다.** 비만 보면 원소를 하나만 켠 채 넓고 낮게 놓았을 때 어긋난다 — 넓으니 수평을 고르는데, 수평에서 석판이 미끄러지는 축은 '높이'라 낮으면 슬라이딩이 죽는다. 칸이 하나면 늘어놓을 것이 없으므로 **긴 쪽을 미끄러지는 축으로** 써야 한다. 고르는 순서는 ① 아이콘이 큰 쪽 ② 비슷하면 미끄러지는 쪽 ③ 그래도 같으면 세로(정사각형에서의 기존 선택). 두 방향 모두 아이콘을 짧은 변에 맞추므로, 한 칸일 때 슬라이딩은 **아이콘을 깎지 않고 얻는다**.
15. **에셋에 거는 시각 효과는 파일을 고치는 것과 구별한다.** 달아오른 석판은 Creator Pack 아이콘에 CSS 필터를 걸어 만든다. 화면에 그릴 때만 거는 것이라 사본을 고쳐 커밋하는 것과 다르다(SPEC 13.1). **에셋 파일 자체는 건드리지 않는다.**

### 위젯 회전·공용 상태 구현 결정 — 2026-08-02

23. **원소 상태는 행낭 전체에 하나뿐이다.** 처음에는 인스턴스별로 나눠 가졌는데 실물을 생각하면 틀렸다 — 식탁 위 원소판은 하나이고 트래커 위젯은 그것을 비추는 창이다. 한 위젯에서 불이 타오르는데 다른 위젯에서 꺼져 있으면 어느 쪽이 판의 상태인지 알 수 없다. 그래서 **위젯을 여럿 놓는 뜻도 달라진다** — 다른 상태를 보려는 것이 아니라 같은 상태를 다른 자리·다른 방향에서 보려는 것이다.
24. **모든 위젯은 90도 단위로 돈다.** 보드게임 상은 마주 앉거나 사방에서 가운데를 본다. 태블릿을 상 가운데 놓으면 누군가에게는 모든 것이 거꾸로다. 임의 각도는 두지 않는다 — 앉는 자리가 넷이다.
25. **회전은 레이아웃 바깥에 인스턴스별로 저장한다**(`widgetRotations`). `widgetSettings`와 같은 이유다 — 레이아웃은 열 수마다 따로 저장되지만 **어느 쪽에 앉았는지는 화면 폭과 무관하다.** 안에 넣으면 폰과 태블릿에서 방향이 따로 논다.
26. **90·270도는 가로세로를 바꿔 그린 뒤 돌린다.** 그래야 돌린 결과가 원래 칸을 정확히 채운다. 위젯은 자기 영역을 관측해 스스로 배치하므로 뒤바뀐 모양에 알아서 맞춘다. 0·180도도 같은 경로를 타게 두어 갈래를 하나로 유지한다 — 회전이 0일 때만 다른 길을 타면 그 길에서만 나는 어긋남이 생긴다.
27. **편집 단추는 회전과 함께 돌지 않는다.** 같이 돌리면 크기 핸들의 방향까지 뒤집혀 오른쪽으로 끌면 줄어드는 꼴이 된다. 조작하는 손은 화면을 마주 보고 있으므로 화면 기준으로 둔다. 대신 **제목 띠는 내용과 함께 돈다** — 마주 앉은 사람에게 제목만 거꾸로면 곤란하다.
29. **HP/XP 값은 인스턴스마다 따로 갖는다 — 원소와 반대다.** 원소판은 식탁 위에 하나뿐이라 모든 트래커가 같은 것을 비춰야 했지만, 체력과 경험은 사람마다 다르다. 넷이 앉으면 다이얼도 넷이다. **위젯을 치워도 값을 지우지 않는다** — 되돌리기로 다시 놓으면 숫자가 돌아온다.
30. **HP/XP의 안쪽 자리는 계산으로 나눠 준다.** flex에 맡겼더니 창이 눌려 숫자가 잘렸고, 창을 고정하자 손잡이가 눌려 타원이 됐다. 한 줄에 넷(표식·손잡이·창·손잡이)이 늘어서므로 서로 밀어낼 뿐이다. **여백까지 한 곳에서 정해 픽셀로 내려보낸다** — CSS가 %로 따로 정했을 때 계산과 어긋나 가운데서 두 `+` 단추가 겹쳤다.
32. **라운드는 행낭 전체에 하나뿐이다(원소와 같다).** 판의 사실이지 사람의 것이 아니다 — 트래커를 둘 놓았는데 하나는 3라운드, 다른 하나는 5라운드일 수 없다. 체력·경험이 사람마다 다른 것과 반대다.
33. **하강은 탭 순서와 다른 함수다(`decayElementState`).** 탭은 꺼짐 → 타오름 → 사그라듦으로 **돌지만**, 하강은 한 방향으로만 내려가고 바닥에서 **멎는다**. 같은 함수를 돌려 쓰면 꺼진 원소가 라운드가 지났다고 다시 타오른다.
34. **라운드와 원소를 잇는 자리는 `roundStore` 한 곳이다.** 화면 쪽에 두면 트래커를 두 개 놓았을 때 어느 쪽이 내리는지 흐려진다. 실물에서 라운드를 넘기는 것과 표식을 되돌리는 것은 한 동작이다.
35. **도는 숫자 띠의 창은 정확히 한 칸이다(`NumberReel`).** 창이 한 칸보다 크면 이웃 숫자의 잉크가 반드시 걸린다 — 흐림을 아무리 다듬어도 걸리는 자리를 없앨 수 없다. 1.62 → 1.3 → 1.14em을 거쳐 알았다. 글자가 제 칸을 다 쓰지 않으므로(잉크는 0.7em 남짓) 한 칸이면 이웃이 들어올 자리가 아예 없고, 도는 동안의 미끄러짐은 그대로 보인다.
31. **실물 카드의 그림을 베끼지 않는다.** 붉은 반쪽·푸른 반쪽·육각 창·빛나는 테라는 얼개만 따르고 문양과 표식은 직접 그린 도형이다. Creator Pack 에셋도 아니므로 격리 규칙(1-1)에 걸리지 않는다.
28. **돌리기 단추는 이끼색(`--sl-moss`)이다.** 치우기(잉걸빛)와 한눈에 갈려야 한다 — 하나는 되돌릴 수 있고 하나는 없앤다. 톱니가 없는 위젯에서는 톱니 자리를 대신 차지한다.
36. **되돌릴 수 없는 일은 `window.confirm`으로 묻지 않는다(`board/ConfirmDialog`).** OS 팝업은 테마 밖이라 앱이 잠깐 남의 것이 되고, **기본 초점이 '확인'에 가 있어** 엔터 한 번에 판이 날아가며, 무엇보다 뜸을 들일 수가 없다. 우리 팝업은 **처음에 무르는 쪽(닫기·취소·배경·Escape)만 눌리고 실행은 5초 뒤에 살아난다** — 손가락이 이미 그 자리에 있으므로 두 번째 탭이 첫 번째 탭의 관성으로 나간다. 초점은 취소에서 시작하고 **살아나도 옮기지 않는다**(엔터를 누르고 있던 손이 그대로 민다). 처음에는 3초로 두었다가 형님이 5초로 정했다 — 되돌릴 수 없고 몇 시간짜리 판이 걸려 있으므로 손이 멎는 값이 조금 비싸도 괜찮다.
37. **팝업은 `createPortal`로 `document.body`에 그린다.** 위젯 틀은 회전 때문에 늘 `transform`을 걸고 있고, **`transform`이 걸린 조상은 `position: fixed`의 기준이 된다.** 그냥 두면 팝업이 위젯 안에 갇히고 위젯과 함께 거꾸로 선다. 90도 돌린 트래커에서 확인했다.
39. **숫자와 영문만 들어가는 자리는 Pirata One이 기본이다(`.sl-numeral`, `index.css`).** CSS로는 "이 글자만 숫자다"를 고를 수 없으므로 규칙 대신 표를 둔다. **한글이 섞이는 자리에는 붙이지 않는다** — Pirata One은 라틴 전용 subset이라 한글이 대체 서체로 떨어져 한 줄에 두 서체가 섞인다. 그래서 라운드 트래커의 이름표는 `ROUND`다. 화면에 영문만 남지만 읽어 주는 쪽에는 한국어 `aria-label`이 그대로 간다. **굵기는 주지 않는다** — 한 벌뿐이라 브라우저가 가짜 굵기를 만들어 획이 뭉갠다.
38. **남은 초는 올려 센다(`secondsLeft`).** 4999ms를 '4'로 적으면 5초짜리 뜸이 4초로 시작해 1초가 통째로 사라져 보이고, 0.4초 남았을 때 '0'이 적히면 눌러도 안 먹는 고장으로 읽힌다. 숫자와 채워지는 띠는 **한 값에서** 뽑는다 — 따로 돌리면(CSS 애니메이션 + JS 타이머) 탭이 뒤로 갔다 올 때 띠는 다 찼는데 단추는 잠긴 꼴이 난다.

### 로그인·네트워킹 구현 결정 — 2026-08-01

16. **게이트는 세션이 있느냐로 판단한다.** 매 실행마다 서버에 물으면 절대 원칙 3이 죽는다. 첫 로그인만 네트워크를 요구하고 이후엔 저장된 세션으로 오프라인에서도 연다. **클라이언트의 게이트는 UX일 뿐이고 강제는 Supabase RLS 정책이 한다** — 레포와 번들이 공개라 화면을 가리는 것은 아무것도 막지 못한다.
17. **공지 페이지(`#/notice`)만 게이트 밖에 둔다.** Creator Pack의 출처표시 의무는 로그인 여부와 무관하게 배포에서 발생한다(SPEC 13.1). 표기를 게이트 뒤에 숨기지 않는다.
18. **세 배포는 빌드 시점에 가른다.** 런타임 분기로 두면 한 번들이 세 모드를 다 알게 되어 `mock`이 진짜 데이터에 닿는 사고가 설정 한 줄로 난다. **플래그가 없으면 로그인을 요구하는 쪽이 기본이다** — 반대로 두면 설정 하나 빠뜨렸을 때 정식 배포가 활짝 열린다.
    - **2026-08-05 — 근거가 바뀌었다.** 원래 이유는 "라즈베리파이 주소를 공개 번들에서 감춘다"였으나 백엔드가 Supabase가 되면서 사라졌다. `anon` 키는 공개해도 되는 값이고 보호는 RLS가 한다. 가르는 것은 그대로 두되 이유만 옮겼다(SPEC 3.1). **`service_role` 키는 클라이언트에 절대 넣지 않는다** — 그것은 RLS를 우회한다.
19. **파티(지속)와 전투(일시)를 나눈다.** 파티는 사람들의 묶음이고 전투는 이번 판에 모인 사람들이다. 공유 단위를 파티로 두면 한 파티가 여러 판을 동시에 돌릴 때 엉키고, 캠페인으로 두면 축 ②가 캠페인을 읽게 되어 두 축의 경계가 무너진다. **전투는 둘 다 피한다.**
20. **파티는 평평하다.** 누구나 만들고, 파티원이면 누구나 초대하고, 누구나 전투를 연다. 여러 파티에 속할 수 있다. SPEC 6장의 "파티장"은 **축 ①의 편집 권한**에 대한 것이며 여기 섞지 않는다. 원소판은 공용물이므로 전투 참여자 누구나 만진다.
21. **초대는 링크로 한다.** 코드를 불러주는 대신 메신저로 보낸다. 링크는 전달되고 캡처되므로 **추측 불가 토큰 + 만료 + 취소**가 함께 있어야 하고, 링크만으로는 아무 데이터도 보이지 않는다(로그인이 먼저).
22. **축 ②의 충돌 처리는 축 ①과 다르다.** 기록지는 커밋 단위로 밀어도 되지만 원소판은 초 단위로 여럿이 만진다. **탭은 로컬에서 즉시 반영하고 서버 확정이 뒤따라 덮는다.** 왕복을 기다리면 손가락이 미끄러진다.

## 시작 시 첫 작업 제안

1. `SPEC.md` 정독.
2. Vite + React + TS 프로젝트 초기화, ESLint/Prettier/Vitest 설정.
3. SPEC.md 7장 데이터 모델을 TypeScript 타입 + Dexie 스키마로 옮긴다.
4. 다중 캠페인 목록 → 캠페인 상세(파티/캐릭터/시나리오) 라우팅 골격.
5. 이후 캐릭터 시트와 시나리오 정산 입력 폼.

막힌 결정은 SPEC.md 12장 `열린 질문`을 참고하고, 거기 없는 새 결정이 필요하면 합리적 기본값으로 진행하되 그 선택을 코드 주석과 이 파일에 기록한다.

### 캐릭터 시트 구현 결정 — 2026-08-07

40. **클래스는 이름 대신 아이콘 번호다.** Creator Pack `Class Icons and Augments.pdf`의 쪽 번호(1~21)를 저장하고 화면에는 그림만 늘어놓는다. 이름은 게임 콘텐츠이고 **잠긴 클래스는 이름 자체가 스포일러**다. 아이콘은 봉투 겉면에 인쇄돼 있어 열기 전에도 보이는 것이라 가릴 것이 없다 — 사람은 손에 든 봉투와 같은 그림을 찾으면 된다. 읽어주는 쪽에도 번호로 간다. 이름을 지어내면 그것이 곧 담지 않기로 한 콘텐츠가 된다. SPEC 12장 1을 이렇게 닫았다.
41. **클래스 아이콘의 색을 건드리지 않고 양피지 원반을 깐다.** 글룸헤이븐 열일곱은 거의 검정이라 어두운 바탕에서 묻히는데, 원소·HP 표식처럼 흰빛으로 물들이면 사자의 턱 넷(색 있는 원반)까지 함께 표백된다. 바탕을 깔면 검정은 검정대로 색은 색대로 보인다. **에셋 파일은 그대로다**(구현 결정 15와 같은 선).
42. **눈금은 실물 시트에서 읽었다.** 레벨 1~9와 0/45/95/150/210/275/345/420/500, 체크마크 세 칸짜리 여섯 묶음 — Creator Pack `Character Sheet/Character Sheet.pdf`를 150dpi로 뽑아 눈으로 확인했다. 수치의 나열이라 SPEC 3장의 경계 안이다(보정 덱 구성·평판 구간표와 같은 성격).
43. ~~**레벨을 대신 올려주지 않는다.**~~ **2026-08-12에 뒤집혔다 — 레벨은 경험치가 정한다.** 원래는 "올릴 때가 되었다"고 알리기만 하고 사람이 눈금을 눌렀다. 형님이 뒤집었고 그 말이 옳다 — **눈금은 표에 적힌 사실이지 사람이 정할 것이 아니다.** 레벨업에 딸린 일(퍽 고르기·능력 카드 바꾸기)은 여전히 사람이 하지만 그것은 시트 밖의 일이다. 켠 퍽이 얻은 수를 넘어도 막지 않는 것은 그대로다 — **규칙 판정은 축 ①에서도 하지 않는다.**
44. **캐릭터는 파티원이 다 보고 고치는 것은 제 것만이다**(SPEC 6장). 기록지가 파티원 누구나 고치는 것과 다르다 — 파티 상태는 함께 쓰는 것이고 캐릭터는 제 것이다. 남의 것도 목록에 오는 이유는 누가 몇 레벨이고 무엇을 들었는지 보이지 않으면 같이 놀 수 없기 때문이다. **막는 것은 RLS다**(`0005_characters.sql`); 화면에서 잠그는 것은 헛손질을 줄이는 것뿐이다.
45. **`owner_id`가 `auth.users`가 아니라 `public.profiles`를 가리킨다.** 둘 다 같은 id이지만 **PostgREST는 자기가 보는 스키마 안의 외래키만 따라간다.** `auth`는 그 바깥이라 캐릭터마다 주인 이름을 따로 물어야 한다.
46. **캐릭터 스토어를 기록지 스토어와 나눴다.** 되살아나는 주기가 다르다 — 기록지는 목록 화면에서도 필요하고 캐릭터는 기록지를 펼친 뒤에야 필요하다. 한데 묶으면 목록을 새로 읽을 때마다 캐릭터도 함께 읽는다.
47. **캐릭터도 Dexie 거울을 갖는다(스키마 3).** 골드와 경험은 지하에서 세 시간 하는 동안 계속 들여다보는 값이라 신호가 끊겨도 보여야 한다. 기록지와 같이 **쓰기는 거울에 하지 않는다.**
48. **컴포넌트 확인은 `react-dom/server`로 문자열까지만 뽑는다.** jsdom을 붙이지 않았다 — 마운트 중에 터지지 않는다는 것과 권한에 따라 칸이 잠기는지는 정적 렌더로 확인되고, 색·간격은 어차피 브라우저에서 봐야 한다. `vite.config.ts`의 `include`가 `.tsx`까지 받는 이유다.

### 간판·아이콘 구현 결정 — 2026-08-08

49. **간판은 형님이 만들어 온 원반 그림이다.** Creator Pack이 아니라 우리 것이라 격리 규칙(1-1)에 걸리지 않으며 `public/assets/sign/`에 따로 둔다. 도형으로 그렸던 `LionCrest`와 그것으로 만든 파비콘은 지웠다 — 쓰는 데 없이 남겨두면 다음에 아이콘을 손볼 때 어느 쪽이 진짜인지 헷갈린다.
50. **원 바깥을 알파로 잘라낸다.** 원본은 나무·돌 바탕이 함께 그려져 있어 사각형 채로 얹으면 그림 속 바탕색이 우리 배경과 어긋나 액자처럼 뜬다. 원으로 자르면 어떤 배경에도 그냥 얹힌다. **잘리는 빛무리는 CSS `drop-shadow`로 메운다** — 어떤 크기에서도 같은 두께로 번지고 파일이 커지지 않는다.
51. **원반 속 글자는 장식이고 읽는 글자는 따로 있다.** 그림에 한글 간판이 새겨져 있지만 폰에서는 손톱만 해 읽히지 않는다. 그래서 워드마크와 한글 이름줄을 그대로 뒀다. 읽어주는 쪽에는 `alt`가 무엇이 그려져 있는지만 전한다.
52. **작은 파비콘은 사자 얼굴만 오려 쓴다.** 원반을 통째로 16px까지 줄이면 글자와 룬이 뭉개져 갈색 얼룩이 된다 — 줄여 보고 알았다. 얼굴만 오려 금테를 두르면 32px에서 감은 눈과 주둥이가 살고 16px에서도 금테로 다른 탭과 갈린다. 큰 자리(홈화면·설치 아이콘)에서는 원반이 통째로 읽히므로 그쪽은 자르지 않는다.
53. **iOS 아이콘은 11% 물려서 넣는다.** 아이폰이 제 스퀘어클 마스크로 깎기 때문에 원반이 정사각형 변에 닿으면 **금테 좌우가 납작하게 잘린다.** 마스크를 흉내 내 얹어 보고 알았다. 안드로이드의 마스커블도 같은 이유로 따로 만든다(가장자리 40%가 안전영역이라 더 많이 물린다).
54. **벡터 파비콘을 두지 않는다.** 원본이 래스터 그림이라 SVG로 옮길 것이 없고, SVG를 함께 걸어 두면 브라우저가 그쪽을 먼저 골라 옛 그림이 뜬다.

### 행낭 설정을 서버에 두기로 함 — 2026-08-08

55. **행낭 설정은 `localStorage`가 먼저, 서버가 뒤따른다(SPEC 5.2 개정).** 계정 id로 열쇠만 갈라 놓았더니 저장소 자체가 지워질 때 다 같이 날아갔다 — 홈화면 아이콘을 지웠다 다시 등록하니 행낭이 초기화됐다. 기록지를 서버로 옮긴 것과 같은 원인이다. **정본은 여전히 로컬이다** — 서버는 백업이자 기기 사이의 다리이며, 못 닿아도 행낭은 완전히 돈다(절대 원칙 3). 서버 오류는 전부 삼킨다.
56. **`satchel_settings`는 통짜 JSON으로 담는다.** 열 수별 레이아웃과 위젯별 설정·회전이 들어 있고 위젯이 늘 때마다 모양이 바뀐다. 칸으로 쪼개면 위젯 하나 들일 때마다 마이그레이션이 딸려 온다. 읽고 쓰는 것은 언제나 통째이며 조각으로 질의할 일이 없다. **모양을 서버가 알 필요가 없다.**
57. **견주는 시각은 뭉치 안에 기기가 찍어 둔 `updatedAt`이다.** 서버의 `updated_at`은 **도착 시각**이라 언제나 로컬보다 늦어, 그것으로 견주면 로컬이 영영 진다. 기기 시계끼리 견줘야 "누가 나중에 고쳤나"를 묻는 것이 된다. 서버 시각은 뭉치에 시각이 없을 때의 갈음일 뿐이다.
58. **빈 설정은 서버에 올리지 않는다(`isEmptySettings`).** 새 기기에서 처음 열면 빈 설정이 만들어지는데, 그것이 올라가면 다른 기기의 배치가 늦게 올라왔다는 이유로 밀린다. **잃지 않으려고 서버에 두는 것인데 그 길로 잃는다.** 지울 것이 있으면 위젯을 치우는 것이지 통째로 비우는 것이 아니다.
59. **시각은 `commit` 한 곳에서만 찍는다.** 찍는 자리가 흩어지면 어떤 경로로 고쳤을 때만 시각이 안 올라, 그 기기는 다른 기기와 맞출 때 영영 진다.
60. **서버로 보내는 것은 늦춰서 한 번만 보낸다(1.5초).** 위젯 하나를 옮기면 조작이 잇달아 들어오는데(놓기 → 크기 → 회전) 그때마다 요청을 띄우면 마지막 것이 먼저 도착하는 일이 생긴다. **로컬 저장은 늦추지 않는다** — 손을 뗀 순간 남아야 한다.
61. **서버 것을 앉힐 때는 시각도 물려받는다.** `commit`으로 찍으면 이 기기가 고친 것이 되어 다음 판정이 어긋난다. 그래서 그 경로만 `saveSettings`를 직접 부른다.

### 판을 새로고침 너머로 잇기 — 2026-08-08

62. **런타임 상태를 한 뭉치로 뜨는 계층을 뒀다(`runtime/snapshot.ts`).** 원소·라운드·HP/XP·덱이 스토어 넷에 흩어져 있으면 어디에도 실어 나를 수 없다 — 새로고침을 견디려면 저장해야 하고 파티원과 나누려면 보내야 한다. **거르는 문도 하나다**(`sanitizeRuntime`): 저장소에서 읽은 것, 서버가 준 것, 파티원이 보낸 것이 모두 여기를 지난다. 둘로 두면 언젠가 어긋난다.
63. **모르는 판은 통째로 버린다.** 반쯤 알아본 판으로 복원하면 원소는 살아나고 라운드는 1로 가는 꼴이 난다. **어긋난 판보다 빈 판이 낫다** — 사람이 실물을 보고 다시 맞추면 되지만, 어긋난 줄 모르면 못 맞춘다.
64. **복원은 판을 굴리는 것이 아니라 옮겨 놓는 것이다.** 그래서 스토어마다 `hydrate`를 따로 뒀다. `advance`로 5라운드를 만들려 하면 원소가 네 번 하강해 다 꺼지고 덱이 네 번 섞인다(구현 결정 34).
65. **카드의 `effect`·`shuffleAfter`는 저장된 것을 믿지 않고 종류표에서 다시 읽는다.** 둘은 `kindId`에서 나온 사본이라, 사본이 표와 어긋나면 화면에 뜬 그림(`kindId`로 고름)과 실제로 셈하는 값(`effect`)이 달라진다.
66. **그물은 화면이 아니라 `main.tsx`에서 친다.** 행낭을 나갔다 들어오는 사이에 그물이 없으면 그 틈에 새로고침된 판을 잃는다. 그리기 **전에** 부르는 것도 뜻이 있다 — 먼저 앉혀야 첫 렌더가 맞은 값을 그린다.

### 전투 세션 구현 결정 — 2026-08-08

67. **값은 `battles.state`에 둔다 — 별도 표가 아니라 같은 행이다.** 수명을 전투와 묶는 가장 확실한 방법이다. 판을 접으면 행이 지워지고 값도 함께 간다. `closed_at`을 찍고 남겨 두면 "어디에도 남지 않는다"가 아니게 되므로 **접는 것은 지우는 것이다.**
68. **즉시는 Broadcast, 따라잡기는 행.** Broadcast만으로는 새로고침한 사람이 빈 화면을 보고, 넷이 다 같이 새로고침하면 판이 날아간다. 행만으로는 왕복을 기다리느라 손가락이 미끄러진다(구현 결정 22). 둘 다 쓴다.
69. **받아서 앉히는 동안에는 빗장을 지른다(`applying`).** Zustand 구독 콜백은 `set` 안에서 **동기로** 불리므로 빗장 없이는 앉히는 것이 "내가 고쳤다"로 읽혀 되돌아 나가고, 두 기기가 메아리를 주고받는다. `finally`로 푸는 것은 복원 중에 하나가 던져도 빗장이 남지 않게 하려는 것이다 — 남으면 그 뒤로 내 조작이 영영 안 나간다.
70. **앉으면 상 위의 값을 물려받는다.** 내 화면의 값이 아니라 그쪽이 사실이다. **행을 먼저 읽어 앉힌 뒤에 통로를 연다** — 순서가 뒤면 물려받는 사이에 들어온 갱신을 행에 있던 옛것이 덮는다. 반대로 **판을 펴는 사람**은 지금 제 화면 값을 첫 판으로 올린다: 상 위에 이미 원소가 놓여 있는데 펴면서 비우면 실물과 어긋난다.
71. **얹는 것은 참여자만이다.** 파티원이라도 앉지 않았으면 못 얹는다 — 옆에서 구경하다 남의 판을 굴리는 일이 없어야 한다. 기존 "파티원이 닫는다" 정책은 이보다 넓어서 좁은 쪽으로 바꿨다.
72. **잊힌 판은 서버가 거둔다(`sweep_stale_battles`).** 사람은 판을 접지 않고 그냥 앱을 닫는다. 하루 지난 것은 지운다 — 한 판이 하루를 넘길 일은 없다.
73. **앉아 있다는 표식은 플레이 모드에도 남긴다.** 남들과 판을 나누는 중인 줄 모르고 만지면 내 화면만 고치는 줄 알고 남의 판을 굴린다. **이끼색으로 둔다** — 금색은 편집 모드가 이미 쓰고 있어 같은 색이면 두 가지가 같은 신호로 말하게 된다.
74. **전투 팝업은 편집 모드와 무관하다.** 판에 앉는 것은 배치를 고치는 일이 아니라 놀이를 시작하는 일이다.

### 캐릭터를 열쇠로 — 2026-08-08

75. **HP/XP와 덱의 열쇠를 위젯 인스턴스에서 캐릭터로 옮겼다(`slotKeyFor`).** 인스턴스 id는 기기마다 다르므로 전투에서 판을 나눠도 서로 다른 자리를 본다 — **공유가 도는 것처럼 보여도 체력과 덱은 따로 논다.** 캐릭터를 고르면 파티원 모두가 같은 자리를 본다.
76. **안 골라도 도구는 돈다.** 로그인 전이거나 캐릭터를 아직 안 만들었으면 종전대로 인스턴스 id를 쓴다(절대 원칙 3). 골라야만 쓸 수 있게 하면 행낭이 축 ①에 매달린다.
77. **캐릭터 이름표는 기록지 스토어를 거치지 않고 따로 읽는다(`satchel/roster.ts`).** `campaignStore`를 부르면 축 ②가 캠페인·파티 상태까지 끌고 들어온다. 필요한 세 칸(id·이름·클래스 표식)만 읽고 한 번 읽으면 그만이다 — 캐릭터가 늘어나는 일은 판 도중에 없다.
78. **덱 설정이 망가져도 고른 캐릭터는 지킨다.** 구성이 어긋났다고 캐릭터까지 놓으면 설정 화면을 한 번 잘못 건드린 뒤로 그 덱만 판에서 떨어져 나간다.
79. **은퇴한 캐릭터는 고를 목록에 없다.** 상에 없는 사람이라 자리만 어지럽힌다.

### 서버 함수 권한 — 2026-08-08

80. **`grant`를 적었다고 남이 못 부르는 것이 아니다.** Postgres는 함수를 만들 때 실행 권한을 이미 `PUBLIC`에 준다. `grant execute ... to authenticated`를 적어 두고 잠갔다고 여긴 것이 틀렸고, `security definer`가 걸린 DELETE 함수를 익명이 부를 수 있었다(`sweep_stale_battles`). **`revoke ... from public`을 함께 적어야 잠긴다.**
81. **`security definer` 함수는 안에서 한 번 더 본다.** 권한은 나중에 누가 `grant`를 다시 적으면 풀리지만 안쪽 검사는 남는다. `auth.uid() is null`이면 거절하는 줄 하나가 그 몫이다.
82. **새 RPC는 배포된 것에 익명으로 찔러 보고 확인한다.** 이것도 그렇게 잡았다 — 코드를 읽어서는 PUBLIC 기본 권한이 안 보인다. `curl`로 `apikey`만 넣고 부르면 익명이 무엇을 할 수 있는지 그대로 나온다(RLS 확인을 `curl`로 하는 것과 같은 이유).

### 전투 공유가 안 되던 두 자리 — 2026-08-08

83. **'판을 편다'가 늘 새 방을 만들었다.** 둘이 몇 초 차이로 누르면 방이 두 개 생기고 각자 다른 방에 앉는다 — **공유가 도는 것처럼 보이는데 아무것도 안 겹친다.** 이제 열린 방이 있으면 새로 만들지 않고 거기 앉는다. 진짜 동시에 들어오는 것까지는 못 막지만, 그 자리는 `findOpenBattle`이 가장 최근 것을 주므로 다음에 여는 사람부터 한쪽으로 모인다.
84. **새로고침하면 전투에서 조용히 빠졌다.** 스토어가 메모리에만 있어 화면은 잊는데 서버에는 앉아 있다고 남는다. **본인은 공유 중인 줄 아는데 조작이 아무에게도 안 가는** 상태가 가장 나쁘다. 행낭을 열 때 `findMyBattle`로 확인하고 자동으로 다시 잇는다. 되돌아갈 때는 **서버 값을 받아 오고 내 것을 올려보내지 않는다** — 자리를 비운 사이 남들이 굴린 판을 덮으면 안 된다.
85. **브로드캐스트가 도는지는 실제 프로젝트에 클라이언트 둘을 붙여 확인한다.** 코드를 읽어서는 전송이 되는지 알 수 없다. `@supabase/supabase-js`로 채널 둘을 구독시키고 한쪽에서 보내 다른 쪽이 받는지 보면 된다 — 이번에도 그렇게 해서 "전송은 멀쩡하고 방이 갈린 것"임을 갈라냈다.

### 판 하나 = 방 하나 — 2026-08-08

86. **혼자면 내 계정이 방이고, 전투에 앉으면 그 전투가 방이다(`runtime/room.ts`).** 배치는 계정에 묶고 판은 브라우저마다 두었더니 **같은 계정으로 두 곳에서 열면 화면이 갈라졌다.** 형님이 "하나의 계정의 행낭 상태가 여러 개 존재하는 게 문제"라고 짚었다. 전투는 **다른 사람과** 나누라고 있는 것이지 같은 사람의 기기 둘 사이에 협상할 것은 없다.
87. **방을 옮기는 일은 `room.ts` 한 곳이 한다.** 구독과 통로를 두 곳에서 붙이면 어느 쪽이 지금 방인지 흐려진다. 방마다 다른 것은 `fetch`·`push`·통로 이름 셋뿐이라 그것만 갈아 끼운다.
88. **행낭을 열 때 전투를 먼저 살피고 없으면 내 계정 방으로 간다.** 순서가 중요하다 — 내 방에 먼저 들어갔다가 전투로 옮기면 그 사이에 내 판이 계정 방에 올라가고, 전투 값을 물려받은 뒤라면 **남들이 굴린 판이 내 계정 방에 새어 든다.**
89. **방 안에서는 늦게 온 것이 그냥 이긴다. 방에 들어갈 때만 시각을 견준다.** 몇 초 단위로 여럿이 만지는 자리에서 시각을 따지면 손가락이 미끄러진다(구현 결정 22). 들어갈 때의 판정은 행낭 설정과 같다 — **빈 것이 알맹이를 밀어내지 못하고**, 그다음은 늦게 건드린 쪽이 이긴다.
90. **전투에서 일어나면 판을 그대로 들고 내 계정 방으로 돌아간다.** 자리에서 일어난다고 상 위의 것이 사라지지 않는다. 다만 이제부터는 내 방에만 쌓인다.

### 행낭 구성이 브라우저마다 달랐던 이유 — 2026-08-09

91. **행낭을 여는 것 자체가 '편집'으로 세어졌다.** 화면 크기를 재면 열 수가 정해지고, 그 열 수의 배치가 없으면 가까운 것에서 뽑아낸다. 그 결과를 `commit`으로 저장해 **시각을 찍고 서버로 올렸다.** 브라우저 둘이 각자 열자마자 제 시각을 찍으니 둘 다 "내가 방금 고쳤다"고 주장했고, 어느 쪽도 남의 것을 받지 않았다. **뽑아내기(`derive`)와 고치기(`commit`)를 갈랐다** — 뽑아낸 것은 로컬에만 남긴다. 다른 기기도 같은 원본에서 같은 것을 뽑아내므로 나를 것이 없다.
92. **표에 얹는 것만으로는 옆에 켜 둔 기기가 모른다.** 다른 기기는 다시 열기 전까지 표를 읽지 않는다. 판이 통로로 즉시 오가는 것과 같은 방식을 행낭 구성에도 붙였다(`satchel-settings:<계정id>`). 받은 것은 시각을 견주지 않고 그냥 앉힌다 — 같은 계정의 내 기기가 방금 보낸 것이라 그것이 가장 최근이다.
93. **통로는 아직 공개다 — 알려진 한계.** Supabase Broadcast는 기본이 공개 채널이라, 채널 이름을 아는 쪽은 들을 수 있다. 이름에 계정 id(또는 전투 id)가 들어가므로 그 값을 아는 사람만 가능하고, 파티원은 서로의 `profiles.id`를 읽을 수 있으니 **파티원끼리는 서로 엿들을 수 있다.** 오가는 것이 게임 진행 상태(원소·라운드·체력·위젯 배치)뿐이라 지금은 두었다. 막으려면 private channel + `realtime.messages` RLS가 필요하다.

### 실시간 통로를 잠갔다 — 2026-08-09

94. **통로는 계정 하나에 하나다(`accountChannel.ts`).** 배치와 판을 따로 두 통로로 날랐는데, 나르는 것이 둘일 뿐 **자격을 정하는 근거는 하나**다. 통로가 늘면 잠그는 정책도 늘고 하나를 빠뜨리면 그쪽만 열린 채 남는다. 종류(`settings`·`state`)로 가른다.
95. **`realtime.messages`에는 손이 닿지 않는다.** Broadcast를 잠그려면 그 표에 정책을 걸어야 하는데, SQL Editor의 어떤 역할로도 안 된다 — `postgres`는 `42501 must be owner of table messages`, `supabase_privileged_role`은 `permission denied for schema realtime`. **두 번 시도해 보고 접었다.** 나중에 Supabase가 길을 열면 다시 볼 자리다.
96. **그래서 통로에 알맹이를 싣지 않는다.** "바뀌었다"는 신호만 보내고 받는 쪽이 표에서 읽어 간다 — **자격은 우리가 건 RLS가 본다.** 통로를 엿들어 알 수 있는 것은 "언제 무엇인가 바뀌었다"뿐이고, 가짜 신호를 보내도 상대가 제 권한으로 표를 한 번 더 읽을 뿐이다. 못 잠그는 길에는 지킬 것을 두지 않는 편이 잠긴 척하는 것보다 낫다.
97. **표에 먼저 쓰고 그다음에 알린다.** 순서가 뒤바뀌면 받은 쪽이 아직 옛 값이 든 표를 읽는다. 알맹이를 실어 보내던 때는 순서가 상관없었지만 이제는 표가 정본이다. **쓰기가 실패하면 알리지 않는다** — 알려 봐야 상대가 옛 값을 읽는다.
97-1. **모아 보내는 시간이 짧아졌다(1500·1200 → 600·350ms).** 표에 쓰는 것이 이제 길목이다. 오래 끌면 옆 사람 화면이 그만큼 늦게 바뀐다.
98. **전투에 앉아도 배치는 계정 통로에 남는다.** 구성은 제 것을 쓰고 상태만 파티의 전투를 따른다(형님이 정한 선). 화면 크기가 사람마다 다르므로 배치를 나누면 남의 화면이 내 것을 덮는다.
99. **`npx tsc --noEmit`는 이 저장소에서 아무것도 검사하지 않는다.** 루트 `tsconfig.json`이 `"files": []`에 references만 두고 있어서 통과한 것처럼 보인다. **타입 확인은 `npm run build`(`tsc -b && vite build`)로 한다.** 이번에 `Wire.send`를 지우고도 `tsc --noEmit`가 조용해서 한참 못 알아챘다.

### 값을 서버가 밀어주게 바꿨다 — 2026-08-10

100. **Postgres Changes로 옮겼다.** Broadcast 채널을 잠글 수 없어 "바뀌었다"는 신호만 보내고 받는 쪽이 표를 다시 읽었는데, Postgres Changes는 **밀기 전에 RLS로 자격을 본다.** 값이 지나는 길을 우리가 지킬 수 있으므로 값을 실어도 되고, 받는 쪽 읽기 왕복 한 번이 사라진다. 켜는 것은 우리 표를 복제 대상에 넣는 일뿐이라 권한 문제가 없다.
101. **판 값을 `battles`에서 떼어 `battle_state`로 옮겼다.** `battles`를 읽는 정책은 **파티원 전체**다 — 열린 판이 있다는 것을 알아야 앉을 수 있기 때문이다. 그 표를 그대로 밀면 앉지도 않은 파티원에게 판이 통째로 간다. **"판이 열려 있다"와 "판에 무엇이 놓였다"는 볼 자격이 다르다.**
102. **내가 쓴 것도 되돌아온다(`EchoGuard`).** Broadcast의 `self: false` 같은 것이 없다 — 서버는 누가 바꿨는지로 가르지 않는다. 값에 찍힌 시각으로 가려낸다. 안 가려내면 **쓰고 나서 돌아오기까지 사이에 또 만진 것이 옛 값으로 덮인다**(원소를 연달아 두 번 켜면 두 번째가 도로 꺼진다). **올리기 전에 적어 둔다** — 밀려오는 것이 응답보다 먼저 올 수 있다.
103. **한 번 걸러낸 시각은 지운다.** 남겨두면 나중에 남이 우연히 같은 시각을 찍었을 때 그 사람의 것을 버린다.
104. **`replica identity full`을 준다.** 기본값이면 밀려오는 옛 행에 기본키만 담긴다. 행 수가 사람 수만큼뿐이라 비용이 거의 없다.

### 골드 카운터 — 2026-08-10

105. **한 칸에서 크지 않는다.** `defaultSize`·`minSize`·`maxSize`를 모두 1×1로 두고 `isSizeAllowed`로 한 번 더 막는다 — 최대·최소만으로는 열 수 파생 경로에서 다른 크기가 새어 들 수 있다. **크기 손잡이도 안 낸다**(`WidgetFrame`의 `resizable`) — 끌 수 있게 보이는데 아무 일도 안 일어나면 고장으로 읽힌다.
106. **금화는 직접 그린 인라인 SVG다.** Creator Pack이 아니므로 격리 규칙(1-1)에 걸리지 않는다. 테를 두른 원반에 빗금을 둘러 두께를 냈을 뿐이고 실물 표식을 베끼지 않았다(구현 결정 31과 같은 선).
107. **SVG 속성에 `rgb(0 0 0 / 45%)`를 쓰지 않는다.** CSS Color 4 표기라 못 읽는 브라우저가 있다. `fill`과 `fill-opacity`를 갈라 적으면 어디서나 같게 읽힌다.
108. **스토어 필드를 `valueOf`라고 짓지 않는다.** `Object.prototype.valueOf`와 부딪혀 Zustand의 `set`에 넘길 때 타입이 어긋난다. `amountOf`로 두었다.
109. **조작은 HP/XP 것을 그대로 쓴다**(`hpxp.ts`의 `stepsFromDrag`·`toLocalDelta`·`TAP_SLOP_PX`). 세는 손놀림이 위젯마다 다르면 상 위에서 헷갈린다.
110. **주운 금화는 캐릭터의 골드가 아니다.** 판이 끝나면 사람이 정산해 시트에 옮긴다 — 축 ②는 캠페인을 고치지 않는다.

### 클래스 수치를 DB에 — 2026-08-11

111. **값은 레포에 없고 DB에만 있다.** 클래스 이름·핸드 사이즈·레벨별 체력은 실물 카드에 인쇄된 게임 콘텐츠다. 레포와 배포 사이트가 공개이므로 커밋하면 그 순간 공개 배포가 된다 — **마이그레이션 SQL에 적는 것도 같다.** 그래서 `0011`에는 표와 정책만 있고 값을 넣는 자리는 앱 안의 관리자 화면이다(`ClassDataEditor`).
112. **읽는 것은 승인된 사람, 쓰는 것은 관리자.** 로그인만으로는 안 된다 — 가입은 열려 있고 들이는 것은 관리자다(`0004`). 파티원 아무나 고치면 상 위의 사실이 사람마다 달라진다.
113. **아이콘 번호가 열쇠다.** 캐릭터가 이미 `class_icon`으로 클래스를 가리키고 있으므로 따로 이어 줄 것이 없다. **Creator Pack에 없는 클래스는 담을 수 없다** — 21쪽이 한계다.
114. **없어도 앱은 완전히 돈다.** 아무것도 안 넣으면 아이콘만 보이던 종전 모습이다. 값이 있으면 이름·핸드 사이즈·최대 체력을 더 보여줄 뿐이고, **규칙은 여전히 판정하지 않는다** — 최대 체력을 읽어주되 피해를 깎거나 상한을 강제하지 않는다.
115. **모르면 모른다고 한다(`maxHpFor`가 `null`).** 짐작해서 숫자를 내면 사람이 그것을 믿는데, 틀린 최대 체력은 판을 어긋나게 한다.
116. **체력표는 아홉 칸이 아니면 없는 것으로 친다.** 반쯤 채워진 표는 잘못 읽히기만 한다. 서버의 `check`와 화면의 거르기가 같은 것을 본다.
117. **칸을 아홉 벌 그리지 않고 JSON을 받는다.** 한 벌에 열두 칸이라 아홉 클래스면 백 칸이 넘는다. **틀린 자리를 짚어 준다** — 어디가 틀렸는지 모르면 붙여넣기가 수수께끼가 된다.

### 아이콘 없는 클래스 — 2026-08-11

118. **클래스가 제 id를 갖는다. 아이콘은 있으면 붙는다(`0012`).** 아이콘 번호를 열쇠로 삼았더니 **팩에 그림이 없는 클래스를 아예 담을 수 없었다** — 그림이 있고 없고는 보여주는 방식의 문제인데 그것이 담을 수 있는지까지 정하고 있었다.
119. **팩의 18~21쪽은 사자의 턱이 아니다.** 실물 카드의 마크와 대조해 보고 알았다 — 1·4·6·13쪽을 색 있는 원반에 담은 것이고, **사자의 턱 넷(폭탄·도끼·홀·갈고리)은 팩에 아예 없다.** ATTRIBUTION에 잘못 적어 두었던 것을 고쳤다. 그림은 눈으로 대조하기 전에는 단정하지 않는다.
120. **이름이 겹치지 않는 열쇠 노릇을 한다.** 붙여넣기로 넣고 고치는 흐름이라 같은 이름이 두 번 오면 덮어써야지 새로 생기면 안 된다. 사람이 손으로 id를 적을 일은 없다.
121. **캐릭터는 `class_id`와 `class_icon`을 함께 든다.** 클래스 표가 비었을 때는 아이콘만으로 골라야 하고(절대 원칙 3과 같은 결), 축 ②의 이름표는 아이콘 번호로 그림을 찾는다(`satchel/roster.ts`). 클래스를 고르면 그 클래스의 아이콘을 함께 적어 두어 두 길이 어긋나지 않게 한다.
122. **클래스를 지워도 캐릭터는 남는다(`on delete set null`).** 수치가 사라졌다고 사람이 사라질 이유가 없다.
123. **같은 아이콘을 두 클래스가 쓰지 못하게 막는다.** 고를 때 어느 쪽인지 알 수 없다 — 번호를 잘못 친 것을 그 자리에서 잡아 준다.

### 카드를 값 하나에서 명세로 — 2026-08-11

124. **카드는 값 하나가 아니다. 값·굴림·표식 셋이 모여 한 장이 된다(`cardSpec.ts`).** 실물 특혜 시트 아홉 장을 펼쳐 보고 알았다 — `+1 부상`을 `+1`과 구별할 수 없었고, `굴림 불`을 적을 자리가 없었고, `+1 불·얼음`처럼 표식이 둘인 카드가 아예 없었다. 종류를 아홉 개로 못박아 둔 것(`CARD_KINDS`)이 그 자리에서 무너졌다.
125. **명세는 한 줄의 글자로 적힌다. 그 글자가 정본이다.** `[r.]<값>[.<표식>]*` — `p1`, `p1.wound`, `r.p0.push2`, `p1.fire.ice`. 구성표의 열쇠로도 저장된 판의 카드 id로도 쓰이므로 객체로 두면 열쇠가 될 수 없다. **글자 하나면 저장된 것을 믿지 않고 다시 뽑을 수 있다**(구현 결정 65) — 종류표가 닫혀 있지 않게 되었어도 그 성질은 지킨다. **옛 열쇠(`x0`·`m1`·`p0`…)가 곧 값 낱말이라 이미 저장된 것이 손질 없이 통과한다.**
126. **표식은 사전 순으로 세운다.** 그러지 않으면 `p1.fire.ice`와 `p1.ice.fire`가 다른 종류가 되어 같은 카드가 구성표에 두 줄로 앉고 장수가 갈린다. 같은 표식이 두 번 붙거나(`p1.fire.fire`) 수를 다는 표식에 수가 없으면(`p1.heal`) **조용히 고치지 않고 버린다** — 짐작해서 채우면 사람이 그것을 믿는다.
127. **굴림은 그림이 아니라 규칙이다(`drawTurn`).** 굴림 카드를 뽑으면 뽑기가 끝나지 않고 굴림 아닌 것이 나올 때까지 이어진다. 아홉 클래스 중 다섯이 굴림 카드를 넣으므로 미룰 수 있는 것이 아니었다. **합은 내지 않는다** — 뽑힌 것을 겹쳐 늘어놓을 뿐이고 셈은 사람이 한다(SPEC 1장). 굴림만 든 덱에서 영영 돌지 않도록 덱 장수에서 멎는다.
128. **마지막 뽑기를 따로 들고 있지 않고 버린 더미에서 되짚는다(`revealedChain`).** 굴림 카드는 언제나 결과 카드보다 먼저 뽑히므로 0번부터 굴림이 이어지는 데까지가 한 번의 뽑기다. 상태를 하나 더 두면 그것이 판을 나를 때 어긋난다.
129. **섞기는 값에서 나온다.** 곱하기 카드 둘(×0·×2)만 섞기 표식을 달고 있으므로 따로 적어 두지 않는다. 굴림에 섞기가 안 붙는 것도 이 규칙 하나로 함께 걸러진다 — 적을 자리가 늘면 그만큼 어긋날 자리가 는다.
130. **판을 나를 때 카드는 열쇠만 싣는다(`WireCard`).** 표식이 붙으면서 명세가 통통해졌다 — 스무 장이면 몇 킬로바이트고 그것이 원소를 한 번 켤 때마다 통로로 나간다. 값·굴림·표식은 앉힐 때 열쇠에서 다시 뽑는다.
131. **표식은 그림이 있는 것만 그림으로, 나머지는 글자로 낸다.** 원소 여섯은 원소 트래커의 아이콘을 그대로 쓰고 상태이상은 글자다 — 팩에서 아이콘을 더 뽑아 오는 것은 ATTRIBUTION을 함께 고쳐야 하는 별개의 일이다(SPEC 13.1). **굴림 표식은 직접 그린 도형이다**(구현 결정 31과 같은 선).
132. **겹쳐 늘어놓을 때 맨 앞 카드는 물러나지 않는다.** 굴림이 몇 장 이어지든 결과 카드가 칸을 정확히 채우고 먼저 뽑힌 것들이 그 뒤로 삐져나온다. 앞에서부터 계단을 놓으면 결과 카드가 자리를 옮겨 다녀 크게 띄운 카드가 날아가 앉을 자리(`landOn`)까지 어긋난다.
133. **덱 설정 화면은 아홉 값을 늘 내고 표식 붙은 것은 있을 때만 낸다.** 표식의 조합은 끝이 없어 다 늘어놓을 수가 없다 — 퍽에서 들어온 것만 손질할 수 있게 낸다. 0장이 되면 아홉 값은 남기고 나머지는 열쇠째 걷는다.
134. **위젯의 서버 렌더 확인은 스토어를 앉혀 놓고 할 수 없다(`CardFace.test.tsx`).** zustand가 `getInitialState`를 서버 스냅숏으로 주므로 `hydrate`한 판이 비치지 않는다. 그래서 카드 앞면을 `CardFace.tsx`로 떼어 그것만 확인한다 — 위젯을 통째로 렌더해 놓고 아무것도 확인 못 하는 시험보다 낫다.

### 특혜 표를 DB에 — 2026-08-11

135. **특혜 글도 DB에만 둔다(`0013`).** 클래스 이름·체력과 같은 선인데, 지금까지 담은 것 중 **가장 또렷하게 저작물성이 있다** — 시트에 인쇄된 문장 그대로다. 마이그레이션 SQL에는 표와 정책만 있고 값은 앱 안의 관리자 화면에서 넣는다(`ClassPerkEditor`).
136. **줄이 아니라 상자가 열쇠다(`perkBoxes`).** 실물 시트는 줄마다 체크상자가 하나에서 셋까지 붙고 다 세면 정확히 열다섯이다. 캐릭터가 켜 둔 것(`characters.perks`)이 지금까지도 **상자 번호**였으므로, 줄을 `sort` 차례로 펴서 `count`만큼 번호를 매기면 그대로 맞물린다 — **캐릭터 쪽 표를 고칠 일이 없다.**
137. **한 클래스의 표는 통째로 갈아 끼운다(`pushClassPerks`).** 줄이 하나 빠진 채 남아 있으면 그 아래 상자 번호가 통째로 밀려 켜 둔 것이 다른 특혜를 가리킨다. **그 어긋남은 눈에 안 보인다.** 관리자 혼자 쓰는 화면이라 지우고 넣는 왕복이 싸다.
138. **덱 변경은 `{"m1": -2, "p1": 1}` 한 객체다.** 손으로 적어 붙여넣는 자리라 짧아야 한다. 교체는 두 줄로 쪼갠다 — 교체라는 갈래를 따로 두면 셈이 두 벌이 되고 실제로 하는 일은 같다.
139. **알 수 없는 카드는 조용히 넘기지 않는다(`parsePerkJson`).** 오타가 나기 쉬운 자리인데 걸러 버리면 켠 상자가 아무 일도 안 하는 채로 남고, **그것이 화면에는 정상으로 보인다.** 반대로 서버에서 읽을 때는(`sanitizePerkChanges`) 조용히 버린다 — 그쪽은 이미 들어간 값이라 화면이 서는 것이 먼저다.
140. **상자가 열다섯이 아니어도 막지 않고 짚어만 준다.** 아홉 장을 세어 보니 다 열다섯이었지만 우리가 안 본 클래스가 다를 수 있다. 규칙을 판정하지 않는다는 선과 같은 결이다.
141. **표가 없으면 `null`이지 빈 목록이 아니다(`usePerkChanges`).** 빈 목록은 "퍽을 하나도 안 켰다"로 읽혀 사람이 설정에 적어 둔 구성을 표준 덱으로 덮는다. 모르는 것과 없는 것은 다르다(`maxHpFor`와 같은 결, 구현 결정 115).
142. **축 ①에 닿는 자리를 `perkSource.ts` 하나로 모았다.** 위젯이 스토어 둘을 직접 부르면 축 ②가 어디서 축 ①에 닿는지 흩어져 보이지 않는다. 넓히거나 좁힐 때 볼 곳이 하나여야 한다.
143. **캐릭터 시트는 표가 있으면 줄로, 없으면 번호로 그린다.** 어느 쪽이든 켜지는 값은 같은 상자 번호다 — 아무것도 안 넣어도 앱은 종전대로 완전히 돈다(절대 원칙 3).
144. **특혜 표를 클래스 수치와 같은 스토어에서 읽는다.** 같은 주기로 바뀌고 같은 때에 필요하다 — 시트가 이름과 특혜를 나란히 그리고, 덱이 클래스를 알아야 그 표를 찾는다. 따로 두면 왕복만 둘로 는다.

### 특혜가 실제로 덱에 닿게 — 2026-08-12

145. **행낭에 들어올 때 이름표와 클래스 표를 다시 읽는다.** 둘 다 "한 번 읽으면 그만"이라 판 도중에는 안 읽는데, **특혜 상자는 판 사이에 바뀌고 그 값이 곧 덱 구성이다.** 그냥 두었더니 기록지에서 상자를 켜고 행낭으로 와도 옛 값을 보고 새로고침해야만 반영됐다. 들어올 때 한 번뿐이며 판이 도는 동안에는 다시 읽지 않는다.
146. **한 번 뽑은 덱은 저절로 다시 짜이지 않는다. 사람이 「덱 새로 짜기」를 누를 때만.** 덱은 첫 뽑기 때 만들어지고 그 뒤 `reshuffle`은 있던 카드를 다시 섞을 뿐 구성을 다시 읽지 않는다 — 실물에서도 판 도중에 덱을 갈아 끼우지 않고 퍽은 시나리오 사이에 얻는다. 그런데 **다시 짤 길이 아예 없었다**(`shuffleNow`·`reset`을 부르는 화면이 하나도 없었다). 담긴 판을 지우면 위젯이 지금 구성으로 새로 편다.
147. **특혜가 몰 때는 장수 손잡이를 걷는다.** `resolveComposition`이 특혜를 이기게 두므로 손잡이를 그대로 내놓으면 눌러도 아무 일이 안 일어난다 — **먹히는 것처럼 보이는 화면이 안 보이는 화면보다 나쁘다.** 나온 구성을 읽기만 하게 보여주고 어디서 왔는지 한 줄 적는다. 「표준으로 되돌리기」도 함께 걷는다 — 되돌려도 특혜가 다시 이긴다.
148. **설정 화면이 보여주는 것은 `resolveComposition`을 거친 구성이다.** 설정에 적힌 옛 수치를 그대로 띄우면 화면의 숫자와 실제로 뽑히는 덱이 갈린다.
149. **설정 편집기가 `instanceId`를 받는다.** 설정을 읽고 쓰는 데는 필요 없지만 **위젯이 담고 있는 런타임 값에 손대야 할 때** 있어야 한다 — 값이 담긴 열쇠는 인스턴스(또는 고른 캐릭터)이므로 그것을 모르면 어느 덱을 다시 짤지 알 수 없다. 놓기 전에 묻는 팝업은 `null`이다.
150. **`getInitialState` 때문에 zustand는 서버 렌더에 안 비친다 — `setState`로도 안 된다.** 구현 결정 134에서 `hydrate`가 안 통한다는 것까지 알았는데, `setState`도 마찬가지다. 스토어에 매달린 화면 갈래는 서버 렌더로 확인할 수 없다. **알맹이를 순수 함수로 빼서 그쪽을 덮고**, 화면 갈래는 브라우저에서 본다.

### 일지를 캐릭터 중심으로 — 2026-08-12

151. **캐릭터가 먼저고 파티는 그 캐릭터가 속한 곳이다.** 일지를 열면 파티가 먼저 보이고 그 안에 캐릭터가 있었다. 사람이 앱을 여는 까닭은 **제 캐릭터를 보려는 것**이고 파티는 어디 속했는지 말하는 이름표에 가깝다. 형님이 짚어 뒤집었다.
152. **표는 안 바꿨다.** 캐릭터는 여전히 기록지에, 기록지는 파티에 달려 있다. **파티 하나에 기록지 하나뿐**이라(`fetchOrCreateFor`가 가장 오래된 하나만 집고 둘째를 만들 길이 UI에 없다) `campaign_id`가 사실상 "어느 파티"와 같다 — 뒤집을 것은 화면이지 표가 아니었다. 마이그레이션 없이 끝났다.
153. **파티 목록은 아래에 남긴다.** 파티 시트·초대·평판으로 가는 길이 그것뿐이다. 캐릭터의 부제를 눌러 들어가게만 두면 **파티만 있고 캐릭터가 없을 때 갈 길이 사라진다.**
154. **캐릭터 세우기는 파티 기록지 안에 그대로 둔다.** 캐릭터는 반드시 파티에 속하므로 파티를 먼저 고르는 것이 실제 순서다. 창구를 둘로 두면 어긋난다 — 첫 화면에는 어디로 가라고만 적는다.
155. **캐릭터 줄을 누르면 시트 한 장만 뜬다(`#/journal/<파티>/<캐릭터>`).** 대신 **기록지로 가는 문을 시트 위에 낸다** — 정산하다 파티 시트를 보러 가는 일이 잦은데 아래에 두면 긴 시트를 끝까지 내려야 한다.
156. **뒤로는 한 칸씩 올라간다(`backHref`).** 캐릭터 → 그 파티 기록지 → 목록 → 여관. 어디서 눌러도 목록으로 튀면 캐릭터를 열 때마다 두 번씩 들어가야 한다.
157. **내 캐릭터는 목록으로 돌아올 때마다 다시 읽는다.** 기록지 안에서 세우거나 거두고 나오면 목록이 달라져 있다 — 한 번 읽고 마는 채로 두면 방금 만든 캐릭터가 첫 화면에 없다.
158. **주소 읽기를 화면 파일에서 뗐다(`journalRoute.ts`).** `react-refresh/only-export-components`가 막았고 그 말이 옳다 — 순수 함수라 Vitest로 못박을 수 있다. 갈래가 셋이 되면서 **잘못 읽으면 엉뚱한 화면이 아니라 빈 화면이 뜬다.**
159. **PostgREST 중첩 조회가 서는지는 익명으로 찔러 확인한다.** 관계를 못 찾으면 `PGRST200`이 오고, RLS로 막히면 `200 []`이 온다 — 둘이 갈린다. 구현 결정 82와 같은 방식이다.

### 시트에서 덱 구성을 본다 — 2026-08-12

160. **시트는 덱의 구성만 낸다. 판은 안 낸다(`DeckPreview`).** 남은 장수·뽑힌 카드는 축 ②의 휘발성 런타임이라 판이 끝나면 사라지는 것이고(SPEC 5.2), **영속 기록지가 비출 것이 아니다.** 여기 있는 것은 켠 특혜에서 순수하게 셈한 값뿐이며 스토어를 하나도 건드리지 않는다.
161. **특혜 표가 없으면 아예 안 낸다.** 그때는 구성의 정본이 위젯 설정이고 시트는 그것을 모른다 — "표준 20장"이라고 적으면 사람이 위젯에서 손으로 맞춰 둔 덱과 어긋난다. **모르면 모른다고 한다**(구현 결정 115·141과 같은 결).
162. **덱 위젯과 같은 그림을 쓴다.** 시트에서 본 것과 상 위에서 뽑는 것이 달라 보이면 같은 덱이라는 것을 알 수 없다.
163. **줄이 아니라 알약으로 흘린다.** 표식이 붙으면 종류가 스물 가까이 되는데 줄로 세우면 시트가 그만큼 길어진다 — 시트는 이미 길다. 표준에서 달라진 만큼(`±n`)을 알약에 함께 적어 **특혜가 실제로 먹었는지 그 자리에서 보이게** 한다.
164. **스토어를 안 쓰니 서버 렌더로 끝까지 확인된다.** 덱 설정 화면의 갈래를 못 덮은 것과 갈리는 지점이다(구현 결정 150) — **스토어에 매달리지 않게 짜면 확인할 수 있다.**

### 캐릭터 시트에 열람·편집 모드 — 2026-08-12

165. **열람이 기본이고 고치려면 편집으로 들어간다.** 그전에는 칸을 건드리는 즉시 서버로 갔다. 다이얼로 골드를 120에서 340으로 옮기면 스물두 번이 나가고 **`version`이 손가락 수만큼 올라 낙관적 잠금이 무의미해졌다.** 게다가 그냥 들여다보는 동안에도 모든 칸이 눌려 있어 **스치기만 해도 값이 바뀌었다.** 글자 칸만 `onBlur`로 늦춰 둔 것은 절반만 막은 것이었다.
166. **바뀐 칸만 보낸다(`sheetDiff`).** 통째로 보내면 안 건드린 칸까지 덮어 그 사이 남이 고친 것을 되돌린다.
167. **저장 단추가 사는 조건과 보낼 것을 한 함수가 낸다.** 둘을 따로 세면 언젠가 어긋나고, 어긋나면 **고쳤는데 저장이 안 눌리는** 꼴이 난다 — 사람이 그 자리에서 할 수 있는 일이 없어진다.
168. **울타리에 걸려 제자리로 돌아온 값은 바뀐 것이 아니다.** 이미 9레벨인데 한 번 더 눌러도 저장이 살아나면 안 된다. 이름의 앞뒤 공백도 턴다 — **눈에 안 보이는 차이로 단추가 살아나면 안 된다.**
169. **저장 단추는 죽어도 자리를 지킨다.** 없어지면 어디 있었는지 다시 찾아야 하고 눌러도 안 되는 까닭이 화면에 안 남는다. 그래서 글자로도 말한다(「고친 것 없음」).
170. **띠는 아래에 붙어 따라온다(`position: sticky`).** 시트가 길어 저장을 누르려고 끝까지 내려야 하면 잊는다. 정산은 여러 칸을 함께 고치는 일이라 더 그렇다.
171. **고칠 수 없게 되면 편집에서 그 자리에서 내려온다.** 편집 중에 서버가 끊기면 칸이 열린 채 남는데 저장은 어차피 안 나간다 — 열려 있는 것처럼 보이는 화면이 잠긴 화면보다 나쁘다.
172. **왜 못 고치는지 화면에 적는다.** 단추를 그냥 없애면 까닭이 남지 않는다 — "남의 시트라 보기만 한다" / "서버에 닿지 못해 지금은 고칠 수 없다".
173. **은퇴는 초안에 담기고 거두기는 그 자리에서 나간다.** 은퇴는 칸 하나라 저장을 눌러야 남고, 거두기는 되돌릴 수 없어 초안에 담을 것이 아니다. `window.confirm`을 걷고 `ConfirmDialog`로 바꿨다(구현 결정 36을 이제야 지킨다).
174. **뜸의 길이는 잃는 것의 크기에 맞춘다.** 거두기는 기본 5초 그대로, 「고치던 것 버리기」는 1.5초다 — 5초는 몇 시간짜리 판이 날아가는 자리에 맞춘 값이라 그만두는 데 그대로 쓰면 벌처럼 느껴진다.
175. **클래스 고르기는 편집 중에만 낸다.** 스물한 개를 늘어놓는 칸이라 열람 화면에서는 자리만 차지한다 — 고른 클래스는 이름줄에 이미 적혀 있다.
176. **은퇴 흐림은 초안이 아니라 저장된 사실을 따른다.** 편집 중에 껐다 켰다 할 때마다 시트 전체가 흐려졌다 밝아지면 눈이 어지럽다.

### 시트가 폼을 벗어나 보이던 자리 — 2026-08-12

177. **`.char`는 로스터 줄 밑에 매달리는 상자다.** 위 테두리를 지우고 위쪽 모서리를 각지게 두어 눌린 줄과 한 덩어리로 보이게 한 것이다. 캐릭터 한 장짜리 화면(어제 만든 `#/journal/<파티>/<캐릭터>`)에는 매달릴 줄이 없는데 그대로 썼더니 **위가 뚫린 채 화면 끝까지 펼쳐졌다** — 폭 제한도 가운데 정렬도 없어 위의 「기록지로」 문과 왼쪽 끝이 어긋났다. `char--solo`가 제 테를 두르고 44rem을 잡는다(파티 시트의 `.sheet`와 같은 값).
178. **다이얼이 제일 크게 티났을 뿐 원인은 시트 틀이었다.** 넓고 각진 요소라 어긋남이 거기서 제일 잘 보인다 — **증상이 보이는 자리와 원인이 있는 자리가 다르다.**
179. **플렉스 항목의 기본 `min-width`는 `auto`라 내용 최소 너비 밑으로 줄지 않는다.** 다이얼 안에는 폭이 고정된 단추가 넷 있어 그 최소 너비가 200px을 넘는다 — 둘이 한 줄에 서면 좁은 화면에서 상자 밖으로 밀려 나간다. `min-width: 0`을 함께 적어야 비로소 줄어든다. 눈에 안 보이던 구멍이라 함께 막았다.
180. **단추는 좁아지면 함께 좁아진다(`clamp(32px, 11vw, 40px)`).** 못박아 두면 좁은 화면에서 넘칠 길이 그것뿐이다. 44px 터치 목표를 못 지키는 것은 아쉽지만 **넘쳐서 눌리지 않는 것보다는 낫다.**

### 클래스는 세울 때 정한다 — 2026-08-12

180-1. **화면 글자는 「생성」이다.** 「캐릭터 세우기」가 어색하다고 형님이 짚었다 — 파티는 세우고 캐릭터는 만든다.
181. **캐릭터가 곧 클래스다. 시트에서 못 바꾼다.** 바위심장이 도끼투척수로 바뀌는 일은 없고, 그러고 싶으면 거두고 새로 세운다. 레벨·경험·퍽·아이템이 전부 그 클래스에 매인 값이라 **클래스만 갈아 끼우면 남은 값들이 통째로 거짓이 된다** — 특히 켜 둔 특혜 상자 번호는 그 클래스의 특혜 표를 가리키므로(`0013`), 클래스가 바뀌면 같은 번호가 딴 특혜를 가리키고 그것이 곧 공격 보정 덱의 구성이 된다.
182. **막는 것은 서버다(`0014`).** 화면에서 칸을 걷는 것만으로는 잠긴 것이 아니다 — 레포와 번들이 공개라 아무나 REST로 직접 찌를 수 있다(구현 결정 44와 같은 결). `campaigns`가 파티를 못 옮기게 막아 둔 것과 같은 짜임이다.
183. **정해진 적 없는 클래스는 한 번 정할 수 있다.** 클래스를 안 고른 채로도 캐릭터를 세울 수 있으므로 무조건 잠그면 **그런 캐릭터가 영영 클래스 없이 갇힌다.** 아직 안 정한 것과 정한 것을 가른다.
184. **`CharacterEdits`에서도 뺐다.** 화면이 안 내고 초안에 없어도 타입에 남아 있으면 언젠가 다시 새어 든다 — **보낼 수조차 없게** 한다.
185. **트리거는 배포된 것에 직접 찔러 확인한다.** 코드를 읽어서는 막히는지 알 수 없다(구현 결정 82와 같은 이유). 다섯 갈래를 트랜잭션 안에서 시도하고 되돌렸다 — 클래스 바꾸기·아이콘만 바꾸기는 막히고, 골드 고치기·같은 값 다시 쓰기·클래스 없던 것에 처음 정하기는 통과했다.

### 전투 목표를 격자로 — 2026-08-12

186. **한 줄에 셋. 줄 하나가 곧 실물의 한 묶음이다.** 실물 시트는 세 칸짜리 여섯 묶음인데(구현 결정 42) 흘려 넣고 셋째마다 여백을 주었더니 **한 줄에 몇 개가 들어가느냐가 화면 폭에 따라 달라져** 묶음이 줄을 넘어가 끊겼다 — 넷째 칸이 다음 줄로 넘어가면 눈에는 묶음이 깨져 보인다. 격자로 못박으면 어떤 폭에서도 줄 하나가 묶음 하나다.
187. **묶음을 여백으로 내던 손질은 걷었다.** 격자가 이미 묶고 있으므로 남겨두면 같은 것을 두 번 말한다. 네모는 격자 칸을 채우게 두어 30px보다 조금 커졌다 — 한 줄에 셋만 서므로 자리가 남고 **급하게 누르는 화면이라 클수록 낫다.**

### 덱 카드를 펼쳐 보는 팝업 — 2026-08-12

188. **알약은 몇 장인지 말하고, 팝업은 어떤 카드인지 보여준다.** 특혜를 여럿 켜고 나면 "그래서 내 덱이 실제로 어떻게 생겼나"가 궁금해지는데 그것은 **세는 일이 아니라 보는 일**이다. 상 위에 덱을 펼쳐 놓고 훑는 것과 같아서 늘어놓을 자리가 따로 필요하다.
189. **덱 위젯이 쓰는 `CardFace`를 그대로 쓴다.** 여기서 본 그림과 상 위에서 뽑는 그림이 다르면 같은 덱인지 알 수 없다. 그래서 `CardFace.tsx`가 제 스타일시트를 스스로 들여온다 — 위젯 바깥에서 쓰이는 자리가 생겼고, 쓰는 쪽이 스타일을 따로 챙겨야 하면 언젠가 빠뜨린다.
190. **카드를 섞지 않는다.** 종류 차례대로 늘어놓아야 무엇이 몇 장인지 훑어진다. 섞으면 실물 덱과 같아지지만 그것은 이 화면이 하려는 일이 아니다.
191. **판은 여전히 안 읽는다.** 구성에서 편 카드일 뿐이고 무엇이 뽑혔는지·몇 장 남았는지는 여기 없다(구현 결정 160).
192. **머리는 안 구르고 격자만 구른다.** 카드를 한참 내린 뒤에 닫으려고 도로 올라오게 하지 않는다. 한 줄에 두 장이면 폰에서도 메달의 숫자가 읽히고, 가로로 구르게 두면 몇 장을 지나쳤는지 알 수 없다.
193. **뒤집히는 시늉은 팝업에서 끈다.** 위젯에서는 한 장을 뽑을 때마다 한 번 도는 것이라 뜻이 있지만 여기서는 스무 장이 한꺼번에 펄럭인다.
194. **자리 잡기(`DeckGallery`)와 알맹이(`DeckGalleryPanel`)를 갈랐다.** `createPortal`은 `document.body`를 요구해서 서버 렌더로 확인할 수가 없는데, 확인하고 싶은 것은 자리가 아니라 **무엇이 늘어서는가**다. 갈라 두면 그쪽만 통째로 덮인다 — 구현 결정 164의 "스토어에 매달리지 않게 짜면 확인할 수 있다"와 같은 손질이다. 덤으로 `role="dialog"`가 배경막이 아니라 판에 붙어 HTML도 옳아졌다.
195. **나가는 길은 × 단추와 Escape다.** 훑어보기만 하는 팝업이라 되돌릴 것이 없고, 되돌릴 것이 없으면 뜸을 들일 이유도 없다(`ConfirmDialog`와 정반대다). **바깥 누르기는 두지 않는다** — 화면을 통째로 덮으면서 바깥이랄 것이 없어졌고, 남은 빈 자리는 카드를 굴리려고 손을 얹는 자리라 거기서 닫히면 구르다 말고 사라진다.

### 카드가 안 보이던 까닭 — 2026-08-12

196. **`.deck__face`는 `<span>`이라 flex 부모 밖에서는 인라인으로 남는다.** 위젯 안에서는 `.deck__pile`·`.deck__discard`가 flex라 항목이 저절로 블록이 되어 `width/height: 100%`가 먹었다. 시트의 덱 펼쳐 보기에서 flex가 아닌 부모 밑에 놓이자 **상자가 통째로 접혀** 카드 그림이 사라지고 값 메달도 폭 0이 되었다. **섞기 표식만 `min-width: 9px` 덕에 살아남아 혼자 떠 보였다** — 증상이 "가끔 셔플 마크만 보인다"였던 까닭이다.
197. **고치는 자리는 근원이다.** 팝업 쪽에서 부모를 flex로 만들어 덮을 수도 있었지만, 그러면 다음에 또 다른 부모 밑에 놓을 때 같은 일이 난다. `.deck__back`·`.deck__face`에 `display: block`을 적어 두면 flex 항목에게는 어차피 블록이라 위젯 쪽은 아무것도 달라지지 않는다.
198. **팝업은 테를 두르지 않고 화면을 통째로 덮는다.** 상자 안에 상자를 넣으면 폰에서 카드가 앉을 자리가 그만큼 준다. 대신 **뒤쪽 화면이 살짝 비치게** 두어 덮은 것이지 딴 화면으로 넘어온 것이 아님을 알린다. 머리는 제 바탕을 깐다 — 카드가 그 밑을 지나가면 글자가 겹쳐 읽힌다.
199. **화면은 덮되 카드는 손에 맞는다.** 격자를 34rem에서 멎게 해 태블릿에서 카드가 무한정 커지지 않게 한다.

### 카드 표식을 실물 자리에 — 2026-08-12

200. **왼쪽 아래 홈은 섞기 자리가 아니라 덱 주인 자리다.** 실물에서는 1·2·3·4·M(몬스터)이나 **그 카드를 넣어 준 클래스의 표식**이 들어간다 — 판이 끝나고 덱을 도로 가를 때 쓴다. 형님이 짚었고 실물 카드 사진으로 확인했다. 섞기는 **오른쪽 아래**다.
201. **자리는 실물 카드를 재서 얻었다.** 형님이 석 장(표준 ×2·굴림 빛·+1 이동불가)을 찍어 주었고 눈금을 그어 읽었다 — 상태이상·원소 배지는 **왼쪽 가운데에 마름모**로 여럿이면 아래로 쌓이고, 굴림은 **오른쪽 가운데의 초록 마름모**다. 웹 검색으로는 위치가 안 나왔다(규칙 문서는 뜻만 적고 배치를 안 적는다) — **가진 그림을 재는 것이 빨랐다.**
202. **가운데 문양은 눈으로 대조해 알아냈다.** 굴림 카드의 가운데 별 문양이 무엇인지 우리 원소 아이콘 여섯과 나란히 놓고 봤더니 `light`와 같은 모양이었다(구현 결정 119와 같은 손질).
203. **색은 실물 상태이상 표식을 따른다.** 중독 초록·부상 주황·이동불가 빨강·무장해제 파랑·기절 남색·혼란 갈색·투명 검정. 실물 카드에서 이동불가가 빨간 마름모인 것을 확인했고 나머지는 표식 색 목록을 따랐다. **저주만 우리 색이다** — 실물에 표식이 없다(카드다).
203-1. **팩의 원소 아이콘은 낱개 문양이 아니라 통짜 배지다.** 색 있는 원반에 흰 문양이 얹히고 흰 테까지 둘려 있다. 그것을 마름모 안에 넣고 `brightness(0) invert(1)`로 물들였더니 **문양이 사라지고 흰 원반만 남았다** — 형님이 "원소 마크가 안 나온다"고 짚었다. **있는 그대로 얹는다** — 자리와 크기만 주고 색은 건드리지 않는다(구현 결정 15). 같은 병이 시트의 알약 목록에도 있어 함께 고쳤다.
203-2. **에셋을 쓰기 전에 그대로 한 번 그려 본다.** "원소 색을 쓴다"고 적어 두고도 그 파일이 이미 색을 들고 있는 줄 몰랐다 — 파일 이름만 보고 낱개 문양이라고 넘겨짚은 것이다. 구현 결정 119와 같은 자리다.
204. **원소 색은 `elements.ts`가 정본이다.** 표식 표에 다시 적지 않는다 — 두 곳에 적으면 언젠가 어긋나고, 원소 트래커와 카드가 다른 색이면 같은 것으로 안 읽힌다.
205. **상태이상 그림을 못 구해 한 글자로 대신한다 — 임시다.** 그 그림은 Creator Pack `General Icons.pdf`의 아직 안 가져온 쪽에 있는데 **팩 원본이 작업 기계에 없다.** 흉내 내 그리지 않는다 — 구현 결정 31이 막는 것은 "실물 카드의 그림을 베끼는 것"이고 이것이 바로 그 자리다. 굴림 화살만 직접 그렸다(도형이지 그림이 아니다).
206. **마름모는 사각형을 45도 돌려 만들고 안의 글자·그림은 되돌려 세운다.** 기울어 있으면 읽히지 않는다.

207. **표식만 얹으면 바탕 없이 뜬다. 홈을 먼저 깐다.** 실물 카드는 **양쪽 아래에 홈이 있고** 왼쪽에 덱 주인, 오른쪽에 섞기가 앉는데, 팩의 빈 카드 그림에는 **왼쪽 홈만 박혀 있다.** 그 홈을 44×44로 오려 원 바깥을 알파로 잘라 두면(`socket.webp`) 어디에나 얹을 수 있다 — 값 메달을 여백만 잘라 뽑은 것과 같은 손질이고 다시 그리지 않았다(SPEC 13.1).
208. **오려낼 자리는 재서 정한다.** 400×271 그림에서 홈 바깥 테를 재니 중심 (54.8, 215.5)·지름 38이었다. 자르는 원의 반지름을 19.5·19.0·18.5로 뽑아 나란히 보고 **18.5를 골랐다** — 그보다 크면 2시 방향에 비늘 잡티가 딸려 온다.
209. **표식 크기는 실물에서 견준다.** 화살이 홈 지름의 7할쯤을 채운다. 그보다 작게 두면 홈만 크고 표식이 점처럼 보인다.
210. **ImageMagick으로 미리 본 것은 화면이 아니다.** 합성해 견주다가 섞기 SVG가 흰 네모로 나와 잠깐 고장으로 읽었는데, SVG는 멀쩡했고 내 합성 파이프라인이 망친 것이었다. **자리와 크기를 재는 데는 쓰되 그려지는 모양은 브라우저에서 본다**(구현 결정 10과 같은 결).

211. **왼쪽 아래 홈에 클래스 표식을 앉힌다.** 실물에서 그 자리는 덱 주인의 것이다(1·2·3·4·M 또는 그 카드를 넣어 준 클래스의 표식) — 판이 끝나고 덱을 도로 가를 때 쓴다. **그림이 없는 클래스는 첫 글자로 대신한다**(사자의 턱 넷은 팩에 그림이 없다, 구현 결정 119).
212. **카드는 누구 것인지 스스로 알아내지 않고 받아서 그린다.** 카드가 캐릭터·클래스 스토어를 직접 부르면 축 ②가 축 ①에 닿는 자리가 흩어진다 — 그 자리는 `perkSource.ts` 하나여야 한다(구현 결정 142). 표식을 짓는 것도 거기 두었고(`useCardOwner`·`ownerBadge`), 캐릭터 시트는 순수 함수 쪽만 부른다.
213-1. **홈 테 안쪽을 따로 재서 적어 둔다(`ownerInner`).** 바깥 지름에 어림수(0.86)를 곱해 쓰다가 원반이 테를 덮어 홈이 사라졌다. 오려 둔 홈 그림을 12배로 늘려 재니 바깥 37px·안쪽 31px이었다(원본 400px 카드와 1:1로 오렸으므로 그대로 카드 좌표다) — **어림수 대신 잰 값을 적으면 딱 들어맞는다.**
213-2. **잰 자리에서 반 픽셀 옮겼다 — 재는 것보다 보는 것이 맞는 자리다.** 재기만 해서는 왼쪽 테가 두껍고 아래 테가 얇아 보였다(홈이 정원이 아니고 빛도 위에서 든다). **눈으로 골라야 하는 것은 후보를 늘어놓고 형님이 고른다** — 카드를 여덟 배로 늘려 0.5px씩 옮긴 아홉 칸을 만들었고 `x−0.5 y+0.5`가 뽑혔다. 값 메달 자리를 정할 때와 같은 손질이며, **소수점 자리를 쓸 수 있는 것은 값이 픽셀이 아니라 퍼센트이기 때문**이다.
213-3. **ImageMagick의 내장 SVG 그리기를 믿지 않는다.** 클래스 표식이 흰 네모로 나와 두 번 헤맸는데 SVG는 멀쩡했다 — `rsvg-convert`로 그리면 제대로 나온다. 자리를 재는 합성에는 그쪽을 쓴다.
213. **표식은 물들이지 않고 양피지 원반을 깐다.** 클래스 표식은 거의 검정이라 어두운 홈에 묻힌다. 시트·일지의 클래스 배지와 같은 손질이다(구현 결정 41) — 화면마다 다른 결로 그리면 같은 것으로 안 읽힌다.

214. **가로 %와 세로 %를 섞어 쌓지 않는다.** 배지의 `width`는 카드 **가로**의 %이고 `top`은 **세로**의 %다. 카드가 가로로 길어(437:296) 가로 15%짜리 배지는 세로로 치면 22%인데 칸 간격을 세로 17%로 두었더니 **원소가 둘 이상일 때 겹쳤다.** 카드 비를 곱해 맞출 수도 있지만 그러면 비가 바뀔 때마다 두 곳을 고쳐야 한다.
215. **쌓는 일은 CSS `translate`에 맡긴다.** `translate`의 %는 **제 border box**를 기준으로 하므로 카드 비와 아예 무관하다 — `115%`가 곧 "배지 하나 + 15% 사이"다. 화면은 몇 번째인지만 넘기고(`--deck-badge-i`, 가운데가 0) 벌리는 폭은 CSS가 안다.

### 상태이상 배지를 팩에서 가져왔다 — 2026-08-12

216. **팩에 `Status Effect Icons.pdf`가 따로 있었다.** `General Icons.pdf`에 있는 줄 알고 한참 찾았다 — **폴더를 먼저 훑었으면 될 일이었다.** 열네 쪽이 곧 카드에 붙는 배지이고 색까지 다 그려져 있다.
217. **어느 쪽이 무엇인지 형님에게 하나씩 물어 확인했다.** 그림만 보고 단정하지 않는다(구현 결정 119) — 실물 카드로 대조된 넷(이동불가·기절·굴림, 그리고 혼란)을 빼고는 짐작이었고, 짐작 넷 중 하나(무장해제)는 이름까지 갈렸다. **객관식으로 그림을 보여 주고 고르게 하는 것이 가장 빨랐다.**
218. **카드 016의 가운데는 빛 원소가 아니라 기절이었다.** 우리 빛 아이콘과 나란히 놓기 전까지 「굴림 빛」이라고 적어 두었다(구현 결정 202) — 우리 빛은 *가는 선으로 그린 별 + 금색 원반*이고 그것은 *속을 파낸 굵은 별 + 남색 마름모*다. **비슷한 그림일수록 나란히 놓아야 갈린다.**
219. **색표를 손으로 적어 두었던 것을 걷었다.** 팩 배지에 색이 이미 들어 있으므로 우리가 정할 것이 없다. 남은 색은 **팩에 배지가 없는 것**(치료·방어·당기기·특수)에만 쓴다.
220. **그림에는 세 갈래가 있다(`markArt`).** ① 완성된 배지(상태이상 14·원소 6) — 그대로 얹는다 ② 검정 실루엣(치료·방어) — 우리가 마름모를 깔고 흰빛으로 얹는다 ③ 아무것도 없음(당기기·특수) — 글자. **셋을 한 함수가 갈라 주어야** 쓰는 쪽이 갈래를 다시 세지 않는다.
221. **수는 팩 그림에 없다.** 밀기2·관통3·치료1의 숫자는 실물 카드도 표식 옆에 따로 적는다. 배지 오른쪽 아래 귀퉁이에 얹되 마름모와 함께 기울지 않게 되돌려 세운다.
222. **굴림 화살도 팩 것으로 바꿨다.** 직접 그려 두었는데 상태이상 열넷 가운데 하나가 바로 그것이었다 — 실물 카드의 초록 마름모와 같다. **직접 그리기 전에 팩을 먼저 뒤진다.**

### 레벨을 경험치에서 뽑는다 — 2026-08-12

223. **레벨은 고르는 값이 아니라 경험치에서 나오는 값이다(`levelForXp`).** 구현 결정 43을 뒤집었다. 눈금은 이제 단추가 아니라 목록이고, 지나온 눈금에 표를 내어 어디까지 왔는지만 보인다.
224. **초안에는 없고 저장할 때 딸려 나간다.** 사람이 고치는 칸이 아니므로 `SheetDraft`에서 뺐다. 그래도 `sheetDiff`가 경험치에서 뽑아 **표에도 적어 둔다** — 목록 화면이 경험치 없이 레벨만 읽는 자리가 있어서, 표에 옛 값이 남으면 시트와 목록이 다른 수를 말한다.
225. **읽는 쪽도 표를 믿지 않고 다시 뽑는다.** 무리 목록과 일지 첫 화면 둘 다 `levelForXp(xp)`로 그린다 — 아직 한 번도 저장하지 않아 표가 어긋난 캐릭터에서도 시트와 같은 수를 말한다. **한쪽만 고치면 두 화면이 갈린다.**
226. **`levelUpReady`를 걷어냈다.** 레벨이 경험치에서 나오므로 뒤처질 일이 없다 — 죽은 코드는 덫이다. 무리 목록의 ▲ 표시와 시트의 "올릴 때가 되었다" 줄, 그 CSS까지 함께 걷었다.

### 값어치 못 하는 시험을 걷었다 — 2026-08-12

227. **상수를 상수와 견주는 시험은 시험이 아니다.** 소스 코드를 한 줄도 지나지 않으므로 무엇도 검증하지 못하고, 상수를 고치면 시험도 따라 고치게 되어 **변경 감지기**로만 남는다. `STANDARD_COMPOSITION`을 같은 리터럴과 견주던 것, `FACE_SLOTS.shuffle.cx > 50`처럼 자리 값끼리 견주던 것을 걷었다. 지켜야 할 사실이면 **렌더를 거쳐** 확인한다 — "섞기가 주인 홈 자리에 안 그려진다"는 그렇게 바꿔 적었다.
228. **어떤 입력에도 늘 통과하는 시험은 근거가 되지 못한다.** 덱 설정의 「덱 새로 짜기」는 스토어에 담긴 판이 있어야 나오는데 서버 렌더에는 스토어가 안 비친다(구현 결정 150) — `not.toContain`으로 적어 두면 통과하지만 **코드가 맞아서 통과한 것이 아니다.** 둘을 걷고 왜 볼 수 없는지 파일 머리에 적었다.
229. **묘비는 세우지 않는다.** "개발용 Test 위젯은 걷어냈다"는 그 id를 누가 다시 만들어야만 실패한다. 걷어냈다는 사실은 CLAUDE.md가 적고 있다.
230. **거르는 눈은 「이 시험이 실패할 수 있는 길이 있는가」다.** 줄 수를 줄이려고 걷는 것이 아니다 — 실패할 길이 없는 시험이 섞여 있으면 **통과 숫자가 안심의 근거가 되지 못한다.**

### 퍽으로 더한 카드를 갈라 보인다 — 2026-08-12

231. **어디서 온 카드인지는 표준 덱과 견주면 갈린다(`buildDeckMarked`).** 퍽으로 더해진 것은 둘 중 하나다 — ① 표준에 **없던 종류**(`p1.wound`·`+3`…) ② 표준보다 **더 놓인 것**(`+1`이 표준 다섯인데 일곱이면 뒤의 둘). **카드에 꼬리표를 달지 않는다** — 구성표와 표준 덱만 있으면 언제든 다시 셀 수 있는 것을 저장하면 저장물이 표와 어긋날 자리가 하나 더 생긴다(구현 결정 65와 같은 결).
232. **더한 쪽만 밝힌다. 원래 있던 것을 흐리지 않는다.** 표준이 스무 장이라 그쪽을 죽이면 화면 대부분이 죽어 보인다 — 형님이 "추가된 것만 강조"라고 짚은 그대로다.
233. **금테 밑에 어두운 선을 한 겹 먼저 두른다.** 카드 그림이 갈색·놋쇠라 금테를 바로 붙이면 묻힌다 — 카드 앞면이 어두운 보드 위에서 뜨지 않게 쓰는 수법과 같다.
234. **글자를 붙이지 않는다.** 카드가 작아 읽을 자리가 없다. 대신 **머리에 한 번만** 무엇인지 알리고(「특혜 N장」+ 같은 테를 두른 조각), **읽어주는 쪽에는 카드마다 글자로 간다** — 테는 눈에만 보이는 것이라 소리로도 갈려야 한다.

235. **당기기는 밀기를 반 바퀴 돌려 쓴다.** 팩에 그림이 없는데 마름모가 대칭이라 틀은 그대로고 화살만 뒤집힌다 — 형님이 짚었다. **파일을 새로 만들지 않고 화면에서만 돌린다**(구현 결정 15 — 그리는 순간에만 거는 것이라 사본을 고쳐 담는 것과 다르다). 돌릴지 말지는 표식이 들고 있고(`badgeTurned`) 화면은 `markArt`가 준 것만 본다.

### 파티 기록지에도 열람·편집 모드 — 2026-08-12

236. **캐릭터 시트와 같은 짜임으로 붙였다.** 까닭도 같다 — 평판을 −5에서 +8로 옮기면 그 사이 열세 번이 나가고 `version`이 손가락 수만큼 올랐다. 그냥 들여다보는 동안에도 칸이 열려 있어 **스치기만 해도 값이 바뀌었다**(구현 결정 165).
237. **바뀐 칸만 보내는 것이 여기서 더 중요하다.** 파티는 파티원이면 누구나 고치므로(구현 결정 44) **통째로 덮으면 그 사이 옆 사람이 고친 것을 되돌린다** — 캐릭터보다 이쪽이 훨씬 잦다.
238. **띠 클래스를 `char__`에서 `sheet__`로 옮겨 두 시트가 함께 쓴다.** 같은 일을 하는 띠를 두 벌 그리면 반드시 어긋난다 — 파티 쪽에 붙이면서 옮겼다.
239. **파티에는 「내 것인가」가 없다.** 캐릭터 시트는 남의 것을 잠그지만 파티 상태는 함께 쓰는 것이라 서버에 못 닿는 때만 잠긴다.

### 캐릭터가 먼저 서고 파티에는 나중에 든다 — 2026-08-12

240. **캐릭터를 먼저 세우고 파티에 넣는다(`0015`).** 사람은 제 캐릭터를 먼저 정하고 누구와 놀지는 그다음에 정한다 — 봉투를 뜯는 것과 약속을 잡는 것은 다른 일이다. `campaign_id`가 비어 있을 수 있게 되었다.
241. **파티가 없으면 「파티원이 본다」가 주인에게서도 감춘다.** 읽기 정책이 `is_party_member(campaign_party(campaign_id))` 하나였는데 `campaign_id`가 `null`이면 그 값도 `null`이라 **누구에게도 안 보인다** — 만든 사람 자신에게도 그렇다. 파티에 들기 전까지는 주인만 보는 줄을 더했다.
242. **`touch_character`가 `campaign_id`를 조용히 되돌리고 있었다.** 파티를 옮기는 갱신이 **오류 없이 통과하고 값만 안 바뀌었다** — 그래서 RLS를 찔러 보는 시험이 "통과"로 읽혔다. **조용히 무시하는 것이 가장 나쁘다**: 화면은 저장했다 하고 서버는 안 바꾸니 사람이 알 길이 없다. 막을 것이면 거절해야 하고 열 것이면 열어야 한다.
243. **막는 자리를 트리거에서 RLS로 옮겼다(`0016`).** 고정을 둔 뜻("남의 파티로 새어 나가지 않게")은 `with check`가 더 정확하게 지킨다 — **제가 든 파티로만** 옮길 수 있고 아니면 거절된다. `owner_id`·`created_at` 고정은 그대로 둔다.
244. **고치기 정책에 `with check`가 비어 있었다.** `owner_id = auth.uid()`만 보고 있어 아무 기록지 id나 적으면 남의 파티에 제 캐릭터를 밀어 넣을 수 있었다 — 화면이 파티를 안 옮겨서 드러나지 않았을 뿐이다. **드는 것이 진짜 기능이 되면서 반드시 막아야 할 자리가 되었다.**
245. **캐릭터가 제 주소를 갖는다(`#/character/<id>`).** 예전 `#/journal/<파티>/<캐릭터>`로는 **파티에 안 든 캐릭터를 열 수가 없다.** 일지는 두 갈래로 줄었다 — 목록과 파티 기록지.
246. **일지에 파티 목록을 내지 않는다.** 파티 기록지로는 캐릭터의 부제를 눌러 들어간다. **세우는 자리는 남긴다** — 첫 파티는 어디선가 세워야 하고, 캐릭터의 「파티에 들기」가 거기로 보낸다.
247. **캐릭터를 세우는 자리는 하나뿐이다.** 파티 화면의 무리 목록에도 세우는 단추가 있었는데 걷었다 — **창구를 둘로 두면 어긋난다.** 거기서는 일지로 가는 길만 알려 준다.
248. **한 장만 읽는 스토어를 따로 뒀다(`oneCharacter.ts`).** `characterStore`는 기록지 하나의 캐릭터 전부를 들고 있어 파티에 안 든 캐릭터에는 닿을 수 없다 — 되살아나는 주기도 다르다(구현 결정 46과 같은 근거).

249. **이름도 생성할 때 정하고 그 뒤로는 못 바꾼다(`0017`).** 파티원은 이름으로 서로를 부른다 — 축 ②의 이름표도 그것이고(`satchel/roster.ts`) 전투에서 누구의 체력·덱인지 가리는 것도 그것이다. **판 도중에 이름이 바뀌면 옆 사람이 보던 것이 딴 사람이 된다.** 클래스와 같은 세 겹으로 막았다 — 화면은 칸이 아니라 글로 그리고, 초안과 `CharacterEdits`에서 빼 **보낼 수조차 없게** 하고, 서버가 거절한다.
250. **잠그는 것이 둘이 되면서 함수 이름을 바꿨다.** `lock_character_class`가 이름까지 잠그면 하는 일과 이름이 어긋난다 — `lock_character_identity`로 옮기고 옛 함수는 지웠다. **이름이 거짓말하는 함수를 남겨 두지 않는다.**

### 클래스 이름만 있고 규칙이 없던 자리 — 2026-08-12

251. **죽은 CSS를 걷다가 살아 있는 규칙까지 지웠다.** 레벨을 목록으로 바꾸면서 `.char__level--reached`를 갈아 끼우려다 `.char__level--on`까지 함께 사라졌고, **그 뒤로 현재 레벨에 아무 표시가 없었다.** 형님이 짚었다.
252. **클래스 이름만 있고 규칙이 없는 것은 시험이 못 잡는다.** 마크업은 멀쩡하므로 "`char__level--on`이 하나 있다"는 시험은 그대로 통과한다 — **화면에만 드러나는 고장**이다. 걷어낼 때는 지우려는 것 하나만 짚어 지운다(`rfind('/*')`로 앞 주석까지 끌어오는 손질은 이웃을 함께 먹었다).
253. **화면이 붙이는데 규칙이 없는 클래스를 한 번 훑었다.** CSS의 선택자와 `.tsx`의 `className`을 맞대 보면 나온다. 이번에 셋이 더 나왔다(`deckview__glyph`·`markn`·`markwrap`) — 어제 `markArt`를 물리면서 만들어 놓고 규칙을 안 쓴 것이라 **치료·방어 아이콘이 크기 없이 펴져 알약을 뚫고 나올 자리였다.**

### 레벨 표에 체력을 함께 적는다 — 2026-08-12

254. **눈금 하나에 경험과 체력을 나란히 적는다.** 「이 레벨의 최대 체력」을 따로 한 줄 두었더니 **지금 레벨의 것 하나만 보였다** — 다음 레벨에 얼마가 되는지, 지금까지 얼마나 올랐는지는 알 수 없었다. 표는 아홉 칸이 이미 늘어서 있으므로 그 칸 안에 넣으면 아홉 개가 한눈에 든다. 따로 두던 줄은 걷었다.
255. **색으로 가른다 — 체력은 붉은빛, 경험은 푸른빛.** 한 칸에 숫자가 셋(레벨·경험·체력) 늘어서므로 자릿수만으로는 어느 것이 무엇인지 알 수 없다. 색은 HP/XP 트래커가 이미 쓰는 것을 그대로 가져왔다 — **같은 값이 화면 두 곳에서 다른 색이면 그것부터 헷갈린다.** 지금 레벨 칸은 금빛으로 채워지므로 그 안에서는 어두운 쪽으로 바꿔 깐다.
256. **클래스 수치가 없으면 체력 자리를 비운다.** `maxHpFor`가 `null`을 주면 아무것도 안 적는다(구현 결정 115와 같은 선) — 표만 아홉 칸 그대로 서고 경험 눈금은 여전히 보인다.
257. **`char__class`도 251과 같은 손질에 함께 먹혔다.** `.char__maxhp`를 걷어내다 붙어 있던 규칙까지 갔다 — 이번엔 훑기(253)가 잡았다. **그 훑기를 손질할 때마다 돌린다.**

### 캐릭터 시트를 한 장의 종이로 — 2026-08-12

258. **상자 안에 상자를 넣지 않는다.** 시트 전체가 상자 하나에 들고 그 안의 칸마다 또 상자가 있었다 — 좌우로만 크롬이 세 겹(페이지 16px + 시트 14px + 칸 16px) 쌓여 폰(390px)에서 **양쪽 93px, 화면 폭의 4분의 1**이 내용 아닌 것으로 갔다. 형님이 짚었다. 칸을 가르는 데 상자는 필요 없다 — **선 하나면 된다.** 실물 캐릭터 시트도 한 장의 종이에 칸이 그어져 있을 뿐이다.
259. **혼자 서는 시트만 상자를 잃는다.** 로스터 줄에 매달리는 `.char`는 눌린 줄과 한 덩어리로 보여야 하므로 바깥 상자를 지킨다(구현 결정 177) — **안쪽 칸만 평평해진다.** 두 자리가 같은 결로 읽히면서 매달린 쪽의 짜임도 안 깨진다.
260. **구분선은 고풍스럽게 — 양끝이 스러지는 금빛 선에 마름모를 문다.** 그냥 일직선은 화면 부품처럼 보인다. **선은 `::before`가, 마름모는 `::after`가 그린다** — 한 겹으로는 안 된다. 마름모는 사각형을 45도 돌린 것이라 `transform`이 필요하고, 돌리면 선까지 함께 기운다(구현 결정 206과 같은 자리). **가운데 틈은 픽셀로 고정한다** — %로 두면 넓은 화면에서 틈이 마름모보다 훨씬 벌어져 선이 두 동강 난 것처럼 보인다.
261. **선은 `.char__col`의 자식 전부에 건다.** 앞에 무엇이 오든(머리든 옆 칸이든) 반드시 무엇인가가 오므로 갈래를 나눌 것이 없다. 두 단으로 서면 양쪽 첫 칸이 머리 밑에 나란히 서는데 그때도 맞다.
262. **두 단은 `display: contents`를 벗으면서 생긴다.** 감싼 상자가 폰에서는 없는 것이나 같고 넓어지면 살아나 각자 한 단이 된다 — **DOM을 갈아 끼우지 않으므로 좁힐 때 차례가 흐트러지지 않는다.** 가르는 자리는 하나뿐이다: 폰에서는 두 단이 이어 붙어 한 줄로 서므로 **DOM 차례가 곧 읽는 차례**이고, 그것을 지키려면 지금 차례를 한 번만 끊을 수 있다. 특혜 뒤에서 끊는 것이 두 단의 키가 가장 비슷하다.
263. **머리는 위에 붙어 따라온다.** 시트가 길어 아이템까지 내려가면 누구의 시트인지 화면에서 사라진다. 저장 띠를 아래에 붙여 둔 것과 짝이다(구현 결정 170) — **오래 머무는 화면에서는 누구의 것인지와 무엇을 누르는지가 늘 보여야 한다.** 혼자 서는 시트에만 건다(매달린 쪽은 바로 위 줄이 이미 이름을 적고 있다).
264. **맨 윗줄 자식이 넷뿐이라는 것을 못박았다(`topLevelClasses`).** 선이 CSS에 달려 있으므로 **감싼 것 바깥에 칸을 두면 앞 칸과 붙어 버리는데 마크업은 멀쩡하다** — 화면에만 드러나는 자리다(구현 결정 252와 같은 병). 짜임을 시험이 지킨다.

### 캐릭터 한 장의 머리를 하나로 — 2026-08-13

265. **붙박이는 화면 끝까지 덮어야 붙박이로 읽힌다.** 페이지 여백 안에 두었더니 **좌우로 다른 것들이 올라가는 것이 스크롤 내내 다 보였다** — 형님이 짚었다. 여백만큼 음수 마진으로 밀고 같은 값을 제 안쪽 여백으로 되돌린다: 알맹이 자리는 그대로인데 바탕만 화면 끝까지 간다. **위쪽은 안전영역까지 덮는다** — `top: 0`으로 붙이고 노치 자리는 제 여백으로 비우므로 노치 밑으로 글자가 지나가지 않는다.
266. **띠가 둘이면 이름이 두 번 나온다.** 뒤로가기 띠와 시트의 머리가 따로 있었다. 뒤로가기·표식·이름·부제·파티 문을 한 줄에 세우고 **시트는 혼자 설 때 제 머리를 안 그린다.** 매달릴 줄이 있는 로스터에서는 위에 아무것도 없으므로 그대로 그린다.
267. **파티로 가는 문은 아이콘이다.** 글자 단추가 한 줄을 통째로 쓰던 자리다. 펼친 기록지를 직접 그린 도형으로 냈고(격리 규칙 1-1 밖), **읽어주는 쪽에는 파티 이름까지 글자로 간다** — 표식은 눈에만 보인다.
268. **`.char .sheet__block`이 `.char__col > *`을 이겨 구분선이 글자를 덮었다.** 상자를 걷으면서 적은 `padding: 0`이 (0,2,0)이고 선의 여백을 잡는 규칙이 (0,1,0)이라 **여백이 아예 안 잡혔다.** 선은 절대 위치라 제자리에 그려지는데 밑에 비워 둔 자리가 없으니 그대로 제목 위에 얹혔다. **여백은 선을 긋는 규칙이 함께 잡는다** — 두 곳에서 정하면 또 어긋난다.
269. **특이도가 뒤집힌 것은 훑기가 못 잡는다.** 클래스는 붙어 있고 규칙도 있다(구현 결정 253의 훑기는 통과한다) — **어느 규칙이 이기느냐**는 화면에서만 보인다. 상자를 걷는 규칙과 여백을 잡는 규칙이 같은 요소를 두고 다투면 그 자리를 의심한다.

### 파티 기록지도 한 장의 종이로 — 2026-08-13

270. **두 시트가 같은 짜임을 나눠 쓴다(`.paper`).** 캐릭터 시트에만 있던 것을 뽑아냈다 — 같은 일을 하는 것을 두 벌 그리면 반드시 어긋난다(구현 결정 238과 같은 손질). `.paper`는 폭과 자리를 잡고, `.paper .sheet__block`이 안쪽 상자를 걷고, `.paper__col > *`이 장식선을 긋는다. 두 단(`.paper--wide`)은 캐릭터 시트만 쓴다.
271. **기록지·무리·파티원이 한 장에 든다.** 셋이 저마다 상자를 두르고 있었다. 기록지는 제 칸이 그대로 단의 자식이 되도록 안쪽을 `paper__col`로 감싸고, 무리와 파티원은 함께 한 단에 담았다 — **감싼 자리가 곧 선이 그어지는 자리**다.
272. **저장 띠는 단 밖에 둔다.** 단 안에 들면 띠 위에 장식선이 그어져 띠가 칸으로 읽힌다. 캐릭터 시트도 같은 자리다.
273. **`.sheet`를 `display: contents`로 지우지 않았다.** 편집 중임을 알리는 금빛 테(`sheet--editing`)는 상자가 있어야 그려진다 — 상자를 지우면 **편집 중이라는 표시가 통째로 사라진다.** 그래서 안쪽을 한 겹 더 감쌌다.
274. **띠에 테를 긋지 않는다.** 바로 밑에 종이의 첫 장식선이 오므로 가로줄이 둘 겹친다. 안 굴렸을 때는 장식선이 가르고, 굴리면 그림자와 밝은 바탕이 가른다.
275. **파티 띠의 부제는 머무는 곳이다.** 상 위에서 "우리 지금 어디였지"를 가장 자주 묻는다. 캐릭터 쪽 부제(주인·클래스·손 장수)와 같은 자리를 쓴다.

### 위젯 모서리를 둥글렸다 — 2026-08-13

276. **모서리 반지름은 `--wf-radius` 한 곳에서 정한다.** 같은 모서리를 셋이 함께 그린다 — 테·속을 오려내는 회전자·그 위의 제목 띠. **속은 테 두께만큼 작아야 한다**(`calc(var(--wf-radius) - 1px)`): 같은 값을 주면 모서리에 테 색이 비쳐 두 겹으로 보인다. 세 곳에 적어 두면 하나만 고쳤을 때 어긋나는데 모서리라 눈에 잘 안 띈다.
277. **금빛 갈고리도 함께 휜다.** 위젯 왼쪽 위의 ㄱ자 표식은 직각이라, 테만 둥글리면 **둥근 테 밖으로 뿔이 삐져나온다.** `border-top-left-radius`를 같은 값으로 준다.
278. **크기 손잡이도 같은 곡선을 탄다.** 34px 상자가 모서리 밖으로 8px 나가 있고 그 안에 14px짜리 갈고리가 가운데 서므로, **갈고리의 꼭짓점은 테의 꼭짓점에서 2px 안쪽**이다 — 반지름을 `--wf-radius - 2px`로 준다. 직각으로 두었더니 세로 팔이 둥근 테를 뚫고 밖으로 나갔다. 형님이 짚었고 그려 보고 확인했다.
279. **후보를 그려 형님이 골랐다(3·6·10·14px → 10px).** 실제 셀 크기(85×77)를 네 배로 늘려 나란히 놓고 봤다 — 구현 결정 213-2와 같은 손질이다. 코드를 읽어서는 "적당히"가 몇인지 알 수 없다.

### 캐릭터 생성을 제 화면으로 — 2026-08-13

280. **고르는 일이 커지면 화면을 준다(`#/character/new`).** 클래스와 이름은 세울 때만 정할 수 있고 그 뒤로는 서버가 거절한다(`0014`·`0017`) — **고칠 기회가 한 번뿐인 자리**를 목록 아래 접힌 폼에서 처리하고 있었다. 봉투를 고르는 일이라 이름만 훑는 것으로는 부족하고 표식·핸드 사이즈·레벨별 체력을 나란히 놓고 견주게 된다.
281. **`/character/new`가 `/character`의 뒷자리를 이기게 했다.** 앞머리만 보면 캐릭터 한 장짜리 화면이 `new`를 id로 알아듣고 **빈 화면**을 낸다. `routeKey`가 **정확히 맞는 것을 앞머리보다 먼저** 보게 한 줄이 그 몫이다 — id가 uuid라 부딪힐 일이 없다.
282. **굴리는 것은 브라우저가 한다(`scroll-snap-type: x mandatory`).** 손가락 넘기기가 공짜로 붙고 관성·되튐까지 기기 것이 그대로 쓰인다. 직접 만들면 그 결을 흉내 내야 하고 흉내는 늘 어딘가 어긋난다.
283. **고른 것은 상태가 아니라 굴린 자리에서 읽는다.** 둘로 두면 손으로 넘겼을 때와 단추로 넘겼을 때가 어긋난다 — 단추도 상태를 바꾸는 것이 아니라 굴린다(`scrollTo`).
284. ~~**「나중에 정한다」가 맨 앞이다.**~~ **같은 날 걷었다 — 형님이 뺐다.** 서버는 클래스 없는 캐릭터를 여전히 받지만(구현 결정 183 — 옛 캐릭터가 갇히지 않게), **세우는 자리에서 고르지 않을 까닭이 없다**: 봉투를 뜯고 나서 만드는 것이라 무엇인지 이미 안다.
285. **이름 규칙을 순수 함수로 뗐다(`characterName.ts`).** 거르는 자리가 이 화면 하나뿐이라 통째로 덮어야 한다. 한글·영문·숫자·공백과 탈이 안 나는 문장부호만 받는다 — `<>&"`는 화면·주소·저장 어디선가 반드시 한 번은 걸린다. **이모지도 뺐다**: 기기마다 다르게 그려져 옆 사람이 보는 이름표와 내가 보는 것이 달라진다.
286. **겹치는 이름은 대소문자를 가리지 않고 막는다.** `Bob`과 `bob`이 한 상에 앉으면 누구의 체력·덱인지 알 수 없다(구현 결정 75). 공백만 다른 것도 같은 이름으로 본다 — **눈에 안 보이는 차이로 다른 이름이 되면 안 된다**(구현 결정 168과 같은 결).
287. **아직 아무것도 안 친 것은 잘못이 아니다.** 화면을 열자마자 빨간 글씨가 떠 있으면 무엇을 잘못한 줄 안다 — 경고는 한 글자라도 친 뒤에 나오고 단추는 그동안에도 잠겨 있다.
288. **경고 자리를 늘 잡아 둔다.** 떴다 사라질 때마다 아래 띠가 오르내리면 **누르려던 단추가 손가락 밑에서 움직인다.**
289. **그림과 플레이버는 아직 없다.** 팩에서 더 가져오는 것은 ATTRIBUTION을 함께 고쳐야 하는 별개의 일이고(SPEC 13.1), 플레이버 글은 카드에 인쇄된 것이라 담지 않는다(절대 원칙 1). 형님이 보류해도 된다고 했다.
290. **`ClassPicker`를 걷었다.** 캐러셀이 그 일을 하므로 쓰는 데가 없다 — 묘비를 세우지 않는다(구현 결정 229).

291. **넘기는 화살은 얹는 것이지 자리를 차지하는 것이 아니다.** 좌우로 3.2rem씩 비워 두었더니 **레벨표가 그만큼 좁아졌다** — 아홉 칸이 들어가는 줄이라 그대로 손해다. 얹으면 카드가 폭을 다 쓴다. **높이는 표식 원반에 맞춘다**: 카드 한가운데에 두면 레벨표 양끝 칸을 덮는데 원반 옆에 서면 덮을 것이 없다.
292. **화살에 테를 두르지 않는다.** 동그라미를 두르면 카드 위에 단추 둘이 더 놓인 꼴이라 무엇이 알맹이인지 흐려진다. 캐럿만 두 배로 키웠다.
293. **지금 보는 점만 다이아다.** 「1 / 22」를 점으로 바꿨는데, 스물 넘게 늘어서므로 크기·색만으로는 어느 것이 켜졌는지 잘 안 보인다 — **모양이 갈리면 훑는 눈에 바로 걸린다.** 누르는 자리는 아니다: 스물이 넘는 점을 손가락으로 짚게 하면 옆의 것이 눌린다.
294. **빈 칸의 안내 글은 연하게 둔다.** 적힌 값과 같은 색이면 이미 무엇인가 적혀 있는 줄 안다. 문구도 「이름을 짓는다」에서 「캐릭터 이름 입력」으로 바꿨다.
295. **칸 위의 제목을 걷었다.** 빈 칸에 「캐릭터 이름 입력」이라 적혀 있으므로 위에 「이름」을 또 적으면 같은 말을 두 번 한다. **읽어주는 쪽에는 `aria-label`이 그대로 간다** — 눈에 보이는 글자만 걷은 것이다.

296. **핸드 사이즈는 그림 안에 수로 적는다.** 「손에 드는 카드 12장」이라고 풀어 쓰면 한 줄을 통째로 쓰는데, 실물 시트도 그림과 수로 적혀 있다. **수는 SVG 밖에 얹는다** — 그래야 `sl-numeral`(Pirata One)이 그대로 먹고 색도 CSS가 정한다. 앉는 자리는 그림 좌표에서 재서 적었다(앞 카드 y 12~34 → 높이의 57.5%, 구현 결정 213-1과 같은 손질).
297. **그 그림은 우리가 그린 것이고 앞으로도 그렇다 — 팩에 핸드 표식이 없다.** 처음에는 못 찾아 "팩이 오면 갈아 끼운다"고 적어 두었는데, **형님이 뒤져 확인해 주어 아예 없다는 것을 알았다.** 도형 둘이라 베낄 것도 없다(금화와 같은 선, 구현 결정 106). **되돌아갈 길로 적어 둔 것을 걷었다** — 오지 않을 것을 기다리는 주석은 다음에 읽는 사람을 헷갈리게 한다(절대 원칙 4의 "되돌아갈 길로도 두지 않는다"와 같은 결).
298. **얼개는 짐작하지 말고 실물을 봤어야 했다.** 부챗살로 석 장을 벌려 그렸는데 실물은 **두 장이 어긋나게 겹친 것**이었고, 고쳐 그린 뒤에도 **기운 쪽이 반대**였다 — 형님이 "뒷장은 1시, 앞장은 11시"라고 짚어 주고서야 맞췄다. 구현 결정 119·203-2와 같은 자리다: **그림은 눈으로 대조하기 전에는 단정하지 않는다.**
298-1. **기울기와 벌어짐은 아홉 벌을 그려 놓고 골랐다(3·5·7 × 3·5·7 → 기울기 3 · 벌어짐 3).** 처음 값(둘 다 7)은 두 장이 너무 벌어지고 기울기도 과했다 — 형님이 "절제할 필요"라고 짚었고, 두 값이 서로를 키우므로 **한 축씩 물어보면 답이 안 나온다.** 격자로 늘어놓아야 고를 수 있다(구현 결정 213-2와 같은 손질).
299. **테를 두르지 않고 속을 채운다.** 선으로 그린 표식이 아니라 밝은 카드 두 장이다. 앞장은 양피지색이고 뒷장은 한 톤 어둡다 — **색이 갈리면 어느 것이 앞인지 선 없이도 읽힌다.** 가장자리는 테 대신 그림자가 짓는다.
300. **수는 기울지 않는다.** 앞장을 따라 눕혀 두었는데 형님이 짚었다 — 카드가 기울었다고 수까지 눕히면 읽기가 나빠진다. 마름모 안의 표식을 되세우는 것과 같은 자리다(구현 결정 206).
301. **`rsvg-convert`는 `@font-face`도 fontconfig도 안 탄다.** 미리보기에서 Pirata One이 계속 산세리프로 나와 앱이 틀린 줄 알 뻔했다 — 앱은 `.sl-numeral`로 멀쩡했고 그리는 쪽이 못 읽은 것이다. **글자를 `fontTools`로 패스로 뽑아 얹으면** 렌더러와 무관하게 모양이 정확하다. 구현 결정 210·213-3과 같은 자리다: **미리 본 것은 화면이 아니다.**

302. **화면 글자에서 「세우기」를 걷고 「생성」으로 맞췄다.** 「캐릭터 세우기」를 이미 고쳤는데(구현 결정 180-1) 파티 단추·오류 글·안내 글에 남아 있었다 — **한 낱말이 화면마다 다르면 같은 일인 줄 모른다.** 주석의 소제목까지 함께 고쳤다: 다음에 찾을 때 같은 낱말로 찾게 된다.
303. **맨 위에서는 붙박이 띠가 넉넉히 서고 굴리면 납작해진다(`useAtTop`).** 처음 마주할 때는 이름과 부제가 여유 있게 보이는 편이 낫고, 읽기 시작하면 띠는 자리를 덜 차지할수록 낫다. **위아래를 함께 늘린다** — 아래만 늘리면 글자가 노치 쪽으로 붙어 올라가고 위만 늘리면 밑줄과 붙는다.
304. **문턱을 0이 아니라 4px에 둔다.** 띠가 납작해지면 그만큼 문서가 짧아지는데 0에 붙여 두면 그 자리에서 다시 맨 위가 되어 **커졌다 작아졌다를 되풀이할** 여지가 생긴다. 몇 픽셀 띄우면 그 진동이 닿지 않는다.
305. **스크롤 상자는 문서 자체다.** `.journal`은 제 스크롤을 갖지 않으므로 `window`를 듣는다 — 요소에 걸면 아무 일도 안 일어난다.

306. **팩 원본은 iCloud Drive에 있다: `~/Library/Mobile Documents/com~apple~CloudDocs/Documents/GloomHaven/Creator Pack V2/`.** 두 번이나 "이 기계에 없다"고 적었는데(구현 결정 205·297) `find`가 `~/Library`를 건너뛰고 있었을 뿐이다. **`mdfind`로 한 번에 찾았다** — 맥에서 파일을 찾을 때는 색인을 먼저 묻는다.
307. **전투 목표의 체크표를 팩 것으로 바꿨다(`General Icons.pdf` 23쪽).** 글꼴의 `✓`는 기기마다 획이 다르고 실물 시트의 것과 닮지도 않았다. **물들이지 않고 그대로 얹는다**(구현 결정 15) — 원본이 거의 검정이고 켜진 칸이 금빛이라 그대로 읽힌다.
308. **꺼진 칸에서도 표식은 자리를 지키고 보이지만 않는다.** 켤 때만 그리면 그 칸만 다시 자리를 잡느라 **눌린 자리가 흔들린다.**
309. **파티도 별도 화면에서 만든다(`#/journal/new`).** 일지에 캐릭터는 「생성」 단추가 있고 파티는 입력란이 놓여 있었다 — **같은 일인데 생김새가 달라** 무엇을 눌러야 할지 한 번 생각하게 된다. 형님이 짚었다. 이름 하나뿐이라 화면이 단출하지만 **문을 맞추는 편이 낫다**: 목록은 보는 자리이고 만드는 것은 들어가서 하는 일이다. 주소는 `/character/new`와 같은 짜임이다(구현 결정 281).
310. **이름 규칙을 캐릭터와 파티가 함께 쓴다(`nameRules.ts`).** 파티 이름은 나중에 기록지에서 고칠 수 있지만 **거르는 규칙까지 둘로 둘 까닭이 없다** — 두 벌이면 언젠가 어긋난다. 겹쳤을 때 **무엇과** 겹쳤는지는 부르는 쪽이 낱말로 넘긴다(`nameProblemText(problem, '파티')`).
311. **두 문 가운데 캐릭터가 금빛이고 파티는 한 톤 낮다.** 일지는 캐릭터를 보는 자리이고 파티는 그 캐릭터가 속한 곳이다(2026-08-12 전환) — 둘 다 금빛이면 무엇이 먼저인지 화면이 말하지 않는다.
312. **막다른 안내는 만드는 자리로 곧장 보낸다.** 「일지에서 파티를 생성한다」가 목록으로만 보내고 있었다 — 이제 `#/journal/new`로 간다. 캐릭터 화면과 전투 팝업 둘 다 고쳤다.

### 특혜 글의 낱말을 그림으로 — 2026-08-13

313. **원소·상태이상·굴림은 실물 시트에도 그림으로 인쇄돼 있다.** 글자로만 두면 카드에서 보는 그림과 시트에서 읽는 낱말이 따로 논다 — 「굴림 바람 카드 2장 추가」가 (굴림)(바람) 카드 2장 추가가 된다. 형님이 짚었다.
314. **자리마다 가장 긴 낱말부터 맞춰 보고 맞으면 통째로 삼킨다.** 「이동불가」 안의 「불」을 잡으면 안 되는데, 낱말 경계를 따로 보지 않아도 이것 하나로 갈린다. **DB의 특혜 글에서 「불」이 열 번 걸리는데 그중 다섯이 「이동불가」 속이었다** — 세어 보고 알았다.
315. **어떤 낱말이 실제로 나오는지는 글을 뽑지 않고 낱말만 세어 알았다.** 특혜 글은 저작물성이 가장 또렷한 것이라(구현 결정 135) 통째로 꺼내 볼 것이 아니다. **공백으로 갈라 낱말 빈도만** 뽑으면 어휘는 다 보이고 문장은 안 나온다.
316. **수는 낱말에 붙어 있을 때만, 수를 다는 표식일 때만 딸려 온다.** `밀기2`는 삼키고 `불2`의 2는 남긴다 — 안 그러면 `4장`의 `4`가 앞 낱말에 붙는다.
317. **「대상」만 잡고 「추가」는 남긴다.** 표식의 이름은 「대상 추가」지만 글에서는 뒷말이 갈린다 — **그림이 삼킬 것은 표식이 가리키는 것까지다.**
318. **읽어주는 쪽에는 원문이 통째로 간다.** 조각마다 `aria-hidden`을 걸고 줄 전체에 `aria-label`을 붙인다 — 조각조각 읽히면 「굴림 바람 카드」가 「초록 마름모 파란 원 카드」가 된다(구현 결정 234와 같은 결).
319. **표식을 그리는 일을 `InlineMark` 한 곳으로 모았다.** 덱 알약이 이미 하던 일이라 두 벌이 될 뻔했다 — **같은 표식이 화면 두 곳에서 다르게 그려지면 같은 것으로 안 읽힌다**(구현 결정 238과 같은 손질). `deckview__mark*` 클래스를 `imark*`로 옮겼다.
320. **덱 알약의 「굴림」도 글자에서 그림으로 바꿨다.** 카드와 특혜 글에서는 초록 마름모인 것이 알약에서만 낱말이었다.
321. **글줄에 얹히는 그림은 밑선을 맞춘다.** 알약에서는 조각들이 홀로 늘어서지만 특혜 글에서는 **글자 사이에 낀다** — `vertical-align`이 없으면 그림만 처져 줄이 들쭉날쭉해진다.
322. **값도 그림이다 — `+1`·`−2`·`×0`은 팩의 값 메달로 바꾼다.** 카드에 찍힌 것과 같은 그림이라 글자로 두면 또 따로 논다. **메달이 없는 값(`+3`·`+4`)도 값으로 갈라낸다** — 덱 알약이 그러듯 숫자를 직접 그려 메달 자리를 대신한다. 처음에는 글자로 흘렸는데 형님이 "+3 특혜도 있나?"라고 물어 세어 보니 **DB에 넷 있었다** — 그대로 두었으면 그 줄만 그림이 빠져 어긋나 보였을 것이다. 카드 값이 아닌 것(`+5`·`−3`)은 글자로 남는다.
323. **빼기는 셋으로 적힌다(아스키 하이픈·U+2212·엔대시). 적는 사람 손에 따라 갈리는 것이지 다른 값이 아니다.** 곱하기도 `×`와 `x`를 함께 받는다. **자릿수는 하나로 못박는다** — `+10`을 `+1`로 읽으면 안 된다.
324. **캐릭터 생성 화면이 특혜 목록을 함께 낸다.** 어느 클래스를 고를지는 그 클래스가 판을 어떻게 바꾸는지에 달렸고 그것을 가장 또렷하게 말하는 것이 특혜다 — 봉투를 고르며 뒷면을 읽는 것과 같다. 형님이 짚었다.
325. **거기서는 켤 수 없다.** 상자를 단추로 두면 눌러 보게 되는데 **아직 캐릭터가 없다** — 켜는 것은 시나리오를 치른 뒤 시트에서 하는 일이다. 자리 표시로만 그린다.

### 팩에 없는 값 메달을 구웠다 — 2026-08-13

326. **`+3`·`+4`도 메달을 갖는다(`tools/bake_medallion.py`).** 팩에는 실물 표준 덱의 일곱만 있다. 처음에는 놋쇠 원반에 숫자를 그려 대신했는데 형님이 "별로다, `+1` 메달에서 숫자를 지우고 그 위에 `+3`을 그리면 안 되나"라고 짚었다 — **그 길이 옳았다.** 이제 아홉 값이 모두 같은 원반 위에 서고, **그림 없는 값을 그리던 갈래가 통째로 사라졌다**(`deck__numeral`·`deckview__numeral`·`imark__numeral`).
327. **숫자만 지우면 옛 숫자의 그림자가 남는다.** 잉크에서 8px까지 함께 지워야 하고, 그렇게 뚫린 큰 구멍은 **다른 메달에서 옮겨 와 채운다** — 빨간 `−1`·`−2`는 기호가 작아 가리는 데가 적으므로 초록 원반으로 색 변환(3×3 최소자승)해 쓰면 대부분이 메워진다. 남은 데만 확산으로 메운다. 이렇게 하니 팩 원반과 차이가 **0.4**가 되었다.
328. **자리는 재서 얻었다.** 팩 메달 넷(`+1`·`+2`·`−1`·`−2`)이 한결같았다 — `+`는 58×58 정사각형에 세로 가운데 102.5, 숫자는 높이 124.5에 세로 가운데 99.5(**기호가 숫자보다 3px 아래**), 덩어리 가로 가운데는 **원반 가운데보다 12px 왼쪽**. 처음에 `+`를 58×91로 읽었던 것은 문턱이 헐거워 **원반의 초록 하이라이트까지 잉크로 잡은 것**이었다 — 채도로 걸러야 한다.
329. **획 가운데가 각지게 솟게 하려면 높이를 자르면 안 된다.** `min(거리, 상한)`으로 두면 굵은 획의 꼭대기가 **평평한 지붕**이 되고 가장자리만 둥글게 떨어진다. 자르지 않으면 기울기가 어디서나 같아 옆면이 평평한 면으로 서고, 획 한가운데에서 두 면이 만나며 **부호가 뒤집혀 각이 선다**. 형님이 "칼자루처럼"이라고 짚은 그것이다.
330. **두께감의 손잡이는 넷이다** — 능선 기울기·빛 방향·능선의 밝은 선·능선 무디기. 넷을 격자로 그려 놓고 형님이 골랐다(기울기 0.9 · 왼위 · 밝은 선 강하고 좁게 · 칼날). **한 축씩 물어서는 답이 안 나온다**(구현 결정 298-1과 같은 자리).
331. **굽는 스크립트는 레포의 메달을 원료로 쓴다.** 팩 원본이 아니라 이미 담아 둔 200×200을 읽으므로 **팩이 손에 없어도 다시 구울 수 있다.** 값이 늘면 같은 값으로 한 줄 더 구우면 된다.
332. **팩 원본은 iCloud Drive에 있다** — 구현 결정 306에 적어 둔 자리다. 이번에 `Attack Modifiers/`의 원본 크기가 200이 아니어서 한 번 걸렸다.

### 캐릭터 시트를 읽는 화면으로 — 2026-08-13

333. **읽을 때는 표식 위의 수, 고칠 때는 다이얼.** 들여다보는 동안에는 손잡이가 자리만 차지한다 — 경험·골드를 **한 줄에** 놓고 **경험은 별, 골드는 금화** 위에 수를 얹었다. 편집으로 들어가면 다이얼로 돌아간다: 수를 옮기는 데는 손잡이가 있어야 한다.
334. **표식은 둘 다 이미 앱에서 쓰는 것이다.** 별은 HP/XP 트래커와 같은 팩 파일이고 금화는 골드 카운터와 같은 조각이다 — 여기서 처음 보는 그림이면 같은 값인 줄 모른다(구현 결정 319와 같은 손질).
335. **금화의 그라디언트 열쇠를 인스턴스마다 가른다.** 못박아 두면 한 화면에 금화가 둘 이상 설 때 겹친다 — 시트가 빌려 쓰면서 실제로 그럴 수 있게 되었다(구현 결정 13과 같은 자리).
336. **핸드 사이즈는 클래스 이름 옆에 그림으로 선다.** 「손 10장」이 글자로 적혀 있었는데 생성 화면에서는 이미 그림이었다 — 같은 값이 두 화면에서 다르게 보였다. 조각을 `HandCards`로 떼어 둘이 나눠 쓴다.
337. **띠는 켜짐/꺼짐이 아니라 이어지는 값으로 좁아진다(`useScrolled`).** 문턱 하나로 갈랐더니 그 자리를 지날 때마다 띠가 툭 튀었다 — 형님이 "서서히"라고 짚었다. 굴린 정도(0~1)를 CSS 변수로 내려보내고 크기는 `calc`가 섞는다. **전환 효과는 걸지 않는다** — 이미 스크롤이 값을 잇고 있어 두 번 부드럽게 하면 손가락보다 늦게 따라온다.
338. **표식과 이름이 함께 커진다.** 맨 위에서 원반 56px·이름 1.55rem, 다 굴리면 34px·1.15rem. 따로 움직이면 한 덩어리로 안 읽힌다. **매 프레임에 한 번만 센다**(`requestAnimationFrame`) — 스크롤 이벤트마다 상태를 바꾸면 굴릴 때 버벅인다.
339. **띠 높이를 정하는 것은 가장 큰 아이다 — 그 아이들을 다 줄여야 띠가 준다.** 표식만 `--tb`를 따라 줄였더니 옆에 선 44px짜리 뒤로가기 단추가 바닥을 받쳐 **굴려도 아무 일이 안 일어났다.** 줄 자체에 `min-height`를 주고 뒤로가기까지 함께 줄인다. 78.4px → 50.6px로 든다.
340. **부제 줄에 그림을 넣으면 띠가 통째로 두꺼워진다.** 손패 그림(31px)이 0.78rem짜리 줄에 서면서 줄 상자를 밀어 올렸고, px로 고정이라 굴려도 안 줄었다. **손패는 레벨 칸 머리로 옮겼다** — 클래스 수치라 레벨별 최대 체력과 한 자리이고, 거기서는 높이를 밀 것이 없다.
341. **남은 수가 아니라 온 만큼을 적는다.** 「다음까지 35」는 목표를 모르면 뜻이 없다 — `60/95 (63%)`처럼 지금 값과 눈금이 함께 있어야 얼마나 왔는지 보인다. 형님이 짚었다. 아홉 레벨이면 다음 눈금이 없으므로 아무것도 안 적는다.
342. **골드 밑에는 글자를 안 적는다.** 금화 위에 수가 있으면 그것이 골드인 줄 안다 — 「골드」라고 또 적으면 같은 말을 두 번 한다. 경험만 밑줄을 갖는데 그것은 이름이 아니라 **진행 정도**다.
343. **두 표식의 잉크 크기를 맞췄다.** 재 보니 별은 상자의 80%, 금화는 90%를 채워 별이 작아 보였다 — 90/80 = 1.12배로 키운다. **상자를 같게 두는 것으로는 안 맞는다.**
344. **경험 표식에 두께감을 구워 담았다(`tools/bake_xp_star.py`).** 팩의 별은 검정 실루엣이라 물들이기만 하면 납작하다 — HP/XP 트래커에서는 CSS 그라디언트를 표식 모양으로 오려 입체를 냈지만 시트에서는 그림 하나로 끝나야 한다. 값 메달과 같은 끌 베벨을 쓰되 **가장자리만 깎는다**: 별은 덩어리가 넓어 자르지 않으면 한가운데가 통째로 능선이 되어 하얗게 뜬다. 색은 트래커의 푸른 쪽과 같은 결이다.
345. **평평한 판을 기준으로 밝기를 가른다.** 그보다 빛을 더 받는 면은 밝게, 덜 받는 면은 어둡게 — 그냥 람베르트 값을 색에 곧게 매기면 판이 흰빛으로 떠 버린다.
346. **뾰족한 것은 둥근 것보다 작아 보인다.** 상자를 같게 두어도 별이 작아 보였다 — 재 보니 잉크가 상자의 34%뿐이고 금화는 64%였다(별은 폭이 80%라도 뿔 사이가 비어 있다). 넓이를 맞추면 1.37배인데 그러면 뿔이 튀어 커 보인다. 그 사이인 **1.28배**로 두었다.
347. **경험·골드를 반씩 나눠 갖게 하고 테를 둘렀다.** 흘려 놓으면 값의 자릿수에 따라 자리가 밀려 화면이 흔들린다 — 340이든 8이든 늘 같은 자리에 서야 한다.
348. **손패에 이름표를 달아 제 자리를 줬다.** 그림만 구석에 놓으니 무엇인지도 모르고 곁다리로 읽혔다(형님이 짚었다). 경험·골드 칸과 같은 테를 둘러 "이것도 값이다"라고 말한다.
349. **굽는 연장을 `tools/bakelib.py`로 모았다.** 거리 변환·흐림·부풀리기를 값 메달과 별이 나눠 쓴다 — 같은 일을 하는 것을 두 벌 두면 언젠가 어긋난다.
350. **띠가 다 줄면 표식과 뒤로가기가 같은 40px이다.** 나란히 선 둘의 키가 어긋나면 줄이 삐뚤어 보인다 — 형님이 짚었다.
351. **라운드 트래커가 한 칸이면 `R3`으로 적는다.** 이름표가 빠진 자리에 숫자만 남으면 상 위의 체력·골드 트래커와 갈리지 않는다. 이름표를 낼 자리가 없을 때만 앞에 한 글자를 붙이고, **그만큼 가로를 나눠 준다**(숫자 몫 0.52 → 0.34).
352. **판이 칸을 가득 채우면 제 테를 두르지 않는다.** 라운드 트래커의 판이 위젯 틀과 테를 겹쳐 두르고 있었는데, `clip-path`를 써서 모서리가 각져 **틀이 둥글어진 뒤로 둥근 모서리에서 그 테가 잘려 검은 이가 빠진 것처럼 보였다.** 테를 걷고 손이 닿았을 때만 안쪽 금빛 선을 낸다.
353. **`.deck__numeral`을 걷다가 메달의 자리 잡는 규칙까지 먹었다.** 값 메달·홈·섞기가 함께 쓰던 `position: absolute; transform: translate(-50%,-50%)` 규칙이 통째로 사라져 **메달이 카드 왼쪽 위로 밀렸다.** 구현 결정 252·이번까지 **세 번째다** — 정규식으로 규칙을 걷을 때는 선택자 목록을 함께 쓰는 이웃이 있는지 먼저 본다.
354. **HP/XP 트래커의 표식도 구운 그림으로 바꿨다.** CSS 그라디언트로 만든 도톰함은 **둥근 돔**이라 시트의 별(끌 베벨)과 나란히 놓으면 다른 물건으로 보였다. 이제 둘이 같은 그림을 쓴다 — 오릴 것도 물들일 것도 없다.
355. **물방울은 별보다 좁게 깎는다.** 꼬리가 가늘어 같은 띠로 깎으면 **평평한 판이 남지 않고** 꼬리 전체가 비탈이 되어 접힌 종이처럼 보인다.
356. **나눠 쓰는 조각은 제 스타일을 스스로 들여온다(`HandCards.css`).** 손패 그림의 규칙이 생성 화면의 스타일시트에만 있어, **시트에서는 그림 크기도 수의 자리도 없어 수가 아예 안 보였다.** 구현 결정 189와 같은 자리다 — 쓰는 쪽이 스타일을 따로 챙겨야 하면 언젠가 빠뜨린다.
357. **칩에서 이름표를 걷었다.** 카드 두 장에 수가 얹힌 그림이면 그것으로 족하다 — 「손에 드는 카드」라고 또 적으면 같은 말을 두 번 한다. 읽어주는 쪽에는 `HandCards`가 이미 우리말로 전한다.
358. **위젯 속(회전자)은 테 안쪽 크기여야 한다.** 틀이 `box-sizing: border-box`라 칸 크기에는 테가 포함돼 있는데 그것을 그대로 속에 주고 있었다 — 속이 사방 1px씩 **테 밑으로 삐져나가** 둥근 모서리가 틀의 것과 어긋났고, 칸을 가득 채우는 위젯(라운드 트래커)에서 **모서리에 검은 이가 빠진 것처럼 보였다.** 판의 테를 걷은 것만으로는 안 고쳐졌던 진짜 까닭이다. 테 두께는 `FRAME_BORDER`로 이름 붙여 CSS와 맞춰 둔다.
359. **앞장에만 그림자를 드리운다.** 그림 전체에 걸면 두 장을 한 덩어리로 보고 **바깥 윤곽에만** 드리워 사이가 안 갈린다 — 두 장이 거의 붙어 보였다(형님이 짚었다). 앞장에만 걸어야 뒷장 위로 그늘이 진다. 짙기는 실제 크기(36px)에서 후보 넷을 견줘 골랐다 — 옅으면 그 크기에서 묻힌다.
360. **바깥 그림자는 옅게 물린다.** 앞장이 제 그림자를 갖게 되었으므로 두 겹이 겹치면 뭉갠다 — 바깥 것은 바탕에서 떼어 놓는 몫만 한다.

### 왕관·연필·캐럿, 그리고 고친 자리 — 2026-08-13

361. **레벨 왕관은 아이콘 묶음에 없다 — 능력 카드 앞면 위쪽에 박혀 있다.** `Icon Pack/` 여섯 파일을 다 뒤지고서야 알았다(구현 결정 222대로 먼저 뒤졌다). 그 자리(37×23px)만 오려 쓴다 — **다시 그리지 않는다.** 값 메달의 홈을 오려낸 것과 같은 손질이다(`tools/extract_crown.py`).
362. **밝기를 알파로 옮긴다.** 이진으로 자르면 37px짜리에서 계단이 진다 — 150~226을 0~1로 펴서 가장자리의 반투명을 살린 뒤 네 배로 늘린다. **카드 테두리의 흰 선이 같은 밝기라 함께 걸리므로** 몸통 한가운데에서 번져 이어진 것만 취한다.
363. **고치기는 글자가 아니라 연필이다.** 띠에 홀로 서는 단추라 이름이 없어도 되고, 연필은 어디서나 '고친다'로 읽힌다. 읽어주는 쪽에는 `aria-label`이 우리말로 간다.
364. **읽을 때와 고칠 때가 같은 모양이다.** 편집으로 들어가면 표식 양옆에 캐럿이 돋을 뿐이다 — 다이얼로 갈아 끼우면 눈이 자리를 다시 찾아야 한다(형님이 짚었다). **표식은 가운데 자리를 지킨다**: 캐럿이 생겼다고 그림이 밀리면 읽던 자리가 흔들린다. 캐럿은 안쪽이 한 칸, 바깥쪽이 여러 칸이다 — 골드를 8에서 340으로 옮기는 일이 있다.
365. **고친 자리를 색으로 짚어 준다.** 오른 것은 녹색, 내린 것은 붉은색. 켠 칸은 녹색으로 또렷하게, 끈 칸은 붉은빛으로 옅게 — 지운 자리는 흔적만 남는다. **저장하면 초안이 곧 레코드가 되어 견줄 것이 없어지므로 저절로 물러난다** — 색을 지우는 코드가 따로 없다.
366. **판단은 순수 함수가 하고 화면은 클래스로 옮기기만 한다(`moveOf`·`toggleOf`).** 그래야 시험이 붙는다.
367. **정규식으로 CSS 규칙을 걷을 때는 선택자 목록과 앞 주석을 함께 본다.** 이번에 `Dial`을 걷다가 바로 위의 `Tally`까지 먹었다 — **네 번째다**(구현 결정 252·353). 앞의 `/**`를 찾아 올라가는 손질이 매번 이웃을 데려온다.

### 고친 기록을 남긴다 — 2026-08-13

368. **정산이 맞았는지는 나중에야 묻는다(`0018_character_log.sql`).** 시나리오가 끝나면 골드·경험·체크마크가 한꺼번에 움직이고 다음 판에서 "이거 지난번에 올린 거 맞나"를 묻게 되는데, 캐릭터 필드는 **현재값만** 들고 있어(구현 결정 1) 물어볼 데가 없었다.
369. **이력이지 정본이 아니다.** 이 표를 폴드해 현재값을 얻지 않는다 — 구현 결정 1이 이벤트 소싱을 물린 까닭이 그대로 살아 있다. 그래서 **고치거나 지우는 정책을 아예 두지 않는다**(안 만들면 RLS가 통째로 막는다). 캐릭터가 사라지면 함께 사라진다.
370. **글이 아니라 값으로 담는다.** 서버에는 `{field, from, to}`만 들어가고 우리말로 옮기는 것은 화면이 한다(`describeChange`) — 글로 담으면 나중에 문구를 고칠 때 **옛 기록만 옛말로 남는다.**
371. ~~**캐릭터를 볼 수 있으면 기록도 볼 수 있다.**~~ **같은 날 좁혔다 — 형님이 「다른 캐릭터의 기록은 볼 수 없어야 한다」고 정했다(`0019`).** 시트의 현재값은 여전히 파티원이 다 본다(SPEC 6장) — **갈리는 것은 이력뿐이다.** 남기는 것도 보는 것도 주인뿐이다.
372. **기록은 값이 들어간 다음에 남기고 실패해도 삼킨다.** 이것 때문에 저장이 되돌아가면 안 된다 — 값은 이미 `characters`에 들어갔다.
373. **레벨은 기록에 안 담는다.** 경험치에서 나오는 값이라 경험 줄을 보면 알 수 있고, 두 줄로 적으면 한 번 고친 것이 두 번으로 읽힌다.
374. **목록은 들고 난 것만 적는다.** 특혜·아이템을 통째로 늘어놓으면 무엇이 달라졌는지 되레 안 보인다.
375. **시각은 사람의 말로 적는다(`whenText`).** `2026-08-13T13:32:42Z`는 기계의 말이다 — 「방금」·「7분 전」·「오늘 오전 9시 05분」·「어제 오후 8시 00분」·「7월 2일 오후 2시 30분」, 해가 바뀌면 연도까지. **알고 싶은 것은 "얼마나 지났나"이지 몇 초인지가 아니다.** 24시간제도 안 쓴다.
376. **지금 시각은 팝업을 열 때 한 번만 잰다.** 렌더 중에 `Date.now()`를 부르면 같은 입력에 다른 결과가 나와 렌더를 되돌릴 수 없다(구현 결정 12와 같은 자리). 떠 있는 동안 「3분 전」이 멎어 있어도 되는 자리다.
377. **고치는 동안에는 「기존값+증감」을 적는다.** `60+35/95` — 정산은 여러 칸을 함께 만지므로 몇을 올렸는지 잊는데 현재값만으로는 알 수 없다. 증감 부분만 색이 붙는다.
378. **라운드 트래커의 글자를 두 번 키웠다.** 한 칸짜리에서 가로 몫을 인색하게 잡으면(0.30) 글자가 점처럼 남는다 — 0.42로 올리고 세로도 0.82→0.90, 판의 여백도 4%→2%로 줄였다.
379. **무엇이 바뀌었는가만으로는 부족하다 — 왜 바뀌었는지가 갈린다(`0019`).** 「골드 120 → 160」이 전투에서 노획한 것인지 손으로 맞춘 것인지 나중에 알 수 없다. **세 갈래뿐이다**: 시나리오 정산 · 직접 수정 · 기타. 더 잘게 쪼개면 고를 때마다 생각해야 하고, 생각하기 싫으면 아무거나 고르게 된다.
380. **까닭은 사람이 고르는 것이 아니라 경로가 정한다.** 저장 곁에 알약 셋을 두었다가 같은 날 걷었다 — **시트에서 편집을 눌러 고친 것은 언제나 「직접 수정」이다.** 시나리오 정산으로 들어오는 길은 따로 난다(형님이 짚었다). 고르게 두면 잘못 고를 수 있고, 무엇보다 **고를 일이 아닌 것을 묻는 셈**이다.
381. **낱말은 흔히 쓰는 것으로 둔다.** 우리끼리만 아는 말을 쓰면 반년 뒤에 제가 못 읽는다.
382. **고치는 동안에도 몇 %인지 남긴다.** 「60+35/95」만 적었더니 목표까지 얼마나 왔는지가 사라졌다(형님이 짚었다). 비율은 **고친 뒤의 값**으로 센다 — 지금 이 손질이 어디까지 데려다 놓았는지가 궁금한 것이다.
383. **목표와 비율도 증감과 같은 색을 입는다.** 「60+35/95 (100%)」에서 증감만 색이 붙었더니 **얼마나 왔는지가 색 없이 남아** 한 덩어리로 안 읽혔다. 형님이 짚었다.
384. **라운드 트래커의 글자를 오른쪽으로 조금 민다.** 왼쪽 위가 사선으로 잘려 있어 덩어리가 상자 가운데에 놓이면 **왼쪽으로 치우쳐 보인다** — 잘린 만큼 되돌려 준다.
385. **팝업이 덮었다고 뒤쪽이 멈추는 것이 아니다(`useScrollLock`).** 화면을 통째로 덮어도 손가락은 문서를 굴린다 — 팝업 안에서 굴리다 끝에 닿으면 뒤쪽 시트가 따라 움직였다(형님이 짚었다). `overscroll-behavior: contain`은 그 넘김만 막을 뿐, **팝업 알맹이가 짧으면 애초에 굴릴 것이 없어 통째로 뒤로 넘어간다.**
386. **`overflow: hidden`만으로는 iOS에서 안 멈춘다.** 몸통을 `position: fixed`로 못박고 **굴러 있던 만큼 위로 끌어올린다** — 그러지 않으면 여는 순간 뒤쪽이 맨 위로 튀어 오르고 닫을 때 제자리로 안 돌아간다. 되돌릴 때는 **인라인 스타일을 지우는 것이 아니라 있던 값을 돌려놓는다**: 다른 곳에서 걸어 둔 것이 있으면 그것까지 지운다.
387. **덱 펼쳐 보기에도 같은 병이 있었다.** 한 곳을 고치면서 함께 붙였다 — 같은 짜임의 팝업이 둘이면 한쪽만 고치는 일이 반드시 생긴다.

### 레벨 왕관을 얼개로 그렸다 — 2026-08-14

388. **윤곽을 따는 길은 막혔다.** 오려낸 37×23px은 40px만 넘겨도 흐리고, 크게 늘리면 확대와 JPEG가 만든 물결이 그대로 벡터가 된다 — **실제 도안의 바깥은 매끈하다.** 자를 굵게 잡아 펴 봐도 곧은 변과 굽은 변이 함께 뭉개졌다. **자리만 재고 얼개로 다시 그린다.**
389. **얼개는 형님이 짚어 주었다.** 아래는 왕관이 둥근 통이라 **아래로 볼록한 원호**, 옆면은 위로 갈수록 바깥으로 더 휘는 곡선, 위는 **아래로 오목한 곡선 넷**(바깥 둘은 짧고 가운데 둘은 길다). 이 셋을 알고 나니 값 몇 개로 끝났다 — 457바이트다.
390. **옆면은 S자가 아니다.** 삼차 곡선에 손잡이를 위아래로 나눠 빼면 S가 난다(형님이 짚었다). 이차 곡선 하나에 손잡이를 **아래 모서리 바로 위**에 세우면 아래는 세로로 서고 위에서만 휘어 기울기가 한쪽으로만 돈다.
391. **골 자리를 눈에 속아 두 번 잘못 잡았다.** 어깨 안쪽의 작은 눈이 닫히는 자리를 골 바닥으로 읽었다. **줄마다 어깨 안쪽 가장자리와 몸통 가장자리를 따로 재어 만나는 자리를 외삽**해야 한다 — 눈이 끼어드는 줄(40~43)은 빼고 32~39행만 쓴다.
392. **곡률은 후보를 겹쳐 놓고 골랐다.** 원본을 회색으로 깔고 그 위에 선을 얹으면 어디가 어긋났는지 바로 보인다 — 채워 놓고 견주는 것보다 훨씬 빠르다.
393. **밑감은 레포에 담지 않는다.** 오려낸 비트맵은 `/tmp`에 두고 **레포에는 SVG만** 담는다 — 같은 것이 두 벌 있으면 어느 쪽이 정본인지 흐려진다.
394. **어깨 홈은 눈이 아니라 삼각형이다.** 화소가 뭉개져 씨앗처럼 보였을 뿐이다(형님이 짚었다). 어깨는 세 선이 두르는 삼각꼴이므로 — 짧은 곡선·옆면·**가운데 곡선을 골 너머로 늘인 선** — 그 셋을 안쪽으로 **같은 폭만큼** 밀어 만나게 하면 홈이 나온다. 가운데 갈매기 홈과 테 사이의 폭을 그대로 쓴다.
395. **밑변은 가운데 곡선의 연장선 **위에** 앉는다 — 밀지 않는다.** 세 변을 다 물렸다가 형님이 다시 짚었다. 밑변까지 밀면 홈이 위로 들려 어깨가 두꺼워지고, 무엇보다 **가운데 곡선과 한 결로 안 읽힌다.** 물리는 것은 짧은 곡선과 옆면 두 변뿐이다.
396. **홈 자리를 따로 적어 두지 않는다.** 골과 곡선에서 셈해 내므로 곡률을 손보면 홈이 저절로 따라간다 — 값을 적어 두면 곡선을 고칠 때마다 어긋난다.
397. **레벨은 왕관 안의 수로 적는다(`LevelBadge`).** 목록의 줄 끝에 수가 하나 서 있으면 골드인지 경험인지 레벨인지 알 길이 없다 — 실물에서도 레벨은 왕관으로 적힌다.
398. **왕관은 덮개로 깔고 색만 화면에서 정한다.** Creator Pack 에셋이라 `.tsx`에 인라인 SVG로 박지 않는다(절대 원칙 1-1) — 파일은 `public/`에 두고 `mask`로 오린다. `<img>`로 얹으면 `currentColor`가 안 먹는다(SVG는 제 바깥의 색을 모른다). HP/XP 트래커가 표식을 다루는 것과 같은 손질이다.
399. **크기는 부모가 글자 크기 하나로 정한다.** 왕관 폭도 수도 `em`으로 매어 두므로 함께 큰다 — 두 값을 따로 주면 어느 한쪽만 고쳤을 때 수가 왕관 밖으로 나간다.
400. **수는 몸통 한가운데(71%)가 아니라 조금 위(65%)에 앉힌다.** 아래가 원호로 파여 있어 한가운데에 두면 처져 보인다 — 71 → 67 → 65로 올려 가며 형님이 골랐다.
401. **레벨 왕관을 눈금표 머리에도 둔다 — 손패 왼쪽이다.** 표 바로 위이므로 **지금 몇 레벨인지가 눈금 옆에 늘 서 있게** 되고, 고치는 동안에도 경험치를 따라 바뀐다. 손패와 같은 테 안에 담아 한 벌로 읽히게 했다.
402. **무리 목록에서 시트를 펼치지도 고치지도 않는다.** 줄을 누르면 그 자리에서 시트가 펴지고 제 것이면 고칠 수도 있었는데 형님이 걷었다 — 캐릭터에는 **제 주소가 있고**(`#/character/<id>`) 시트는 거기 한 장이면 된다. 같은 시트를 두 자리에서 열면 어느 쪽에서 고쳤는지 흐려지고, 무엇보다 **파티 기록지가 캐릭터 편집기까지 겸하게 된다.**
403. **줄은 단추에서 문으로 바뀌었다.** 여기 남는 몫은 「누가 몇 레벨인지」 한눈에 보는 것뿐이다 — 경험·골드 수치도 걷고 레벨 왕관만 남겼다. **무엇까지 보일지는 다시 이야기하기로 했다.**
404. **나란히 선 칩은 키가 같아야 한다.** 레벨 왕관과 손패가 어긋나면 줄이 삐뚤어 보인다 — 손패 그림이 43px이므로 왕관도 43px로 맞춘다. 왕관은 폭이 `3.1em`이고 비가 152:104라 높이가 `2.12em`이니 **글자 크기를 43/2.12 = 20.3px**로 둔다. 셈을 주석에 남겨야 나중에 크기를 고칠 때 따라간다.
405. **연필을 따로 떼어 두 시트가 나눠 쓴다(`PencilIcon`).** 같은 일을 하는 단추를 두 벌 그리면 반드시 어긋난다. **직접 그린 도형이라 `.tsx`에 인라인으로 두어도 된다** — 격리 규칙 1-1은 팩 에셋에 대한 것이다.
406. **글자가 그림이 되어도 읽어주는 이름은 남는다.** 시험이 「고치기」를 그대로 보므로 `aria-label`을 빠뜨리면 그 자리에서 걸린다.
407. **캐릭터를 만든 것도 기록에 남긴다(`0020`).** 이것이 없으면 기록이 중간부터 시작해 **맨 아래 줄이 첫 정산인 것처럼 읽힌다.** `reason`은 「어떤 경로로 이렇게 되었나」를 말하는 칸인데 생성은 고친 것이 아니라 셋 중 어디에도 안 들어맞아 갈래를 하나 더 뒀다(`created` → 「생성」).
408. **생성 줄에 이름을 적지 않는다.** 기록은 캐릭터 한 장의 것이라 누구인지는 이미 알고, 무엇보다 **우리말 조사(을/를)가 받침에 따라 갈려** 이름을 넣으면 어색해진다.
409. **특혜 상자는 세 칸 폭을 늘 잡아 둔다.** 줄마다 상자가 하나에서 셋까지라 실제 개수만큼만 자리를 주면 **글의 시작이 줄마다 어긋난다.** 캐릭터 시트는 그렇게 하고 있었는데 생성 화면만 안 하고 있었다 — 같은 목록을 두 자리에 그리면 한쪽만 손질이 빠진다(형님이 짚었다).
410. **생성 화면에서 레벨표가 특혜보다 위에 선다.** 클래스를 고를 때 먼저 묻는 것은 "얼마나 단단한가"이고 특혜는 그다음에 읽는 긴 글이다 — **긴 것이 앞에 서면 짧고 굵은 것이 화면 밖으로 밀린다**(형님이 짚었다).
411. **끝에 닿은 몸짓이 문서로 넘어가면 화면 전체가 밀린다.** 캐러셀을 마지막 칸까지 넘긴 뒤 한 번 더 밀면 그랬다(형님이 짚었다) — `overscroll-behavior-x`를 굴리는 상자와 몸통 두 곳에서 막는다. 안쪽만 막으면 다른 가로 상자에서 또 나고, 몸통만 막으면 브라우저에 따라 새어 나간다.
412. **캐러셀 화면에서만 손가락 둘로 화면을 줄이지 못하게 한다.** 카드를 넘기느라 손가락이 둘 다 얹히는 자리라 넘기려다 확대·축소가 걸리는 일이 잦다 — `touch-action: pan-x pan-y`로 미는 것만 남긴다. **앱 전체에는 걸지 않는다**: 다른 화면에서는 키워 보는 것이 도움이 된다.

### 어두운 빈 화면을 없앴다 — 2026-08-14

258. **조각을 못 불러오면 트리가 통째로 걷힌다(`RouteBoundary`).** 형님이 「파티 생성을 누르면 그냥 어두운 화면만 나온다」고 짚었다. 화면은 전부 `lazy`로 갈라 두었는데 그 예외를 받는 곳이 없어 React가 뿌리째 지웠다 — 남는 것은 바탕색뿐이라 **무엇이 잘못됐는지 화면에 한 글자도 안 남는다.** 어두운 빈 화면은 고장이 아니라 고장을 숨긴 것이다.
259. **까닭은 대개 낡은 껍데기다.** 조각 이름에 내용 해시가 붙어 배포할 때마다 바뀌는데, 홈화면 아이콘으로 앱을 켜 둔 채 배포가 몇 번 나가면 손에 든 `index.html`이 이미 없어진 이름을 부른다. **그 화면을 그때 처음 여는 사람만** 그렇게 된다 — 이미 불러온 화면은 멀쩡하므로 앱이 통째로 고장 난 것처럼 보이지 않아 되레 찾기 어렵다.
260. **낡아 보이면 한 번만 새로 불러온다(`staleChunk.ts`).** 껍데기가 새것이 되면 그대로 풀린다. **무한 되불러오기가 가장 나쁘므로** 마지막 되불러오기가 20초 안이면 그만두고 화면에 적는다 — 조각이 진짜 없어진 것이라면 새로 불러도 같은 자리에서 터진다. 판단은 순수 함수로 떼어 표로 못박았다(구현 결정 158과 같은 손질).
261. **무엇이 터졌는지 그대로 적는다.** 형님이 읽어 옮겨 줄 수 있어야 다음 것을 잡는다. 브라우저마다 말이 달라(크롬·사파리·파이어폭스) 셋을 다 본다.
262. **오류 경계는 클래스여야 한다.** React가 함수 컴포넌트에는 이 갈고리를 주지 않는다 — 「함수형 컴포넌트만」에서 이 파일 하나만 벗어난다.
263. **조각을 받아 오는 사이에도 빈 화면이다.** 곧바로 무엇을 띄우면 깜빡임만 남으므로 **0.5초 뒤에 서서히 떠오르게** 두었다(`.pageload`). 느린 자리에서만 보인다.

### 생성 화면의 이름 칸이 단추와 함께 온다 — 2026-08-14

264. **짓는 것과 누르는 것은 한 동작이다(`.newchar__foot`).** 클래스 카드가 길어 이름을 적고 나면 단추가 화면 밖으로 밀리고, 단추를 보러 내려가면 무슨 이름을 적었는지 안 보였다. 띠에 함께 담아 같이 따라다닌다 — 파티 생성도 같은 틀이다.
265. **띠 안의 띠는 붙들지 않는다.** `.sheet__bar`가 이미 `sticky`인데 그대로 두면 두 겹이 서로 다른 자리에서 멎어 어긋난다. 바깥이 붙들고 안은 놓는다.
266. **「그만두기」를 「취소」로 바꿨다.** 앱 다섯 자리에 흩어져 있었다(비밀번호 바꾸기·위젯 설정·파티 기록지·생성 둘). 캐릭터 시트만 먼저 「취소」로 바꿔 두어 **같은 일에 두 가지 말이 돌고 있었다.**
267. **HP/XP 알약도 위젯 테와 한 겹 띄운다(`.hpxp__pad`).** 원소 트래커와 같은 값(0.35rem)이다 — 위젯마다 다르면 나란히 놓았을 때 한쪽만 헐거워 보인다. 재는 자리는 안쪽 알약이라 배치는 좁아진 만큼 알아서 따라온다.

### 셋째 문 — 참고 (2026-08-14)

268. **적지도 굴리지도 않고 그냥 들여다보는 자리를 따로 둔다(`#/reference`).** 두 축(SPEC 1장) 어디에도 안 들어간다 — 일지는 적는 곳이고 행낭은 굴리는 곳인데, 표식이 무엇인지 눈금이 어디부터인지는 **찾아보는 것**이다. 지금까지는 그런 것이 화면 여기저기 곁다리로 붙어 있어 볼 때마다 어딘가로 들어가야 했다.
269. **게임 원문은 여기도 안 온다(절대 원칙 1).** 늘어서는 것은 우리가 그린 표식과 우리가 정한 이름뿐이다. 규칙 문장·카드 원문·시나리오 서사는 오지 않으며, 그 선을 화면에도 적어 둔다.
270. **표식 목록은 `MARKS` 하나에서 나온다.** 참고 화면이 제 표를 따로 들면 카드에 그려지는 것과 어긋난다 — 어긋난 참고는 없느니만 못하다.
271. ~~**셋이 되면서 웰컴의 두 칸 격자에 구멍이 남는다.**~~ **같은 날 넷이 되면서 걷었다** — 둘씩 두 줄이 딱 맞는다. 눕히는 손질은 지웠다(묘비는 세우지 않는다, 구현 결정 229). 아주 넓은 자리(72rem)에서만 한 줄로 편다.

### 다섯 자리를 더 손봤다 — 2026-08-14

272. **물방울은 별보다 밝아야 한다.** 구운 색(mid `#e08a7a`)이 붉은 반쪽(`#b0241d`~`#7c1512`)과 같은 계열이라 형님이 「물방울이 안 보인다」고 짚었다. **별이 푸른 반쪽에서 갖는 만큼의 밝기 차**를 물방울에도 준다(mid `#f7cdc4`). 후보 넷을 실제 바탕 위에 나란히 구워 놓고 골랐다(구현 결정 213-2와 같은 손질).
273. **표식은 조금 올리고 숫자는 조금 내린다.** 정확히 겹치면 숫자가 표식 한가운데를 막아 무엇이 그려져 있는지 덜 읽힌다 — 갈라 놓으면 표식은 표식대로 보이고 숫자는 밑을 받치는 자리에 앉는다. 물방울은 위가 뾰족하고 아래가 불룩하므로 숫자를 더 내린다(16% : 8%).
274. **나란히 선 두 단추의 키를 글자에 맡기지 않는다.** 하나는 `<button>`이고 하나는 `<a>`라 **링크 쪽이 본문 줄높이(1.6)를 물려받아 더 높게 섰다**(형님이 짚었다). 줄높이를 못박고 테 두께까지 맞춰야 둘이 같아진다 — 같은 여백을 준 것만으로는 안 된다.
275. **점 표기도 띠에 함께 담는다.** 카드 밑에 두었더니 내려가면 가려져, 몇 번째를 보고 있는지가 **정작 이름을 지을 때 사라졌다.** 띠에 든 것은 늘 보인다.
276. **띠 위에서는 옆으로 밀리지 않는다.** 카드를 넘기려다 손가락이 위·아래 띠에 얹히면 그 몸짓이 문서로 넘어가 화면 전체가 옆으로 흘렀다(형님이 짚었다). 띠에 `touch-action: pan-y`를 주어 세로로 미는 것만 남기고, 화면 자체는 `overflow-x: clip`으로 잘라 **흐를 길을 없앤다** — `hidden`은 스크롤 상자를 만들어 붙박이 띠가 죽는다.
277. **「참고」가 아니라 「참조」다.** 형님이 정했다.
278. **참조의 섹션은 접힌다(`<details>`).** 찾아보는 자리라 늘어날 일만 남았고, 다 펴 두면 원하는 것에 닿기까지 남의 것을 한참 지나야 한다. **여닫는 것은 브라우저가 한다** — 직접 만들면 키보드·읽어주기까지 흉내 내야 한다. 맨 위 하나만 펴 둔다(무엇이 든 자리인지 보여 주는 몫).

### 한 번 미끄러진 것과 진짜 없는 것 — 2026-08-14

279. **조각을 한 번 못 받았다고 포기하지 않는다(`lazyPage.ts`).** 형님이 참조 화면에서 「어두운 화면이 뜨는데 재시도하다 보면 다시 잘 나온다」고 짚었다 — **다시 잘 나온다는 것이 곧 한 번 미끄러졌다는 뜻이다.** 조각 하나를 여는 데 파일이 여럿 날아오고(제 것·함께 쓰는 것·스타일) 그중 하나만 못 받아도 `import()`가 통째로 엎어진다. 배포된 파일을 직접 찔러 보니 전부 200이었다(구현 결정 82와 같은 확인).
280. **쉬는 사이를 늘려 가며 두 번 더 해 본다(300·900ms).** 한꺼번에 세 번 던지면 신호가 끊긴 자리에서 셋 다 같은 이유로 엎어진다. 끝까지 못 받으면 마지막 오류를 그대로 올려 `RouteBoundary`가 받는다 — **다시 해 보는 것과 포기하는 것을 한 곳에서 정한다.**
281. **기다리는 것을 시험에 넘긴다.** 진짜 쉬면 시험이 그만큼 느려지고, **느린 시험은 결국 안 돌린다.**
282. **받아 오는 사이에 「불러오는 중…」이라 적는다.** 점 하나를 아래에 띄워 두었는데 형님 눈에는 여전히 어두운 화면이었다 — **빈 화면은 무조건 고장으로 읽힌다.** 0.5초 뒤에 떠올라 느린 자리에서만 보인다.

### 시나리오 레벨 표 — 참조에 넣되 셈이 쓰는 값으로 (2026-08-14)

283. **보여 주는 표와 셈하는 값을 두 벌 두지 않는다(`rules/scenarioLevel.ts`).** 형님이 「단순히 사람이 참조하는 데이터가 아니라 게임 로직에서 참조되는 값」이라고 짚었다 — 참조 화면이 이 표를 그리고, 나중에 전투를 펴거나 시나리오를 정산할 때도 **같은 데서 값을 꺼낸다.** 화면 파일에는 수가 하나도 없다.
284. **수치의 나열이라 레포에 둔다.** 클래스 이름·특혜 글은 DB로 보냈지만(구현 결정 111·135) 이것은 인쇄된 글이 아니라 표다 — 레벨 눈금·보정 덱 구성과 같은 성격이다(구현 결정 42). **규칙 문장은 옮기지 않는다**: 뜻은 화면이 우리 말로 한 줄 적을 뿐이다.
285. **식이 있는 것은 셈하고 없는 것만 적어 둔다.** 규칙서가 함정 피해를 `2+L`, 보너스 경험을 「4 + 2×레벨」로 적어 두었으므로 그 둘은 셈해서 낸다 — 값을 또 적으면 표와 어긋날 자리가 하나 는다(구현 결정 65와 같은 결). **금화만 식이 없다**(2·2·3·3·4·4·5·6 — 마지막이 두 칸 뛴다).
286. **인쇄된 여덟 줄을 시험이 붙들고 있다.** 식이 틀리면 화면의 표가 통째로 거짓이 되는데 **그것은 눈으로 안 보인다.** 규칙서에서 읽은 값을 적어 두고 식을 지나온 값과 맞댄다 — 상수끼리 견주는 것이 아니므로 구현 결정 227에 걸리지 않는다.
287. **두 규칙서를 다 펼쳐 대조하고서야 표를 하나로 뒀다.** 글룸헤이븐 15쪽과 사자의 턱 29쪽이 칸 하나까지 같다. **남이 옮겨 적은 것을 믿지 않고 원본 그림을 봤다** — 형님이 「일치하는가」라고 물어 확인한 자리다(구현 결정 119와 같은 손질).
288. **위험 지형은 표에 없다.** 규칙서 본문이 「함정 피해의 절반, 내림」이라 적는다. 사자의 턱은 규칙 용어집을 아직 못 봐서 **같은 셈인지 확인하지 못했다고 적어 둔다** — 모르는 것은 모른다고 한다(구현 결정 115).
289. **✕ 매듭이 「규칙서의 표」와 「웹 표」를 가른다.** 양피지·갈색 잉크·두 겹 줄까지 맞춰도 그것이 없으면 안 닮는다. 칸 경계에 얹되 **밑의 줄을 종이색으로 한 번 지우고** 그 위에 그린다 — 안 그러면 매듭 가운데로 줄이 지나간다. 문양은 우리가 그린 도형이라 인라인으로 두어도 된다(구현 결정 405와 같은 자리).
290. **머리글에 Pirata One을 붙이지 않는다.** 한글이 섞이면 라틴 전용 subset이라 한 줄에 두 서체가 선다(구현 결정 39) — 이름은 명조로, 수만 `.sl-numeral`로 간다. **한글은 낱말 안에서 끊지 않는다**(`word-break: keep-all`): 「시나리 / 오 / 레벨」이 되면 못 읽는다.
291. **헤드리스 크롬으로 그려 보고 고쳤다.** 이 기계에 크롬이 있다 — 표처럼 자리가 촘촘한 것은 코드를 읽어서는 세 줄로 접히는지 알 수 없다(구현 결정 10과 같은 결).

### 가려진 값을 띠가 대신 말한다 — 2026-08-14

292. **긴 시트라 조금만 내려도 레벨과 값이 화면 밖으로 나간다.** 그것을 보려고 도로 올라갔다 내려오게 하지 않는다(형님이 짚었다) — 가려진 칸의 수를 띠에 작은 배지로 세운다: 레벨·손패·경험·골드.
293. **가려진 것만 뜬다.** 두 칸(레벨 눈금 · 값 줄)을 따로 재어 각자 가려질 때 각자 나온다 — 한꺼번에 띄우면 아직 보이는 값을 띠가 또 적는다. `IntersectionObserver` 대신 자리를 직접 재는 것은 **띠 높이가 굴린 정도에 따라 변하기** 때문이다(`useScrolled`).
294. **그림 안의 수를 그냥 줄이지 않는다.** 왕관 안의 수는 이 크기에서 안 읽힌다 — 그림만 내고(`bare`) 수는 옆에 세운다. 형님이 말한 「(기호) (수치)」 꼴이며 넷이 같은 모양으로 선다.
295. **자리는 페이지가 내주고 그리는 것은 시트가 한다(`chipSlot` + `createPortal`).** 고치는 동안의 값은 **초안이 들고 있고 그것은 시트 안에 있다** — 페이지가 저장된 값으로 그리면 골드를 120에서 340으로 옮겨 놓고 내려갔을 때 띠만 120이라 말한다.
296. ~~**배지가 서면 부제는 물러난다.**~~ **같은 날 뒤집혔다 — 형님이 둘 다 두라고 했다.** 부제 아랫줄에 한 줄을 더 낸다: 누구의 무엇인지는 계속 보이는 편이 낫다.
297. **별은 금화보다 크게 준다(1.28배).** 뾰족한 것은 둥근 것보다 작아 보인다 — 시트가 쓰는 비를 그대로 쓴다(구현 결정 346).
298. **띠 높이를 짐작하지 않고 잰다.** 56px이라 적어 두었는데 아이폰은 안전영역이 얹혀 띠가 100px 가까이 된다 — 값이 **한참 가려진 뒤에야** 배지가 떴다(형님이 「흡수되는 시점이 조금 늦다」고 짚었다). 띠를 직접 재면 기기마다 알아서 맞고, 굴린 정도에 따라 띠가 줄어드는 것까지 따라간다.
299. **조금 일찍 튼다(44px).** 완전히 사라진 다음에 뜨면 그 사이가 비어 「없어졌다 나타났다」로 읽힌다 — 칸의 끝자락이 띠에 물리기 시작할 때 이미 서 있어야 한다. **배지가 서면 띠가 그만큼 두꺼워져 기준선이 내려가는데**, 그것이 곧 되돌아가는 문턱을 높여 주어 경계에서 깜빡이지 않는다.
300. **줄이 여닫히는 것이 보여야 한다(0.18초).** 툭 나타나면 띠가 튀는 것으로 읽힌다 — 키·여백·짙기를 함께 굴린다. 배지 자체는 늘 그려 두고 **켜짐만 바꾸므로** 닫힐 때도 부드럽다. 뒤늦게 합류하는 배지(값 줄이 나중에 가려진다)는 제 몫으로 한 번 스민다.
301. **배지 줄은 이름 칸 안이 아니라 윗줄 밖이다.** 이름 밑에 넣었더니 배지가 붙을 때 이름 칸이 길어져 **가운데 맞춤이던 뒤로가기와 클래스 표식이 아래로 밀렸다**(형님이 짚었다). 윗줄(`topbar__inner`) 밖으로 내면 그 줄의 키가 변하지 않으므로 둘이 제자리를 지킨다. **자리를 옮겼을 뿐 보이는 것은 같다** — 윗줄과 같은 상자(44rem·가운데)에 서고, 뒤로가기·표식·사이만큼 들여 써서 이름줄 밑에 정확히 앉는다. 셋 다 굴린 정도를 따라 줄어들므로 들여쓰기도 같은 식으로 셈한다.

### 파티 기록지에도 로그 (2026-08-14)

302. **「고친 기록」을 「로그」로 줄였다.** 형님이 정했다.
303. **파티 로그는 파티원이 다 본다 — 캐릭터와 반대다(`0021`).** 캐릭터 이력은 주인만 본다(구현 결정 371, 형님이 좁혔다) — 제 것이기 때문이다. 파티 상태는 **함께 쓰는 것**이라 누가 무엇을 고쳤는지 서로 보여야 뜻이 있다. **나만 보이는 공용 장부는 장부가 아니다.**
304. **표는 둘인데 옮기는 규칙은 하나다.** 칸 목록이 달라(`campaignChangesOf`) 뽑는 것은 갈랐지만, 우리말로 옮기는 것(`describeChange`)은 함께 쓴다 — 수는 차이까지, 목록은 들고 난 것만. 두 벌로 두면 언젠가 어긋난다.
305. **글자 칸은 무엇으로 바뀌었는지 그대로 보여 준다.** 이름·머무는 곳은 값이 짧다 — 「메모를 고쳤다」처럼 뭉뚱그리면 되짚을 수가 없다.
306. **팝업은 어디 것을 볼지만 받는다(`source`).** 캐릭터와 파티가 같은 팝업을 쓴다 — 읽어 오는 함수만 갈아 끼운다.
307. **`204`가 「지워졌다」는 뜻이 아니다.** 익명으로 DELETE를 찔러 보니 204가 왔는데, RLS가 막으면 보이는 줄이 없어 **0줄을 지우고도 204**다. 진짜 줄을 하나 심어 놓고 지워 본 뒤 그 줄이 남아 있는 것까지 봐야 확인이다(구현 결정 82를 한 겹 더).

### 모험 — 넷째 문 (2026-08-14)

308. **판을 여는 문을 대문에 낸다(`#/adventure`).** 지금까지는 행낭 안쪽에만 있었다 — 도구를 꺼내려고 들어간 김에 여는 꼴이었다. **판을 여는 것은 도구를 꺼내는 것보다 앞선 일**이다(형님이 정했다).
309. **새 표를 만들지 않는다 — 모험 하나가 전투 세션 하나다.** 판을 나누는 짜임은 `battles`가 이미 들고 있다(2026-08-08). 여기서 하는 일은 그 문을 앞에 내는 것뿐이라 마이그레이션이 없다.
310. **목록에 무엇이 오는지는 RLS가 정한다.** 내가 든 파티의 판만 보인다(`0007`) — 화면에서 거르지 않는다. **접는 것이 곧 지우는 것이므로**(구현 결정 67) 행이 있으면 열려 있는 것이고 따로 걸러 낼 조건도 없다.
311. **파티에 든 캐릭터가 하나는 있어야 문이 열린다.** 판은 파티 단위로 열리므로(구현 결정 19) 어느 파티인지 말할 수 없으면 열 데가 없다. **잠근 까닭을 카드에 적는다** — 안 적으면 고장으로 읽힌다(구현 결정 172와 같은 결).
312. **하나면 어디서, 여럿이면 몇 개(`adventureNote`).** 둘만 되어도 이름이 늘어서 카드가 무너진다 — 자세한 것은 들어가서 본다(형님이 정했다).
313. **소식 배지는 이끼색이다.** 금색은 카드의 「열기」가 이미 쓰고 있어 같은 색이면 두 가지가 같은 신호로 말한다(구현 결정 73과 같은 자리).
314. **도는 모험이 없으면 참여 쪽을 잠근다.** 눌러 봐야 빈 목록이 나오는 자리를 열어 두면 고장으로 읽힌다.
315. **대문이 서버를 본다 — 그래도 못 닿으면 그냥 선다.** 모험 소식과 「열 수 있는가」는 물어봐야 아는 것이지만, 대문이 그것 때문에 멎지는 않는다(절대 원칙 3). 배지가 없고 모험 칸이 잠길 뿐이다.
316. **목록은 스토어 하나에서 나온다.** 대문은 세기만 하고 모험 화면은 늘어놓는다 — 두 곳에서 따로 읽으면 **대문의 수와 안의 목록이 어긋난다.**

### 삭제에 이틀의 유예 (2026-08-14)

317. **되돌릴 수 없는 일은 곧바로 벌어지지 않게 한다(`0022`).** 여태 캐릭터를 거두면 그 자리에서 사라졌다 — 팝업의 5초 뜸(구현 결정 36)은 손이 미끄러지는 것을 막는 값이라 얇다. **잘못 눌렀다는 것은 대개 한참 뒤에 안다.** 지우는 표시만 해 두고 이틀 뒤에 진짜로 지운다. 그동안은 목록에 그대로 있고, 들여다볼 수 있고, 고칠 수는 없고, 언제든 되돌릴 수 있다(형님이 정한 넷).
318. **고치지 못하게 막는 것은 서버다.** 화면에서 칸을 잠그는 것은 헛손질을 줄이는 것뿐이다(구현 결정 44) — 표시된 줄은 `deleted_at` 말고 아무것도 안 바뀐다. **조용히 무시하지 않고 거절한다**(구현 결정 242). 다섯 갈래를 트랜잭션 안에서 찔러 확인했다.
319. **시각은 서버가 찍는다.** 기기 시계가 어긋나 있으면 유예가 늘거나 준다 — 언제 사라지는지는 우리가 정할 값이 아니다.
320. **지운 뒤에도 그 화면에 남는다.** 목록으로 튕겨 내면 되돌리려고 다시 찾아 들어와야 한다. 시트 머리 밑에 띠를 두고 **되돌리는 문을 그 안에 함께 낸다** — 되돌릴 수 있다는 것을 아는 자리와 되돌리는 자리가 갈리면 찾으러 다닌다.
321. **남은 시간은 올려 센다.** 1시간 1분을 「1시간」이라 적으면 있는 시간보다 적게 말하는 것이고, **아직 되돌릴 수 있는데 늦었다고 여긴다**(구현 결정 38과 같은 결). 「1일 24시간」이 되는 자리는 하루로 올린다.
322. **하루가 넘게 남았으면 시간까지 적는다.** 「2일 남음」만으로는 오늘 안에 손을 써야 하는지 알 수 없다.
323. **유예가 끝난 것은 앱을 열 때 지나가며 치운다(`sweep_deleted_characters`).** 잊힌 판을 거두는 것과 같은 짜임이다(구현 결정 72) — 사람은 이틀이 끝나기를 지켜보지 않는다. **목록을 읽기 전에 부르므로** 지난 것은 애초에 안 온다. `revoke ... from public`과 `auth.uid()` 검사를 함께 두고 익명으로 찔러 확인했다(구현 결정 80·81·82).
324. **팝업의 뜸은 5초 그대로다.** 유예가 있다고 손가락이 미끄러져도 되는 것은 아니고, **사라진 줄 모르고 이틀을 보내면 그때는 진짜로 없다.**
325. **두 표식은 형태가 달라 안정적으로 보이는 자리가 다르다.** 표식과 숫자를 조금 갈라 놓는 그 폭을 둘이 맞추려 한 것이 잘못이었다(형님이 짚었다) — 물방울은 위가 뾰족하고 아래가 불룩해 **표식을 올리고 숫자를 많이 내려야** 가운데가 차고, 별은 사방이 고르고 뿔이 위아래로 뻗어 **표식은 제자리에 두고 숫자만** 내리는 편이 안정된다. 물방울 −7%/+16%, 별 0%/+10%. **아홉 벌씩 두 판을 그려 형님이 골랐다** — 한 축씩 물어서는 답이 안 나온다(구현 결정 298-1과 같은 자리).

### 상점 — 우리가 적는 아이템 (2026-08-14)

326. **캐릭터 이름이 겹쳐도 된다.** 형님이 정했다 — 구현 결정 286을 뒤집는다. 값이 담기는 열쇠는 캐릭터 id이므로(구현 결정 75) 같은 이름이 둘 있어도 체력·덱이 섞이지 않는다; 상에서 헷갈리는 것은 사람이 알아서 할 몫이다. **파티는 그대로 막는다** — 기록지를 고를 때 가릴 길이 이름뿐이다. 견줄 목록을 안 넘기면 그 검사가 통째로 빠지게 두었다(`checkName(raw)`).
327. **남의 데이터베이스를 들이지 않고 우리가 적는다(`0023`).** 아이템 데이터를 모아 둔 저장소를 훑어 보니 큰 것(`worldhaven`)은 재사용을 명시적으로 막고 나머지도 라이선스가 없다 — **형님이 보류했다.** 실물을 손에 들고 노는 자리라 이름과 값을 옮겨 적는 것이 어렵지 않다.
328. **이름과 가격 둘뿐이다.** 슬롯·소모·해금은 자리를 두지 않는다 — 필요해지면 늘리면 되고, **비어 있는 칸은 무엇이 확인된 것인지만 흐린다.**
329. **아이템 이름도 DB에만 둔다.** 클래스 이름·특혜 글과 같은 등급이다(구현 결정 111·135) — 레포와 배포물에는 표와 정책뿐이고 마이그레이션 SQL에도 값을 안 적는다.
330. **읽는 것도 쓰는 것도 승인된 사람이다.** 클래스 수치가 관리자만 쓰는 것과 갈린다 — 상점은 놀다가 그 자리에서 한 줄 더하는 곳이라 **관리자를 부르면 판이 멎는다.** 지우는 것만 적은 사람과 관리자로 좁혔다: 남이 적어 둔 것을 아무나 지우면 옆 사람이 사려던 것이 사라진다.
331. **산 것은 초안에 담긴다.** 저장을 눌러야 남는 것은 시트의 다른 칸과 같다(구현 결정 165) — **상점에서 잘못 눌러도 취소하면 없던 일이 된다.** 골드도 초안의 값을 넘기므로 방금 산 것이 그 자리에서 빠지고, 다음 것을 살 수 있는지 그때 알 수 있다.
332. **모자라면 못 산다.** 규칙을 판정하는 것이 아니라 셈이다 — 골드가 음수가 되는 자리는 뜻이 없다. **까닭을 글자로도 적는다**(「골드 부족」): 눌러도 안 되는 채로 두지 않는다.
333. **같은 이름을 두 번 적지 못하게 서버가 막는다.** 목록이 흐려지고 어느 쪽을 산 것인지 모른다 — 화면은 그 거절을 받아 「같은 이름이 이미 있는지 보라」고 짚어 준다.
334. **아이템이 들어오는 길은 상점 하나뿐이다.** 손으로 적는 칸(「아이템을 적는다」·「더하기」)을 걷었다(형님이 정했다) — **창구를 둘로 두면 어긋난다**(구현 결정 247과 같은 결). 상점을 거치면 이름이 목록의 것과 같고 골드가 함께 빠지는데, 손으로 치면 그 둘이 다 새어 나간다. 지우는 것은 보유 목록에서 그대로 한다 — 파는 것이 아니라 **잃거나 쓴 것을 치우는 자리**라 골드가 돌아오지 않는다.
335. **상점 거래는 로그의 한 갈래다(`0024`).** 시트에서 고친 것이지만 「직접 수정」과는 다른 일이다 — 골드가 빠지고 아이템이 는 것은 손으로 맞춘 것과 **값만 같을 뿐**이고, 로그를 두는 까닭이 그 「왜」다(형님이 정했다). **고르는 자리는 여전히 없다**: 상점을 거쳐 온 것만 이 갈래가 된다(구현 결정 380).
336. **한 번 저장했다고 한 가지 일을 한 것이 아니다(`splitByShop`).** 상점에서 사고 나서 경험치도 손으로 올린 채 저장할 수 있다 — 한 줄로 남기면 골드가 왜 줄었는지 알 수 없다. **가르는 자리는 「산 뒤 · 손대기 전」의 값**이다: 상점 줄이 거기까지를 말하고 직접 수정 줄이 그 뒤부터를 말한다. 순수 함수로 떼어 표로 못박았다 — 여기가 틀리면 **기록이 거짓말을 한다.**
337. **파티 로그에도 같은 갈래를 열어 두었다.** 아직 상점을 거칠 일이 없지만 두 표가 다른 말을 쓰면 나중에 한쪽만 고치게 된다.
338. **값은 금화 그림에 수로 적는다(`Price`).** 수만 있으면 그것이 값인 줄 모른다 — 상점의 값과 시트의 아이템 값이 같은 그림으로 서야 같은 것으로 읽힌다(구현 결정 334와 같은 결). 조각이 제 스타일을 스스로 들여온다(구현 결정 356).
339. **들고 있는 것은 상점에서도 그렇다고 말한다.** 같은 것을 둘 사는 일이 없지는 않으므로 **막지 않고 몇 개인지만** 적는다.
340. **시트에 값을 붙이려면 이름으로 찾아야 한다(`costOf`).** 들고 있는 것은 이름뿐이다 — 상점 목록을 스토어 하나로 두어 **상점에서 한 줄 적으면 시트도 그 값을 바로 안다.** 목록에 없는 이름이면 값을 안 적는다(구현 결정 115).
341. **없어지는 길이 하나가 아니다(`ItemDropDialog`).** 상점에 되팔면 금화가 돌아오고, 잃거나 써 없앤 것은 그냥 사라진다 — **값이 같아도 어느 쪽인지는 사람만 안다**(형님이 정했다). 갈래가 셋이라(되돌림·그냥·취소) `ConfirmDialog`를 못 쓰고, **뜸도 두지 않는다**: 초안을 고치는 일이라 저장 전이면 언제든 물릴 수 있다.
342. **되돌려 받은 것은 산 목록에서도 한 벌 무른다.** 그래야 로그의 상점 줄이 **실제로 오간 만큼만** 말한다 — 사고 곧바로 되팔면 그 줄이 아예 안 남는다.
343. **상점 목록에서 지우는 것도 한 번 묻는다.** ×만 누르면 그 자리에서 사라졌다(형님이 짚었다) — **목록은 함께 쓰는 것**이라 남이 사려던 것이 손가락 한 번에 없어지면 안 된다. 팝업이 그 사실을 함께 적는다: 지우면 다른 사람의 상점에서도 사라지고, **이미 들고 있는 것은 그대로 남는다**(시트가 든 것은 이름이라 목록과 매이지 않는다).
344. **목록은 처음부터 모두의 것이다.** 승인된 사람이면 다 읽고 다 적는다(`0023`) — 관리자를 부르면 판이 멎기 때문이다(구현 결정 330). 좁혀 둔 것은 **지우기뿐**이다.
345. **적기 전에 먼저 찾는다 — 꼬리표를 다는 결이다(형님이 정했다).** 목록이 길어지면 같은 것을 두 번 적게 되는데, 서버가 막아도 그때는 이미 헛손질을 한 뒤다. 치는 대로 걸러 주고 **똑같은 이름이 없을 때만 「추가」가 나온다** — 부분만 맞는 것은 다른 물건이므로 그때도 나온다.
346. **값은 새로 적을 때만 팝업으로 묻는다.** 칸을 늘 세워 두면 찾는 자리가 좁아지고, 찾기만 할 때는 쓸 일이 없는 칸이다. **이름은 이미 쳤으므로 값만 받는다.**
347. **작은 팝업의 껍데기를 하나로 모았다(`MiniDialog`).** `ConfirmDialog`는 되돌릴 수 없는 일에 뜸을 들이는 것이라 갈래가 둘로 못박혀 있다 — 여기 것들은 갈래가 셋이거나(빼기) 칸을 받는다(값). 두 벌로 그리면 언젠가 어긋난다.
348. **약간의 차이로 없다고 하면 같은 것이 두 벌 생긴다(`searchFold.ts`).** 「가죽 장화」를 「가죽장화」로 쳐서 못 찾으면 **없는 줄 알고 새로 적게 되고**, 그렇게 늘어난 목록이 다시 헷갈리게 한다(형님이 짚었다). 찾을 때도 겹치는지 볼 때도 같은 눈으로 견준다 — **공백은 줄이는 것이 아니라 통째로 뺀다.**
349. **NFC로 모아 놓고 견준다.** 맥에서 친 한글은 자모가 갈려 오는 일이 있어(NFD) **눈에는 같아 보여도 글자로는 안 맞는다.**
350. **저장되는 이름은 사람이 친 그대로다.** 다듬은 값은 견주는 데만 쓴다 — 그것을 저장하면 화면에 「가죽장화」라고 뜬다.
351. **서버의 유일 인덱스도 같은 셈으로 바꿨다(`0025`).** 화면이 「없다」고 보고 적으려는 것과 서버가 「있다」고 막는 것이 갈리면, **사람은 왜 안 되는지 알 수 없다.** 공백·대소문자만 다른 것을 넣어 보고 확인했다.
352. **업적도 함께 적어 두고 골라 담는다(`0026`).** 여태 기록지에서 손으로 쳤는데 **같은 것을 사람마다 다르게 적어 두면 나중에 무엇이 무엇인지 알 수 없다.** 상점을 꼬리표 다는 결로 바꾼 것과 같은 까닭이고 **다른 것은 값이 없다는 것뿐**이다(형님이 정했다) — 그래서 「추가」를 누르면 물어볼 것 없이 곧바로 들어간다.
353. **늘어놓는 모양을 하나로 모았다(`CatalogPopup`).** 상점과 업적이 다른 것은 **머리에 무엇을 얹는가(가진 금화)와 줄 끝에 무엇을 세우는가(구매·담기)** 뿐이다 — 그 둘만 받고 나머지(찾기·추가·지우기 묻기·보유 세기)는 한 곳에서 한다. 두 벌로 그리면 언젠가 한쪽만 고치게 된다.
354. **자리 잡기와 알맹이는 여기서도 가른다(`CatalogPanel`).** `createPortal`은 `document.body`를 요구해 서버 렌더로 확인할 수 없다(구현 결정 194) — 확인하고 싶은 것은 자리가 아니라 무엇이 늘어서고 언제 「추가」가 나오는가다.
355. **업적은 여러 번 담을 수 있다.** 같은 업적을 두 번 이루는 판은 없지만 **막아 두면 잘못 뺐을 때 되돌릴 길이 없다** — 몇 개인지만 적는다.

### 몬스터 덱 (2026-08-15)

356. **덱 위젯이 누구의 것인지 고른다(`owner`).** 규칙서가 둘로 가른다 — 캐릭터는 각자 제 덱을 갖고 **몬스터는 한 덱을 함께 쓴다**(글룸헤이븐 5쪽·사자의 턱 규칙서를 그림으로 대조했다). **표준 20장은 양쪽이 같다**: +0 여섯, +1 다섯, −1 다섯, +2·−2·×2·×0 하나씩 — 우리 `STANDARD_COMPOSITION`과 칸 하나까지 같다.
357. **몬스터 덱의 열쇠는 못박은 한 낱말이다(`deckSlotKey`).** 캐릭터 id로도 인스턴스 id로도 안 된다 — **상에 하나뿐인 덱이므로 전투에 앉은 넷이 같은 자리를 봐야 한다.** 인스턴스 id로 두면 기기마다 다른 덱에서 뽑는다(구현 결정 75와 같은 자리).
358. **몬스터 덱은 퍽을 안 읽는다.** 특혜·아이템으로 바뀌는 것은 캐릭터 덱뿐이다 — 몬스터 덱은 표준에서 시작해 **저주·축복만 섞여 든다.** 그 둘을 넣고 빼는 손잡이는 아직 없다: 지금은 장수를 손으로 맞춘다.
359. **카드 홈에는 `M`이 앉는다.** 실물 카드의 그 자리는 덱 주인의 것이고 몬스터 덱에는 `M`이 박혀 있다(구현 결정 200) — 그림이 없으므로 글자로 간다(구현 결정 211과 같은 손질).
360. **라틴 한 글자면 글룸헤이븐 서체로 적는다.** 실물의 `M`이 그 서체다. **한글에는 안 붙인다** — Pirata One은 라틴 전용이라 대체 서체로 떨어진다(구현 결정 39). 글자를 보고 갈래를 정하므로 클래스 첫 글자(한글)와 `M`이 저절로 갈린다. `.sl-numeral`만 붙이면 홈 글자 규칙과 **특이도가 같아 파일 순서에 기대게 되므로** 한 줄로 못박고 크기도 함께 올렸다(그 서체는 대문자가 작게 잡혀 있다).

### 축복·저주 위젯 (2026-08-15)

361. **한 칸짜리 단추 하나다.** 하는 일은 카드를 만들어 **남의 덱에 섞어 넣는 것**이라 위젯에 설정을 두지 않는다 — 받는 쪽은 줄 때마다 고르는 것이지 위젯에 매어 둘 것이 아니다.
362. **아직 안 뽑은 카드에 섞인다(`shuffleIn`).** 규칙서 그대로다: *"shuffle a CURSE card into its **remaining** attack modifier deck."* 이미 뽑아 놓은 것은 건드리지 않는다.
363. **뽑히면 덱에서 빠진다 — 버리는 것이 아니라 없애는 것이다(`isOneShot`).** 다만 **곧바로 지우지 않고 버린 더미에 남겨 두었다가 섞을 때 걸러 낸다**(`reshuffle`) — 곧바로 지우면 방금 무엇이 나왔는지 화면에서 사라진다.
364. **값이 ×2·×0이지만 섞기가 안 붙는다.** 실물 카드에 그 표식이 없다 — 뽑히면 빠지는 카드라 섞을 일이 없다. 구현 결정 129(섞기는 값에서 나온다)에 예외를 하나 두었고, 그 예외는 표식으로 갈린다.
365. **줄 수 있는 것은 이름이 있는 덱뿐이다.** 캐릭터를 고른 덱과 몬스터 덱이다 — 캐릭터를 안 고른 덱은 열쇠가 기기마다 달라(구현 결정 75) 남에게 줄 수가 없다.
366. **아직 한 번도 안 뽑은 덱이면 그 자리에서 만든다.** 그때 쓰는 구성은 몬스터면 표준 20장, 캐릭터면 켠 특혜를 따른다 — 미리 만들어 두면 퍽으로 바뀐 덱이 표준으로 굳는다(`reveal`과 같은 까닭).
367. **수는 열까지 고른다.** 실물의 축복·저주 덱이 각각 열 장이고 한 덱에 들어갈 수 있는 수도 그만큼이다 — **규칙을 판정하는 것이 아니라 고를 수 있는 수를 그 범위로 두는 것뿐이다.**

### 봉투·상자 개봉 조건 (2026-08-15)

368. **인쇄된 캠페인 시트의 그 칸을 기록지에 옮겼다(`0027`).** 형님이 실물을 찍어 보내 주었다 — 조건이 줄로 늘어서고 줄마다 체크상자가 붙는데 하나는 상자가 열 개다(금화 기부). **글은 특혜와 같은 등급이라 레포·배포물에 안 담고 DB에만 둔다**(절대 원칙 1, 구현 결정 135) — 마이그레이션 SQL에도 값을 안 적고 관리자 화면에서 넣는다.
369. **켠 것은 줄 번호가 아니라 조건 id로 센다.** 특혜는 상자 번호를 이어 붙여 셌지만(구현 결정 136) 그 짜임은 **표를 다시 넣어 번호가 밀리면 켠 것이 딴 줄을 가리킨다** — 그래서 특혜는 통째로 갈아 끼워야 했다(구현 결정 137). 여기서는 조건마다 제 id를 두고 몇 칸 켰는지만 센다.
370. **켠 칸은 앞에서부터 찬다.** 몇 번째를 눌렀느냐가 곧 몇 칸이며, **같은 칸을 다시 누르면 그 앞까지만 남는다** — 열 칸짜리를 하나씩 되돌릴 수 있어야 한다.
371. ~~**표가 비었으면 그 칸이 아예 안 나온다.**~~ **같은 날 뒤집었다 — 형님이 「UI가 안 보인다」고 물었다.** 아무것도 안 넣어도 앱이 도는 것(절대 원칙 3)과 **왜 없는지 안 보이는 것**은 다르다(구현 결정 172와 같은 결). 칸은 서고, 비었으면 어디서 넣는지 적는다.
372. **붙여넣는 꼴은 JSON이 아니라 한 줄에 하나다(`unlockText.ts`).** 시트를 보고 옮겨 적는 자리라 손으로 치기 쉬운 편이 낫다 — 상자가 여럿인 줄만 앞에 `[10]`을 적는다. 특혜가 JSON인 것은 덱 변경이라는 표가 딸려 있기 때문이고(구현 결정 138) 여기는 글과 수 하나뿐이다.
373. **로그에는 몇 칸 켰는지만 남는다.** 어느 줄인지는 조건 글이 있어야 아는데 그 글은 DB에만 있다 — 값으로 담고 화면이 옮기는 원칙(구현 결정 370)이 여기서는 「셋 → 다섯」으로 나온다.
374. **전역 업적과 파티 업적은 다른 표다(`0028`).** 실물 시트가 둘로 갈라 적는다(형님이 짚었다). 목록 표에는 갈래만 더했고 **옛 줄은 다 파티 업적**이다 — 지금까지 적어 둔 것이 그것뿐이다. 이름이 겹치는지 보는 눈도 **갈래 안에서만** 본다: 같은 이름의 파티 업적과 전역 업적이 따로 있을 수 있다.
375. **전역 업적은 되풀이해 이룬다 — 켰다/껐다가 아니라 횟수다.** 개봉 조건에 「전역 업적 … n회 달성」이 있는 것이 근거다(형님이 짚었다). 담는 모양이 갈린다: 파티 업적은 이름의 목록, 전역 업적은 `{ 이름: 횟수 }`(개봉 조건 진행과 같은 모양).
376. **로그에는 무엇이 몇 번 늘었는지까지 적는다.** 개봉 조건은 어느 줄인지 알려면 DB의 글이 있어야 해서 수만 적었지만(구현 결정 373), **전역 업적은 이름이 곧 열쇠**라 「드레이크 +2」로 적을 수 있다.
377. **조건 글의 ☀·☾는 잠긴 클래스 봉투의 표식이다(`conditionParts.ts`).** 형님이 「크리에이터 팩에서 찾아 이모티콘을 대체하라」고 짚었고, 팩 아이콘 스물하나를 늘어놓고 대조해 **해는 11번, 월식은 7번**임을 확인했다(구현 결정 119와 같은 손질). **글은 DB에 글자로 그대로 두고 바꾸는 것은 화면뿐이다** — 저장된 것을 그림으로 고치면 표를 다시 넣을 때 어긋난다. 표식은 물들이지 않고 양피지 원반을 깐다(구현 결정 41).
378. **영문·숫자는 웬만하면 글룸헤이븐 서체다(형님이 정했다).** 「A봉투」의 A도 「77번」의 77도 그 서체로 적는다 — 한글에는 안 붙인다(구현 결정 39). 조건 글은 한 줄에 한글과 영문·숫자가 섞이므로 **낱말을 갈라 그 자리에만** 붙인다.
379. **글은 형님이 준 사진에서 내가 뽑아 넣는다.** 「너가 넣어줘야지」 — 관리자 화면은 길을 열어 둔 것이고, 실제로 채우는 일은 여기서 한다(적위병 특혜를 고친 것과 같은 자리). 확신이 낮은 줄은 표로 짚어 물었고 형님이 다섯 군데를 고쳐 주었다: A봉투·한 파티가·드레이크를 돕다·여로 이벤트·도시 기록 책자.
380. **평판과 물건값을 캐릭터 시트의 경험·골드와 같은 짜임으로 뒀다(형님이 정했다).** 읽을 때는 수만, 고칠 때 **표식 양옆에 캐럿이 돋는다**(구현 결정 364와 같은 자리) — 들여다보는 동안에는 손잡이가 자리만 차지한다. 두 값을 한 줄에 나란히 두면 **평판이 물건값을 정한다**는 것이 그림으로 읽힌다.
381. **그림 없이 수만 서는 칸을 만들었다(`.tally__n--solo`).** `.tally__n`은 표식 위에 얹히려고 절대 위치를 쓰는데 여기는 얹을 그림이 없다 — 자리 잡는 규칙을 되돌리고 크기를 올린다.
382. **`markClass`를 두 시트가 나눠 쓴다.** 오른 것은 녹색·내린 것은 붉은색인데, **같은 뜻이 화면 두 곳에서 다른 색이면 그것부터 헷갈린다**(구현 결정 255와 같은 결).
383. **CSS 규칙을 걷을 때 선택자 목록을 다시 먹었다 — 다섯 번째다.** `.sheet__rep-value`가 `.sheet__input:focus`·`:disabled`와 **한 줄에 묶여 있어** 그 규칙이 통째로 날아갔다(구현 결정 353·367). 이번에는 중괄호를 세는 파서로 걷었는데도 그랬다 — **선택자에 이름이 있으면 규칙째 지운다**는 규칙 자체가 틀렸다. 지울 때는 **그 선택자만 목록에서 빼야** 한다.

### 번영도 표와 아이템 번호 (2026-08-16)

384. **번영도별 아이템 카드 범위를 참조에 넣었다(`rules/prosperity.ts`).** 형님이 실물 시트를 찍어 보내 주었다 — **번호의 범위일 뿐 카드의 글이 아니라** 레포에 둘 수 있다(구현 결정 42·284와 같은 선). 아이템 **이름**은 여전히 DB에만 있다(`0023`).
385. **1레벨만 열넷이고 그 뒤로는 일곱씩이다.** 식이 있으므로 셈해서 내고 인쇄된 아홉 줄은 시험이 붙든다(구현 결정 285·286과 같은 손질).
386. **상점 아이템에 실물 카드 번호를 둔다(`0029`).** 번영도 표가 번호의 범위로 말하므로 이름만으로는 그 표와 이을 수가 없다. **이미 적어 둔 것은 −1**(형님이 정했다) — 「아직 안 적었다」는 뜻이며 0은 실제 번호일 수 있어 안 쓴다.
387. **같은 번호를 두 번 적지 못하게 막되 −1은 여럿이다.** 부분 인덱스로 그 자리만 비켜 둔다 — 안 적은 것이 여럿인 것은 정상이다.
388. **목록이 번호 차례로 선다.** 실물 카드가 그렇게 정렬돼 있다. **안 적은 것(−1)이 맨 앞에 모이는 것도 뜻이 있다** — 적을 것이 남았다는 표가 된다.
389. **번호는 안 적어도 된다.** 실물 카드가 손에 없으면 알 수 없고, 없는 채로도 사고파는 데 지장이 없다.

### 위대한 떡갈나무 (2026-08-16)

390. **판을 실물 그대로 늘어놓는다.** 형님이 찍어 보낸 판이 110부터 1000까지 열씩이고 오십마다 번영도 표식이 박혀 있다 — **수만 적으면 「얼마나 왔나」가 안 보인다.** 담기는 것은 쌓인 수 하나뿐이고 번영도·다음 표식은 셈해서 낸다(`rules/greatOak.ts`).
391. **남의 골드를 깎는 일이라 서버가 한 번에 한다(`0030`).** 캐릭터는 제 것만 고칠 수 있으므로(`0005`) 화면에서 파티원 넷의 골드를 각각 깎을 수 없다. 함수가 파티원인지·가진 것보다 많이 내지 않는지·**합산이 열 단위인지**를 보고 통째로 거절하거나 통째로 반영한다 — **반만 깎이는 것이 가장 나쁘다.**
392. **합산만 열 단위면 된다 — 각자는 아니어도 된다**(형님이 정했다). 판의 칸이 열씩이라 그 사이에 멈출 자리가 없다.
393. **떡갈나무의 번영도는 기록지의 번영도에 더하지 않는다.** 따로 보인다 — **어디서 온 번영도인지 갈려야 되짚을 수 있다.**
394. **해금 조건과 코드를 잇지 않는다.** 금화 100개 기부는 개봉 조건 표의 한 줄이고 그 글은 DB에만 있어 코드가 알아볼 수 없다 — **칸은 늘 서고 열리기 전에는 판이 비어 있을 뿐이다.**
395. **축복·저주는 뽑히는 순간 사라진다(형님이 짚었다).** 한동안 버린 더미에 남겼다가 섞을 때 걸러 냈는데 그러면 **버린 더미의 장수가 실물과 다르다.** 방금 무엇이 나왔는지는 뽑기가 돌려주는 것으로 화면이 크게 띄우므로 사라져도 안 보이지 않는다.
396. **번영도는 판에 적힌 수 하나다(`0031`).** 상점에 풀리는 아이템 카드가 여기서 정해지므로(`rules/prosperity.ts`) 그 범위를 칸에 함께 적는다.

### 모험은 참가자를 미리 정한다 (2026-08-16)

397. **아무 판에나 난입할 수 없다(`0032`).** 여태 「파티원이면 다 본다」였는데, 한 파티가 두 자리로 갈려 놀 수 있으므로 **지정된 캐릭터의 주인에게만** 보인다(형님이 정했다) — 옆에서 구경하다 남의 판을 굴리는 일도 그만큼 막힌다.
398. **판을 여는 일은 서버 함수가 한 번에 한다.** 보이는 자격이 참가자 줄에 달려 있어 **줄을 다 넣기 전에는 방금 만든 판이 제 눈에도 안 보인다** — 그 사이에 끊기면 아무도 못 보는 판이 남는다. 「내 캐릭터가 하나는 참가해야 한다」도 여기서 막는다.
399. **시나리오 레벨은 고른 사람들에게서 나온다.** 평균 ÷ 2 올림이 권장이고 캐럿으로 얹거나 뺀다 — 레벨은 표를 믿지 않고 경험치에서 다시 뽑는다(구현 결정 225).
400. **판을 여는 창구는 모험 화면 하나뿐이다.** 행낭의 전투 팝업에서 바로 열던 단추를 걷고 모험으로 보낸다 — 누가 앉을지와 레벨을 정해야 하므로 거기서 열 수가 없다(구현 결정 247과 같은 결).
401. **떡갈나무는 B봉투가 열려야 나온다(`0033`).** 그 조건 줄에 **표를 달아** 가른다 — **코드가 글을 뒤져 찾지 않는다**: 문구를 한 번 고치면 조용히 어긋난다. 붙여넣는 꼴에서는 맨 앞의 `*`가 그 표다.
402. **판이 열리는 것만으로 번영도 하나를 받는다**(형님이 정했다) — 금화 100개를 낸 삯이며 판에 오르기 전의 몫이라 표식과 따로 센다. 화면도 「개봉 1 + 표식 N」으로 갈라 적는다.
403. **기부는 한 단위로 오르내린다**(형님이 짚었다). 열 단위여야 하는 것은 **이번에 내는 합산**이지 각자의 몫이 아니다 — 합이 안 맞으면 단추가 잠긴다.
404. **말로 적히는 값은 작게 둔다.** 물건값의 「그대로」가 수와 같은 크기로 서면 칸을 넘고 옆 칸과 키도 안 맞는다.
405. **상점의 값·카드 번호 칸을 같은 모양으로 세웠다**(형님이 짚었다). 하나만 금화를 밖에 달고 있어 다른 물건처럼 보였다 — 금화는 이름표 안으로 들어갔다.
406. **캠페인 상태 위젯 — 읽는 위젯이다.** 시나리오 레벨 하나에서 넷이 따라 나온다(금화 환산·함정 피해·위험 지형·보너스 경험). **모험에 앉아 있으면 그 판의 레벨이 이긴다**(`0032`) — 안 앉았으면 설정의 값으로 돌고 그 사실을 화면에 적는다(어디서 온 수인지 모르면 고장으로 읽힌다).
407. **임시로 든 카드는 덱 위젯에 따로 적는다(`tempCounts`).** 퍽은 판이 끝나도 남는 구성이고 축복·저주는 이번 판에만 있다 — 섞여 보이면 **「내 덱이 왜 스물한 장이지」**가 된다(형님이 짚었다). **아직 안 뽑은 것만 센다**: 뽑힌 것은 이미 사라졌다.
408. **번영도는 레벨이 아니라 몇 칸 찼는가로 담는다(`0034`).** 형님이 실물 판을 찍어 보내 주었다 — 네모가 길게 늘어서고 문턱 칸 위에 레벨이 굵게 적힌다. **레벨은 셈해서 낸다**(4·9·15·22·30·39·50·64) — 캐릭터 레벨을 경험치에서 뽑는 것과 같은 결이다(구현 결정 223).
409. **번영도도 떡갈나무와 같은 조건으로 열린다**(형님이 정했다) — 같은 자물쇠(`opens_oak`)를 본다.
410. **문턱 아닌 칸에도 수 자리를 비워 둔다.** 줄이 들쭉날쭉하면 칸을 못 센다 — 전투 목표 체크가 자리를 늘 잡아 두는 것과 같은 손질이다(구현 결정 308).
